# 8. Testing & CI/CD

[← Back to index](SPEC.md)

---

## Test Suite

**Framework:** Jest 30.x — run with `npm test`. The script is `node --experimental-vm-modules node_modules/.bin/jest` because the KB handler tests import `unpdf` (ESM-only, wraps `pdfjs-dist`) and Jest's VM needs the flag to allow runtime `import()` calls.

| File | Focus |
|------|-------|
| `test/toolUtils.test.ts` | Shared backend helpers: extractToolDetails, extractToolOutcome, extractUsage, shortenPath, sanitizeSystemPrompt, isApiError |
| `test/backends.test.ts` | BaseBackendAdapter (including generateTitle, getMemoryDir + extractMemory default null), BackendRegistry (including shutdownAll), ClaudeCodeAdapter (metadata models including `supportedEffortLevels` per model, --model flag passthrough, --effort flag passthrough with silent drop when the selected model does not support the requested level, Opus-only `max` enforcement, Opus-4.7-only `xhigh` enforcement (passes for `claude-opus-4-7`, dropped for `claude-opus-4-6`), Haiku effort drop, --mcp-config passthrough from `mcpServers` option and omission when absent, `runOneShot` --mcp-config passthrough when mcpServers provided and omission when absent, extractMemory with frontmatter parsing and `~/.claude/projects` path resolution, git worktree canonicalization to main repo memory, getMemoryDir path resolution including empty/missing/sanitized/worktree-canonical cases), parseFrontmatter helper, resolveCanonicalWorkspacePath helper, mcpServersToClaudeConfigJson (ACP env-array to Claude Code env-object shape transform, omitted env key, multiple servers, missing args coercion). **ClaudeCodeAdapter runOneShot:** stdout success returns trimmed output, empty stdout returns empty string, stderr logged as errors, non-zero exit code throws, killed/signal throws with timeout message, stderr filtering (strips `Update available` lines), `--model` flag passthrough, `--effort` flag passthrough with silent drop for unsupported combos, timeout passthrough, `--permission-mode` flag, workingDir as cwd, `--print -p` flags always present, maxBuffer 4 MB |
| `test/kiroBackend.test.ts` | KiroAdapter metadata (including dynamic models, deprecated/internal filtering, model family detection), lifecycle (shutdown, onSessionReset), extractKiroToolDetails tool name normalization, generateSummary/generateTitle fallbacks. **One-shot ACP path (`_acpOneShot`, exercised through `runOneShot`)** via a mocked `kiro-cli acp` simulator that drives the JSON-RPC exchange: returns only the post-tool agent text (filters out pre-tool reasoning by clearing the buffer on each `tool_call`), returns concatenated `agent_message_chunk` text when no tools are called, forwards `mcpServers` into the `session/new` request, issues `session/set_model` when `model` option is set, auto-approves `session/request_permission` notifications with `allow_always`, rejects with a friendly install-instruction error on spawn ENOENT, rejects on `timeoutMs`. |
| `test/codexBackend.test.ts` | CodexAdapter metadata (id/label/icon/capabilities — `thinking: true, planMode: false, agents: false, toolActivity: true, userQuestions: false, stdinInput: false`; fallback `models` list populated immediately while background `model/list` refresh is gated behind `JEST_WORKER_ID` so no real codex processes spawn during tests; default working dir under `.codex`; custom workingDir override; extends BaseBackendAdapter; can be registered in BackendRegistry alongside ClaudeCodeAdapter and KiroAdapter; sendMessage returns `{stream, abort, sendInput}` with sendInput as a no-op no-throw). Lifecycle (`shutdown` and `onSessionReset` no-throw with no live processes; registry `shutdownAll` invokes adapter shutdown). **`extractCodexToolDetails`** (item-type-based dispatch — `commandExecution` → Bash with command preview and truncation past 60 chars, generic label without command; `fileChange` → Edit with first changed path, generic label without changes; `mcpToolCall` preserves underlying tool name with `server.tool` description; `dynamicToolCall` preserves namespace.tool; `webSearch` → WebSearch with query preview; `imageView` → Read with path; `imageGeneration` → ImageGen; unknown item types like `agentMessage` return null). **generateSummary / generateTitle fallbacks** (empty messages, empty userMessage). **`runOneShot` (codex exec via execFile mock)** — returns trimmed stdout on success, ENOENT mapped to "Codex CLI is not installed" friendly error, `model` option forwarded as `-m` flag to `codex exec`, `mcpServers` option injected via repeated `-c` flags (`mcp_servers.<name>.command="…"`, `mcp_servers.<name>.args=[…]`, `mcp_servers.<name>.env={ KEY = "…" }`) so the user's `~/.codex/` is never touched, no `-c` flags emitted when `mcpServers` is absent. |
| `test/chat.test.ts` | Chat routes: WebSocket streaming (text, tool_activity, stdin input, abort, assistant_message), WebSocket reconnection (replay buffered events, CLI survives disconnect, CLI crash buffers error, abort clears buffer, session reset clears buffer), turn boundaries, turn_complete event forwarding, tool activity persistence, **ordered contentBlocks persistence** (interleaved text/tool ordering, adjacent text chunks merged into one block, thinking included as an ordered block with legacy `thinking` field still populated, `tool_outcomes` patches the corresponding tool block, intermediate messages at `turn_boundary` carry their own `contentBlocks`, omitted when the message has no persisted blocks), parallel agent persistence, session overview aggregation, auto title update on session reset, usage event forwarding and persistence (including sessionUsage), usage stats endpoints (GET/DELETE), file upload/serve, workspace instructions, workspace context injection (discussion history pointer on new sessions, memory pointer on new sessions when memory is enabled, no memory pointer when memory is disabled, neither pointer on resumed sessions, memory content never leaks into the system prompt), workspace memory GET endpoint (empty returns `{enabled:false, snapshot:null}`, unknown workspace returns same empty shape, saved snapshot returned with `claude/` filenames), PUT `/memory/enabled` toggle round-trip and non-boolean rejection, DELETE `/memory/entries/:relpath` including path-traversal rejection, DELETE `/memory/entries` bulk clear (returns deleted count and empty snapshot, no-op returns 200 with `deleted: 0`), `memory_update` WS frame (first capture lists all files, second capture only diffs, no frame when adapter has no memory dir — gated on `setWorkspaceMemoryEnabled`), workspace knowledge base GET endpoint (returns `{enabled:false, state:<empty scaffold>}` when KB disabled, `{enabled:true, state:<persisted scaffold>}` when enabled, 404 for unknown workspace), PUT `/kb/enabled` toggle round-trip, non-boolean rejection, 404 for unknown workspace, Memory independence (toggling KB does not touch `memoryEnabled` and vice versa), `POST /workspaces/:hash/kb/raw` multipart upload (202 happy path staging an `ingesting` entry then reaching `ingested` after `waitForIdle`, 400 `KB disabled` when toggle is off, unsupported file extensions surface as `failed` with error on the state entry, 400 pre-flight rejection of legacy `.doc` with a "resave as .docx" message and no raw entry created, 400 pre-flight rejection of `.docx` uploads when pandoc is unavailable — verified hermetically by pointing PATH at an empty dir and resetting the pandoc detection cache — with an install hint that includes the pandoc.org URL), `DELETE /workspaces/:hash/kb/raw/:rawId` (cascade removal of raw file + converted dir + state entry, 404 for unknown rawId), `GET /workspaces/:hash/kb/raw/:rawId` (streams bytes with the stored mimeType, 400 for malformed rawIds, 404 for unknown rawIds), `GET /workspaces/:hash/kb/raw/:rawId/media/*` (streams a seeded media file from `converted/<rawId>/media/Slide123.jpg`, serves nested `slides/slide-001.png`, 404 for missing files, 400 for non-hex rawIds, 400 on `..%2F..%2F` path traversal), `GET /kb/libreoffice-status` shape (boolean `available`, nullable `binaryPath`, string `checkedAt`, availability/binaryPath consistency), archive/restore endpoints, **GET `/active-streams`** (returns `{ ids: [] }` when no streams are active, returns the conversation IDs currently registered in the router's in-memory `activeStreams` map and excludes idle conversations), **PATCH `/conversations/:id/unread`** (sets the `unread` flag and surfaces it on the next `GET /conversations` summary, clears it back out so it's omitted from the summary, treats an empty body as `unread: false`, returns 404 for an unknown conversation), message queue persistence (GET/PUT/DELETE against the typed `QueuedMessage` shape, persists attachments on queued messages, included in conversation response, cleared on reset/archive, PUT rejects legacy `string` entries with 400, PUT rejects attachment entries missing a `path`), model passthrough (explicit model, stored model, model update), effort passthrough (explicit effort on send, silent downgrade when model switch drops `max`, stored effort reused on subsequent sends), **`external_session` stream event** (the adapter-emitted `{ type: 'external_session', sessionId }` frame is consumed by `processStream`, persisted onto the active `SessionEntry.externalSessionId` on disk, **not forwarded to the frontend WebSocket**, and the persisted value flows back to the adapter via `SendMessageOptions.externalSessionId` on the next send — the full round-trip that makes server-restart resume work). **REST endpoints:** POST `/conversations` (creates conversation, creates with workspace, creates with model, returns 400 for empty title), PUT `/conversations/:id` (updates title, returns 404 for unknown id), DELETE `/conversations/:id` (deletes conversation, returns 404 for unknown id, concurrent delete returns 404), GET `/conversations/:id` (returns conversation with messages, returns 404 for unknown id, returns empty messages for new conversation), GET `/sessions` (lists active sessions, returns empty array when none), GET `/download` (returns 400 without conversationId, returns 404 for unknown conversation, returns markdown for valid conversation), GET `/settings` (returns current settings, returns default settings on fresh service), PUT `/settings` (updates and returns settings, returns 400 for invalid payload), GET `/backends` (returns registered backends, includes metadata), POST `/reset` (resets active session, returns 404 for unknown conversation, returns 409 when stream is active, succeeds when no active session). **Workspace file explorer:** `GET /workspaces/:hash/explorer/tree` (lists root + nested folders, hidden files are always included, dirs before files then alphabetical, traversal `..` rejected with 403, leading slash on `/etc` is stripped so absolute-looking paths are treated as relative, 404 for unknown workspace, 400 when path resolves to a file), `GET /workspaces/:hash/explorer/preview` (`mode=view` returns text JSON, 413 when the file exceeds 5 MB, `mode=download` sets `Content-Disposition: attachment`, `mode=raw` sets the inferred MIME type, traversal rejected with 403, 404 for missing file, 400 when path is a directory), `POST /workspaces/:hash/explorer/upload` (single-file happy path returns entry metadata, upload into nested folder auto-targets the folder, 409 on filename conflict without `overwrite=true`, `overwrite=true` replaces the existing file, 413 on over-limit payloads, traversal in destination `path` rejected with 403, 400 when destination resolves to a file), `POST /workspaces/:hash/explorer/mkdir` (creates a folder at root, creates a folder nested inside an existing parent, 409 on name collision with an existing file or folder, 400 on slashes/backslashes in the name, 400 on empty or whitespace-only names, 403 on parent-path traversal, 404 for missing parent), `POST /workspaces/:hash/explorer/file` (creates an empty file at root, creates inside a nested parent, accepts optional seed `content` body, 409 on collision with an existing file, 409 on collision with an existing folder of the same name, 400 on slashes in the name, 400 on empty names, 403 on parent-path traversal, 404 for missing parent, 413 when seed content exceeds the 5 MB edit limit and no file is created), `PUT /workspaces/:hash/explorer/file` (overwrites an existing text file with the `content` body and returns the post-write size, saves into nested paths, accepts an empty string to truncate, 400 on non-string content, 413 when byte length exceeds the 5 MB edit limit and leaves the file untouched, 404 for missing files, 400 when the path is a directory, 400 when the path resolves to the workspace root via a stripped leading slash, 403 on traversal), `PATCH /workspaces/:hash/explorer/rename` (file rename, directory rename, 409 on target collision without overwrite, `overwrite: true` replaces the target, 400 on root rename, 403 on traversal of either field, 404 for missing source), `DELETE /workspaces/:hash/explorer/entry` (file delete, recursive directory delete, 400 on root delete attempt, 404 for missing entry, 403 on traversal). |
| `test/knowledgeBase.handlers.test.ts` | Per-format KB handlers: **pdfHandler** (real fixture in `test/fixtures/kb/sample.pdf` — rasterizes each page to `pages/page-NNNN.png`, asserts the PNG magic bytes on disk, the `## Page N`/`![Page N](…)` sections in the thin markdown index, the absence of any extracted prose, `handler='pdf/rasterized'`, and `pageCount`/`renderedPageCount`/`rasterDpi:150` metadata; the `wordCount` key is asserted undefined because we no longer extract text), **docxHandler** (stubs `runPandoc` via `jest.spyOn` on the pandoc module — happy path with simulated `--extract-media` output verifies flattened media layout, collision-safe rename, GFM table preservation, `media/media/...` reference rewriting, `docx/pandoc` handler tag, and argument shape; no-media branch; and a rejection branch verifying pandoc errors propagate so the orchestrator can mark the entry failed), **pptxHandler** (slide text, speaker notes, embedded media, `slideCount`/`slidesToImagesRequested` metadata; a hermetic hidden-slide test that synthesizes a 3-slide in-memory pptx with `show="0"` on slide 2 and asserts that only 2 slides are emitted, renumbered 1..2, with `slideCount=2`, `totalSlideCount=3`, `hiddenSlideCount=1`, and a `"1 of 3 slides … hidden"` note in the markdown; plus a hermetic "LibreOffice missing" test that sets PATH to an empty dir and resets the detection cache to force the fallback warning path), **passthroughHandler** (markdown inlined, non-markdown text wrapped in a code fence, preserves full content of large text files without truncation, image files copied into `media/` with an inline reference, `passthroughSupports` extension matrix), **dispatch** (`pickHandler` extension-first + MIME-fallback, `ingestFile` routes real fixtures through the right handler and returns the new rasterized PDF shape, `UnsupportedFileTypeError` for unknown extensions). |
| `test/knowledgeBase.ingestion.test.ts` | `KbIngestionService` end-to-end against a real `ChatService` with a fresh `state.db`: `enqueueUpload` rejects with `KbDisabledError` when KB is off, stages a text file (raw on disk, `ingesting` row in the DB, `kb_state_update` frame emitted) and transitions to `ingested` after `waitForIdle` with `text.md`/`meta.json` written under `converted/<rawId>/`, dedupes identical content across two different filenames (second upload returns `deduped: true` and no second raw row but still adds a new location), marks unsupported file types as `failed` with `errorClass='unknown'` and the error message stored on the raw row. `deleteRaw`/`purgeRaw` cascades (raw file + converted dir + raw row + raw_locations + entries + entry dirs all removed, single `kb_state_update` frame emitted), returns `false` for unknown rawIds, and raises `KbDisabledError` when KB is off. **Substep emissions** — emits `substep` frames for converting and storing phases during ingestion so the UI can show live progress. |
| `test/knowledgeBase.db.test.ts` | `KbDatabase` unit tests against a temp-dir-backed SQLite file: schema bootstrap (`KB_DB_SCHEMA_VERSION`, root folder auto-insert, all counters zero on empty DB), `normalizeFolderPath` (accepts valid paths, rejects `..`/control chars/over-long segments), raw CRUD (`insertRaw`/`getRawById`/`getRawBySha` round-trip, `updateRawStatus` persists error class + message, `setRawHandler`/`setRawMetadata`/`setRawDigestedAt` update in place, `deleteRaw` cascades to `entries`/`entry_tags`/`raw_locations`), `raw_locations` multi-location (`addLocation` auto-creates folders, same rawId across folders listed independently, `removeLocation` + `countLocations` drive ref counting, `listLocations` sorted), folder operations (`createFolder` ancestor-chain idempotent, `renameFolder` moves subtree + rewrites `raw_locations`, rejects target collisions, refuses to drop root, `listFolderSubtree` returns deepest first), `getCounters` status bucket aggregation + pending = ingested + pending-delete, entry CRUD + tag filtering (`insertEntry`/`getEntry` returns tags sorted, `listEntries` filters by rawId/tag/folder, title substring search is case-insensitive and escapes `%`/`_` so LIKE wildcards match literally, multi-tag filter uses AND semantics (must carry every requested tag; dedupes duplicates, ignores empty strings, merges legacy single-tag param), uploaded (`raw.uploaded_at`) and digested (`entries.digested_at`) ISO date ranges filter inclusively and compose with AND, `countEntries` mirrors list filter results ignoring `limit`/`offset` pagination, limit+offset slice the ordered-by-title result set, `listAllTags` returns distinct tags with usage counts ordered by count desc then name asc, `deleteEntriesByRawId` cascades to `entry_tags`, `entryIdTaken` detects slug collisions), `listIngestedRawIds`/`listPendingDeleteRaw` status scoping, `openKbDatabase` migration (fresh DB no-op, legacy `state.json` → raw + raw_locations with re-hashed sha256 + JSON renamed to `.migrated`, mid-flight `ingesting`/`digesting` statuses snapped to `failed`), **synthesis meta** (`get`/`set` round-trips, returns null for missing keys, overwrite existing key), **synthesis topics CRUD** (`upsertTopic`/`getTopic`, upsert updates existing, `deleteTopic`, `listTopics`, `listTopicSummaries`), **entry-topic assignments** (`assignEntries`/`listTopicEntryIds`, `unassignEntries`, `listEntryTopicIds`, duplicate assignment idempotent, `entryCount` computed), **connections** (`upsertConnection`/`listConnectionsForTopic`, `removeConnection`, `listAllConnections`, `connectionCount` computed, topic delete cascades connections), **needs_synthesis column** (default 1 on new entries, `clearNeedsSynthesis`, `markAllNeedsSynthesis`, `markCoTopicEntriesStale`), **god-node detection** (empty when no outliers, flags high-entry-count topics), **wipeSynthesis** (removes topics + connections + resets meta), **getSynthesisSnapshot** (topic/connection counts, status, lastRunAt), **connection discovery queries** (`listTopicPairsBySharedEntries` — returns empty when no shared entries, finds pairs sharing entries, excludes already-connected pairs in both directions, orders by shared count descending; `listTransitiveCandidates` — returns empty when no 2-hop paths, finds A→B→C candidates, excludes pairs with direct connections, returns relationship labels for the path), **synthesis reflections** (`insertReflection` + `listReflections` returns inserted reflection, `getReflection` returns full detail with `citedEntryIds`, `getReflection` returns null for missing id, `wipeReflections` removes all reflections and citations, `listStaleReflectionIds` detects stale reflections when entry is re-digested, `deleteReflections` removes specific reflections, synthesis snapshot includes reflection counts, `wipeSynthesis` also wipes reflections). |
| `test/knowledgeBase.dreamOps.test.ts` | `parseDreamOutput` (clean JSON, markdown fences, preamble text, non-JSON warning, invalid JSON warning, missing operations array, unknown op types, field validation for create/update/merge/split/delete/assign/unassign/add_connection/remove_connection, confidence value validation, **string-aware brace scanner** — handles braces inside string values, escaped quotes inside string values, and does not cut mid-string on trailing unmatched braces), `applyOperations` (create_topic, update_topic with missing-topic warning, delete_topic, assign_entries, unassign_entries, merge_topics combines sources + reassigns entries, split_topic distributes entries + rewires connections, add_connection, remove_connection, multi-op transaction). |
| `test/knowledgeBase.dreamMarkdown.test.ts` | `regenerateSynthesisMarkdown` (index.md with topic table, per-topic markdown files with content + entries + connections, connections.md, wipes stale files on regenerate, handles empty DB). |
| `test/knowledgeBase.dream.test.ts` | `extractAffectedTopicIds` (empty operations, create/update/delete/merge/split topic ID extraction, deduplication, skips non-topic operations, mixed operations), `parseVerificationOutput` (valid JSON, markdown-fenced JSON, no-JSON warning, malformed JSON warning, missing verified array, invalid entry shape skipping, preamble text handling, **string-aware brace scanner handles braces inside string values**), `parseDiscoveryOutput` (valid accepted results with filtering rejected, no-JSON warning, missing results array warning, defaults confidence to inferred, skips entries with missing required fields, markdown-fenced JSON, all-rejected returns empty, **string-aware brace scanner handles braces inside string values**), **parseReflectionOutput** (parses valid reflections, returns warning when no JSON found, returns warning for missing reflections array, defaults invalid type to insight, skips reflections without `cited_entry_ids`, skips items missing title or content, handles markdown fenced JSON, returns empty reflections for empty array, **string-aware brace scanner handles braces inside string values**), **identifyTopicClusters** (returns empty for empty input, returns empty when all topics are singletons, identifies a single connected component, identifies two separate clusters, excludes singletons from clusters), **buildReflectionPrompt** (renders topics with summaries, renders connections with labels, renders entries with titles and body text, handles null/missing summaries gracefully, handles null/missing connection labels, handles null/missing entry body, renders mixed complete and incomplete data, handles empty arrays for each section), **KbDreamService** (isRunning returns false initially, dream sets isRunning true during execution then false after, dream calls backend with correct prompt pipeline, dream emits progress via onEmit callback, redream calls wipeSynthesis before running, dream returns error result on failure, dream cleans up isRunning on error, dream resolves settings and backend from registry, dream rejects when backend not found, dream rejects when dreaming CLI backend not configured, dream revokes KB search MCP session on completion, dream revokes KB search MCP session on error, concurrent dream calls for same workspace rejected while running, **parse-failure retry** — retries the CLI once when the parser reports a JSON-parse/extraction failure and processes the entry on the second attempt, writes raw attempt outputs to `_dream_debug/parse-failure-<phase>-<iso>.txt` when both attempts fail to parse, does **not** retry when the first attempt parses cleanly, does **not** retry when the CLI returned `null` on the first attempt and preserves the existing skip behavior), **hasParseFailure** (empty warnings → false, "JSON parse error" → true, "no JSON found"/"no JSON object found" → true, case-insensitive, structural/schema warnings like missing arrays or unknown op types → false, mixed warnings containing any parse failure → true) |
| `test/kbSearchMcp.test.ts` | KB Search MCP server factory: session lifecycle (`issueKbSearchSession` token + mcpServers config, token reuse for same session key, different tokens for different session keys, independent tokens for same workspace with different session keys, `revokeKbSearchSession` invalidates token + no-op for unknown), auth (rejects missing/invalid token), tool dispatch (rejects unknown/missing tool), `get_topic` handler (returns topic with connections + entries, error for missing topic_id, error when topic not found, error when DB unavailable), `search_topics` handler (empty for empty query, warning when no embedding config), `search_entries` handler (empty for empty query, warning when no embedding config), `find_similar_topics` handler (error for missing topic_id, warning when no embedding config), `find_unconnected_similar` handler (error for missing topic_id, warning when no embedding config, filters out already-connected topics, respects limit parameter), `kb_ingest` handler (error for missing file_path, error when file not found, error when ingestion service not provided, error when path is a directory, ingests real file via enqueueUpload with conversation-documents folder), Ollama fallback (search_topics falls back to keyword search when embedding fails, search_entries falls back to keyword search when embedding fails, find_similar_topics returns empty when embeddings unavailable). |
| `test/knowledgeBase.multiLocation.test.ts` | `KbIngestionService` multi-location (Option B) end-to-end: same bytes uploaded in two folders reuse a single raw row + single on-disk file but insert two `raw_locations` rows, same bytes at same folder with two different filenames add two locations, uploading **different** bytes into an already-occupied `(folder, filename)` slot throws `KbLocationConflictError` (→ 409), `deleteLocation` removes only the targeted slot and leaves other locations live on the same rawId, deleting the **last** location always fully purges raw + bytes + converted dir (both auto-digest ON and OFF), deleting the last location with auto-digest OFF also cleans up existing entries, `purgeRaw` unconditionally wipes every location + downstream artifacts regardless of auto-digest, `deleteLocation` returns `false` for an unknown slot so the route can 404, re-uploading same bytes after full purge creates a fresh raw. |
| `test/knowledgeBase.folders.test.ts` | Virtual folder CRUD via `KbIngestionService`: `createFolder` creates nested paths idempotently and emits a `folders: true` frame, rejects root creation, rejects path-traversal segments (`..`), rejects when KB disabled. `renameFolder` moves a subtree + rewrites every `raw_locations` row under the old prefix in one transaction (verified by listing the raw in the new folder after rename), rejects rename of or to root, rejects when KB disabled. `deleteFolder` refuses to drop a non-empty folder without `cascade: true`, cascading delete always fully purges the raw + bytes + converted dir regardless of auto-digest setting, rejects root delete, rejects nonexistent folders, rejects when KB disabled. |
| `test/knowledgeBase.embeddings.test.ts` | Embedding service unit tests: `resolveConfig` applies all defaults when no config given, applies defaults for missing fields, uses all provided values; `EMBEDDING_DEFAULTS` has expected values; `embedText` calls Ollama and returns embedding (mocked fetch), throws on non-OK response, throws on unexpected response shape, strips trailing slashes from ollamaHost; `embedBatch` returns empty array for empty input, delegates to embedText for single item, sends multiple texts in one request, throws on count mismatch; `checkOllamaHealth` returns ok:true on success, ok:false on network error, ok:false on API error. |
| `test/knowledgeBase.vectorStore.test.ts` | PGLite vector store unit tests against a temp-dir-backed PGLite DB (3-dimensional for simplicity): `upsertEntry` stores and counts entries, updates on conflict; `upsertTopic` stores and counts topics; `deleteEntry`/`deleteTopic` removes entries/topics; `vectorSearchEntries`/`Topics` returns results sorted by cosine similarity; `keywordSearchEntries`/`Topics` matches on title and summary via tsvector; `hybridSearchEntries` combines vector and keyword results (e1 ranks highest matching both); `hybridSearch` searches both entries and topics; `findSimilarTopics` excludes the query topic itself; `embeddedEntryIds`/`topicIds` returns correct ID sets; `wipeAllEmbeddings` clears data but tables remain; `setModel`/`getMeta` persist model info; reopening with different dimensions wipes existing data; vector search respects topK limit. |
| `test/knowledgeBase.digest.test.ts` | `KbDigestionService` parser + end-to-end: `parseEntries` (empty output → `[]`, single entry with inline tag array, single entry with YAML list tags, multiple entries separated by `---`, auto-derives slug from title when missing, `DigestParseError` on unterminated frontmatter, `DigestSchemaError` on missing required fields + empty body, `---` horizontal rules within body text are preserved and not treated as entry boundaries, horizontal rules in body do not break multi-entry parsing, strips leading/trailing triple-backtick code fences, skips preamble prose, lowercases frontmatter keys, tag normalization + dedup), `stringifyEntry` (deterministic frontmatter order + `schemaVersion`, quotes YAML specials, optional `uploadedAt`/`digestedAt` timestamps in frontmatter), `slugify` (url-safe lowercase), `buildDigestPrompt` (output-format rules, source context, converted-text inclusion, explicit "Root folder" label), `enqueueDigest` end-to-end against a stubbed backend adapter (writes `entry.md` with `uploadedAt`/`digestedAt` timestamps + DB rows + transitions raw status to `digested`, redigest replaces stale entries for the same raw, `-2` suffix on slug collisions within one run, `malformed_output` on parse error, `cli_error` when `runOneShot` throws, `timeout` class when the error mentions timeout, `KbDigestDisabledError` when KB is off, unconfigured Digestion CLI surfaces as a `DigestResult.error`), `enqueueBatchDigest` (digests every ingested raw and emits `digestProgress` frames ending with a `null` drain signal, returns empty array when nothing is eligible). **digestProgress aggregate progress** — single `enqueueDigest` bumps `total` and emits a progress frame; `etaMs` is absent while `done < 2` and present once `done >= 2` (with `avgMsPerItem > 0`); a mid-session enqueue into an in-flight session bumps `total` without resetting `avgMsPerItem`; `GET /kb` returns `digestProgress` mid-session and `null` after drain; crash recovery clears persisted `digest_session` rows on new `KbDatabase` construction; crash recovery flips stuck `status='digesting'` raws back to `ingested`. **Digestion session counter** — single digest emits `digestion: { active: true, entriesCreated }` then exactly one `active: false` frame with the final count; batch digest accumulates the counter across raws and closes once; counter resets after completion so a second run starts at zero; failed digests still emit a closing `active: false` frame with zero entries. **Substep emissions** — emits `substep` frames for CLI analysis and parsing phases during digestion. |
| `test/chatService.test.ts` | ChatService CRUD, messages (including toolActivity persistence), sessions, generateAndUpdateTitle, archive/restore (flag set/remove, file preservation, list filtering, search filtering, delete-after-archive), **`setConversationUnread`** (sets and clears the unread flag on the workspace-index entry — verifies the field is *deleted* (not set to false) when cleared so the on-disk file stays lean, idempotent for repeat-true and clear-already-clear, returns false for unknown id, and `listConversations()` surfaces `unread: true` on the summary row), usage tracking (addUsage with conversationUsage/sessionUsage, usageByBackend, daily ledger with backend+model dimensions, model separation, getUsage, getUsageStats, clearUsageStats, Kiro credits accumulation, contextUsagePercentage snapshot, skipLedger option, contextUsagePercentage cleared on backend change via `updateConversationBackend`, preserved when backend is unchanged, and cleared on `resetSession`), model selection (create with model, updateConversationModel, listConversations model), effort selection (create with effort, silent downgrade when requested level is unsupported, fallback to highest supported level, updateConversationModel downgrades stored effort, updateConversationEffort set/clear, listConversations includes effort), workspace storage, workspace memory (save under `files/claude/`, re-capture replaces only the `claude/` subtree while preserving `notes/`, `addMemoryNoteEntry` writes to `notes/` and refreshes snapshot, `deleteMemoryEntry` removes files and rejects path traversal, `clearWorkspaceMemory` wipes both `claude/` and `notes/` and is a no-op when empty and preserves `memoryEnabled`, `getWorkspaceMemoryEnabled` defaults to false and persists after `setWorkspaceMemoryEnabled`, `getWorkspaceMemoryPointer` returns null when memory disabled or hash empty, returns bracketed block with absolute `memory/files/` path when enabled, creates the files dir on first access, get/captureWorkspaceMemory including adapter stub happy path, no-memory fallback, and extraction errors), workspace knowledge base (`getWorkspaceKbEnabled` defaults to false and persists after `setWorkspaceKbEnabled`, KB toggle is independent of Memory toggle, `getWorkspaceKbAutoDigest` defaults to false and persists after `setWorkspaceKbAutoDigest`, `getOrInitKbDb` returns null when KB disabled (no disk touch), opens `state.db` on first access when enabled, `getKbStateSnapshot` returns an empty scaffold with zero counters + root-only folder tree for enabled workspaces with no files, `getWorkspaceKbPointer` returns null when KB disabled or hash empty, returns bracketed block with absolute `knowledge/` path when enabled, creates the `entries/` dir on first access), migration, markdown export. **Attachment + queue helpers:** `attachmentFromPath` infers `AttachmentKind` from extension (images, PDF, md, text, code, folder fallback); `parseUploadedFilesTag` splits the legacy `[Uploaded files: p1, p2]` tag off the end of a content string and returns typed `AttachmentMeta[]`, returns `null` when no tag is present or malformed; `normalizeMessageQueue` migrates legacy `string[]` entries on read (parsing any `[Uploaded files: …]` tag into typed attachments), passes through `QueuedMessage[]` unchanged, strips empty/malformed entries, and backfills missing `kind` on existing attachments via `attachmentFromPath`. |
| `test/memoryWatcher.test.ts` | MemoryWatcher: watch/unwatch/unwatchAll lifecycle, idempotence, rejects missing dirs and file paths, detects .md create/update, ignores non-.md files, debounces rapid bursts into a single callback, re-fires after debounce window closes, cancels fire when unwatched mid-debounce, multi-key independence, swallows sync + async onChange errors without crashing |
| `test/memoryMcp.test.ts` | Memory MCP server factory: `issueMemoryMcpSession` mints unique tokens with an `mcpServers` array pointing at the stub (correct command/args/env including `MEMORY_TOKEN` and `MEMORY_ENDPOINT`), reissuing a session for the same conversation+workspace returns the same token (idempotent), reissuing with a different workspace hash rotates the token, reissuing after explicit revoke mints fresh token; `extractMemoryFromSession` returns 0 when memory is disabled (no CLI call), returns 0 when CLI returns `NONE`, parses a single frontmatter entry and saves it under `source: session-extraction`, parses a multi-entry `===`-delimited response and saves all entries, swallows runOneShot errors/empty responses and returns 0. **POST /mcp/memory/notes HTTP handler:** 401 for missing token, 401 for invalid token, 400 for missing/empty content, 403 when memory disabled for workspace, 200 with filename when CLI formats a note, 200 with skipped field when CLI says SKIP, 502 when CLI returns empty output, passes type hint and tags to CLI prompt, prompt includes existing memory list when workspace has entries, prompt says "(none yet)" when workspace has no entries. **nameFromFrontmatter:** extracts unquoted name, extracts double-quoted name, falls back to typeHint when no name field, falls back to "note" when no name and no typeHint. **Edge cases:** 502 when Memory CLI throws, saves note even without frontmatter delimiters, fires WebSocket `memory_update` when WS is connected |
| `test/tabIndicator.test.ts` | Browser tab status state machine (mirrors `public/v2/src/tabIndicator.js`): idle default, `running` on stream start, returns to idle when last stream ends with tab visible, transitions to `done`/`error` only when tab is hidden, multi-conversation aggregation (stays running while any stream active), error flag sticky across concurrent streams and consumed on transition, `visibilitychange` clears `done`/`error` back to idle, running state unaffected by visibility change |
| `test/messageQueue.test.ts` | Message queue: adding, deleting, rendering, in-flight protection, pause/resume, per-conversation isolation, send button state, suspended (restored) state |
| `test/streamStore.test.ts` | v2 `StreamStore` (`public/v2/src/streamStore.js`) loaded into jsdom with a fake WebSocket: plan-exit `tool_activity` frame sets `pendingInteraction = { type:'planApproval', planContent }` + `uiState='awaiting'` and contributes to `convStates()`; plan-enter frame is ignored; `isQuestion` frame sets `pendingInteraction = { type:'userQuestion', question, options }` reading the first element of `questions`; plan/question frames are NOT pushed into the streaming message's `contentBlocks`; `error` frame clears `pendingInteraction` and flips `uiState='error'`; `done` frame preserves `pendingInteraction` and sets `uiState='awaiting'` when still set; `turn_complete` frame is a documented no-op (does not clear `streamingMsgId`, does not drop `contentBlocks`, does not touch `uiState` or `pendingInteraction`); `replay_start` frame wipes the streaming placeholder's `contentBlocks` + `content` + `pendingInteraction` + `planModeActive` (keeps `streamingMsgId` so replayed deltas land on the same placeholder) and a full replay (`replay_start` → `text` chunks → `replay_end`) rebuilds a single merged text block without duplication; `replay_start` is a no-op when no placeholder exists; `replay_end` is a pure no-op that preserves `streamingMsgId`, `contentBlocks`, and `uiState`; `respond()` POSTs to `/input` with `{ text, streamActive }` and on `mode:'stdin'` clears the interaction without sending, on `mode:'message'` clears the interaction and chains to the normal `/message` send. **Attachment + queue helpers:** `attachmentKindFromPath` infers kind from extension (image/pdf/md/text/code/file), `parseUploadedFilesTag` extracts `[Uploaded files: …]` into typed `AttachmentMeta[]` and returns `null` when no tag is present, blank `ConvState` has empty `pendingAttachments` and `queue` arrays. **Queue flow:** `enqueue` optimistically appends to `state.queue` and PUTs the full queue; `removeFromQueue` drops an index and PUTs the shortened queue; `clearQueue` empties state and DELETEs `/queue`; a `done` frame on an idle conversation (no `pendingInteraction`, no `streamError`) auto-drains the queue head — composes `[Uploaded files: …]` back onto `content` and POSTs to `/message`; a `done` frame with a `pendingInteraction` set does **not** drain the queue; `updateQueueItem` patches text + attachments and PUTs the full queue, preserves existing attachments when only `content` is patched, and is a no-op (no request) for out-of-range indices. **Stream error recovery:** `clearStreamError(convId)` nulls `streamError` and resets `uiState` (null when no pendingInteraction, `'awaiting'` when one is still set) without draining the queue; `clearStreamError(convId, { resumeQueue: true })` clears the error then calls `drainQueueIfReady` so the head of the queue POSTs to `/message` immediately; an empty queue with `resumeQueue: true` is a safe no-op (no network call); `clearAllStreamErrors()` sweeps every ConvState and nulls `streamError` + resets `uiState` in one pass (used by the silent re-auth flow so stale "SESSION EXPIRED" cards disappear after the popup closes). **Draft restore on POST failure:** when `send()`'s POST rejects (e.g. 401 from an expired session), the patch restores both `pendingAttachments` and `input` from the pre-wipe snapshot, sets `streamError`, and — because `flushDraftNow(convId)` was called synchronously before the optimistic wipe — the persisted localStorage draft (`ac:v2:draft:<convId>`) still holds the user's typed text for page-reload recovery. **Unread state:** a `done` frame on a non-active conv (per `setActiveConvId`) flips `state.unread = true`, fires `AgentApi.markConversationUnread(convId, true)`, and contributes `'unread'` to `convStates()`; a `done` frame on the *active* conv leaves `unread = false` and contributes `'idle'`; `done` with a `pendingInteraction` set yields `'awaiting'` (no unread); `done` after an `error` frame yields `'error'` (no unread). `markRead(convId)` flips the flag back, POSTs `unread:false`, and contributes `'idle'`; on a cold conv (no prior ConvState) it still creates a touched entry so the sidebar can override stale `c.unread`. `markUnread(convId)` is the inverse and works on both warm and cold convs. `subscribeGlobal` fires when a previously untouched conv first enters `convStates()`. |
| `test/graceful-shutdown.test.ts` | Server shutdown on SIGINT/SIGTERM |
| `test/sessionStore.test.ts` | Session file-store persistence |
| `test/updateService.test.ts` | Version comparison, status, trigger guards, interval management, interpreter verification, PATH setup for restart, plus standalone `restart()` method (in-progress/active-streams guards, script + double-fork shape, does not call git/npm) |
| `test/auth.test.ts` | `requireAuth` middleware: unauthenticated `/api/*` returns JSON 401, unauthenticated non-API paths redirect to `/auth/login`, authenticated passthrough, localhost bypass for both API and non-API paths. **setupAuth routes:** `/auth/login` renders login page (200 HTML), redirects authenticated users to `/`, passes basePath to template, handles missing basePath, supports custom login template, **propagates `?popup=1` into the provider button hrefs** so the OAuth round-trip carries the popup flag through to the session, **omits the popup query when not in popup mode** so normal logins get the clean URLs; `/auth/denied` renders denied page with email and allowed-email info, 200 HTML, passes basePath; `/auth/logout` destroys session and redirects to `/auth/login`, handles session destroy errors gracefully, redirects to basePath-prefixed login; OAuth routes `/auth/google` and `/auth/google/callback` are registered and respond (not 404); **`/auth/popup-done`** returns 200 HTML that posts `{ type: 'ac-reauth-ok' }` to `window.opener` via `postMessage` with `window.location.origin` as the target origin, then calls `window.close()` so the popup self-destructs after the parent tab clears its stale session banner. **`meHandler` (`GET /api/me`)** — unauthenticated non-local request returns JSON 401, authenticated request returns `{ displayName, email, provider }` for both Google (`provider === 'google'`) and GitHub (`provider === 'github'`) users, localhost-bypass request with no user object returns `200` with all three fields set to `null` so the v2 sidebar footer can render a neutral placeholder. |
| `test/claudePlanUsage.test.ts` | `ClaudePlanUsageService` (cached Anthropic `/api/oauth/usage` snapshot for the Claude Code chip tooltip): credential resolution (prefers `~/.claude/.credentials.json` over macOS Keychain, falls back to Keychain only when file is missing, ignores Keychain on non-darwin), OAuth fetch (sends `Authorization: Bearer`, `anthropic-beta: oauth-2025-04-20`, and `User-Agent` headers; parses the `account`, `rateLimits` (including codename buckets like `seven_day_omelette`), and `extra_usage` block with `currency`), refresh throttle (respects `REFRESH_MIN_INTERVAL_MS` 10-min floor between successful fetches; `maybeRefresh` is a no-op inside the window; `force=true` bypasses the floor), in-flight coalescing (concurrent `maybeRefresh` calls share a single network request), failure handling (network error sets `lastError` but keeps the previous `fetchedAt`/data so the frontend shows last-known values; `stale` flips to true when `Date.now() - fetchedAt > STALE_AFTER_MS`), disk persistence (`_persist` writes to `state/plan-usage.json`; `init` restores from disk on boot so the chip renders immediately; missing/corrupt file yields an empty cache without throwing), token-expiry guard (refuses to fetch when `expiresAt - now < EXPIRY_BUFFER_MS`, exposes `lastError: 'credential_expired'`). |
| `test/kiroPlanUsage.test.ts` | `KiroPlanUsageService` (cached `AmazonCodeWhispererService.GetUsageLimits` snapshot for the Kiro chip tooltip, built against a real SQLite auth DB seeded by the `createKiroDb(dbPath, opts)` helper): `init` loads the empty shape when no cache file exists and restores a previously-persisted snapshot on boot; `getCached` returns the in-memory snapshot with `stale` derived from `Date.now() - fetchedAt > STALE_AFTER_MS`; `_refresh` happy path opens the DB read-only (`kirocli:odic:token` + `api.codewhisperer.profile` rows), POSTs to `https://q.us-east-1.amazonaws.com/?profileArn=…&origin=KIRO_CLI` with JSON-1.0 headers + bearer token, normalizes the response into `KiroUsageData`, writes it to `data/kiro-plan-usage.json`, and flips `stale` false; `maybeRefresh` respects the `REFRESH_MIN_INTERVAL_MS` 10-min floor (tracked on *attempt*, not success); in-flight coalescing (concurrent `maybeRefresh` calls share the same promise); token-expired guard (refuses to fetch when `expiresAt - now < EXPIRY_BUFFER_MS = 30s`, sets `lastError: 'token-expired'` without hitting the network); DB-missing error path (`{fileMustExist:true}` + empty temp dir surfaces `kiro-cli DB unavailable at <path>: …` while preserving any prior snapshot); missing `auth_kv` token row → `kiro-cli auth_kv missing access token`; missing `state` profile row → `kiro-cli state missing profile`; HTTP 401 preserves prior `fetchedAt`/`usage` and sets `lastError` to `GetUsageLimits 401: …`; network error preserves prior snapshot with verbatim error message in `lastError`. |
| `test/utils.atomicWrite.test.ts` | `atomicWriteFile` (`src/utils/atomicWrite.ts`): writes a new file; replaces an existing file in full; 20 concurrent writers converge to one parseable JSON payload (no torn byte-interleaving); cleans up the sibling `.tmp.` file when `rename` fails (e.g. missing parent dir); 50 parallel reads concurrent with an atomic write only ever observe one of the two valid payloads, never a partial mix. |
| `test/utils.keyedMutex.test.ts` | `KeyedMutex` (`src/utils/keyedMutex.ts`): same-key operations run sequentially in FIFO order (proven by interleaving deferreds and flushing microtasks via `setImmediate`); different keys run concurrently; `run` returns the value produced by `fn`; a thrown error in one call does not block subsequent calls on the same key; a synchronous throw inside `fn` still rejects `run()` and releases the lock; the internal `_tails` map entry is removed once the chain drains and remains present while work is pending; serialized read-modify-write on a shared counter across 50 concurrent `run` calls reaches the exact expected total (no lost updates). |
| `test/chatService.concurrency.test.ts` | Regression guard for the Apr 2026 workspace `index.json` corruption bug (concurrent `fsp.writeFile` → byte-interleaved file → JSON.parse crash loop). Against a real `ChatService`: 30 concurrent `createConversation` calls on one workspace all persist, the final index parses cleanly, and every returned id is present; 20 concurrent `renameConversation` calls across distinct conversations in one workspace all retain their new titles; interleaved creates + renames on the same workspace converge with the correct conversation count and the seed entry's title matching one of the issued values (proves no torn mix); two different workspaces run concurrent writes without cross-contamination (per-hash `_indexLock`, not a global lock); 100 parallel readers against the live `index.json` during 20 concurrent writes never observe a torn file. |

## CI/CD

**Test workflow** (`.github/workflows/test.yml`): Runs on PRs to `main`. Steps: checkout, Node.js 18 setup, `npm ci`, `npm test`.

**Version bump** (`.github/workflows/version-bump.yml`): Runs on push to `main`. Skips `chore: bump version` commits. Steps: `npm version patch --no-git-tag-version`, commit, tag, push.
