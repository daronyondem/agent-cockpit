# Context Map Specification

This file is retained only for historical ADR/path compatibility.

Context Map was replaced by [Workspace Context](spec-workspace-context.md). The
current implemented feature is markdown-first operating memory under
`workspaces/{hash}/workspace-context/`; it has no graph database, no candidate
review queue, and no Context Map MCP server.
