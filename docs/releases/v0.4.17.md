# Agent Cockpit v0.4.17

## Shipped For Users

- Fixed Windows installs so the Agent Cockpit server no longer leaves a Node.js console window visible on the desktop.

## Developer Details

- Changed the Windows PM2 ecosystem config to monitor `bin\run-agent-cockpit.ps1` through `powershell.exe -WindowStyle Hidden` instead of spawning `node.exe` directly.
- Added Windows runner generation to both fresh installer runs and production self-update config rewrites so updated installs keep the hidden-process launch path.
- Kept PM2 ownership, install-local `PM2_HOME`, scheduled-task startup, and private Node runtime behavior unchanged.
- Updated Windows installer/update tests and deployment/testing specs for the hidden runner path.

## Verification

- `npm test -- --runInBand test/windowsInstallerScript.test.ts test/updateService.test.ts -t "Windows|windows|PM2 config"`
- `npm run typecheck`
- `npm run maintainability:check`
- `npm run spec:drift`
- `npm run adr:lint`
- `npm run release:notes -- --version 0.4.17 --out /tmp/agent-cockpit-release-notes-v0.4.17.md`
- `npm run release:package -- --version 0.4.17 --source-ref main --commit local --out-dir dist/release-test-0.4.17`

## Source Links

- Previous release: https://github.com/daronyondem/agent-cockpit/releases/tag/v0.4.16
- Compare: https://github.com/daronyondem/agent-cockpit/compare/v0.4.16...v0.4.17
- Pull requests: none
- Issues: none
