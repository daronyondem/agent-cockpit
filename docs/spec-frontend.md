# 6. Frontend Behavior

[← Back to index](SPEC.md)

---

**Files (default UI — V2):** `public/v2/index.html`, `public/v2/src/*.{jsx,js,css}`

**Files (legacy UI — V1, retained as fallback):** `public/index.html`, `public/js/*.js` (11 ES modules), `public/styles.css`

## V2 — Default Frontend

**Paths:**
- `/` — redirects to `/v2/` (handled in `server.ts`).
- `/v2/` — real app entry (`public/v2/index.html` → `shell.jsx`). The default UI.
- `/v2/deck.html` — static mockup slide deck (all 16 screens from the design handoff, navigable with ←/→). Preserved during the rewrite so the target design can be referenced in-place; driven by `public/v2/src/app2.jsx`.
- `/legacy/` — redirects to `/index.html` (the V1 UI — see [§ Legacy UI (V1)](#legacy-ui-v1) below).

V2 is the production UI. The legacy vanilla-JS UI at `/index.html` is preserved as a fallback during the cutover and will be removed once V2 proves itself; `/v2/deck.html` keeps the full design deck around as reference. All paths are behind `requireAuth`. The V2 bundle is a React 18 + Babel Standalone prototype (no build step — see the production build issue on GitHub). Source tree:

- `public/v2/index.html` — real app entry. Loads React/ReactDOM/Babel, `marked` (markdown parser), and `DOMPurify` (HTML sanitizer) from unpkg, the four stylesheets (`tokens.css`, `app.css`, `dialog.css`, `tooltip.css`), then `api.js`, `streamStore.js`, `planUsageStore.js` (Claude Code account plan usage singleton — see below), `kiroPlanUsageStore.js` (Kiro account plan usage singleton — see below), `codexPlanUsageStore.js` (OpenAI Codex account plan usage singleton — see below), `tabIndicator.js` (plain JS, self-initialising — mounts the browser-tab favicon-dot subscriber on top of `StreamStore.subscribeGlobal`), `icons.jsx`, `dialog.jsx`, `toast.jsx`, `tooltip.jsx`, `chip-renderers.jsx`, `primitives.jsx`, `screens/kbBrowser.jsx` (the real KB Browser), `screens/filesBrowser.jsx` (the real Files browser), `screens/settingsScreen.jsx` (the real Settings screen), `folderPicker.jsx`, `workspaceSettings.jsx`, `sessionsModal.jsx`, `updateModal.jsx`, and `shell.jsx`. Load order matters: `dialog.jsx` must come after `icons.jsx` (uses `Ico.*`) and before `shell.jsx` / the screens (which call `useDialog`); `toast.jsx` must come before `shell.jsx` and the screens that call `useToasts`; `tooltip.jsx` must come before `primitives.jsx` / `shell.jsx` / the screens (which may mount `<Tip>`); `settingsScreen.jsx` must come before `shell.jsx` because shell references `<SettingsScreen>`; `updateModal.jsx` must come before `shell.jsx` because shell references `<UpdateModal>` and `<RestartOverlay>`; `workspaceSettings.jsx` must come before `shell.jsx` because shell references `<WorkspaceSettingsModal>`; `sessionsModal.jsx` must come before `shell.jsx` because `ChatLive` references `<SessionsModal>`. Does **not** load the mock deck screens (`screens/kb.jsx`, `screens/files.jsx`, and the other `screens/*.jsx` stubs) — those live only in `deck.html`. Each script/stylesheet URL carries a `?v=N` cache-buster — bump when any of the imported JSX/JS/CSS files change so browsers fetch the new version instead of a cached copy.
- `public/v2/deck.html` — slide deck entry. Loads the same base bundle plus `screens/*.jsx` and `app2.jsx` (the `SLIDES` scaffold + `Frame`/`Tweaks` overlay).
- `public/v2/src/tokens.css`, `public/v2/src/app.css`, `public/v2/src/dialog.css`, `public/v2/src/tooltip.css` — design tokens + cockpit chrome styles + anchored-popover dialog styles + tooltip primitive styles. Driven by `data-theme` / `data-density` / `data-radius` / `data-direction` attributes on the root element.
- `public/v2/src/api.js` — CSRF-aware fetch helper exposed as `window.AgentApi` (`apiUrl`, `chatUrl`, `chatWsUrl`, `fetch`, `listConversations`, `createConversation`, `restoreConversation`, `getVersion`, `getUpdateStatus`, `checkVersion`, `triggerUpdate`, `browseDir`, `mkdirDir`, `rmdirDir`, `getBackendsCached`, `setSessionExpiredHandler`, `kb`, `explorer`, `settings`, `conv`). The conversation list is no longer cached at the API layer — `StreamStore.getConvList()`/`subscribeConvList()`/`loadConvList()` own that state instead, so callers that need the list subscribe to the store rather than calling a cache helper. The four version helpers back the sidebar version indicator: `getVersion()` fires `GET /api/chat/version` (returns `{ version, remoteVersion, updateAvailable }` — the current server version plus whatever remote/update flags the background updater has cached), `getUpdateStatus()` fires `GET /api/chat/update-status` (read-only poll — safe to run every 5 min), `checkVersion()` fires `POST /api/chat/check-version` (forces a fresh upstream `git ls-remote`), and `triggerUpdate()` fires `POST /api/chat/update-trigger` (the server pulls, installs, and pm2-restarts; the in-flight fetch may reject with `TypeError: Failed to fetch` because the process dies mid-request — the UI treats that shape as success, same as V1). `restoreConversation(id)` fires `PATCH /api/chat/conversations/:id/restore` and is the inverse of `StreamStore.archive(convId)` — used by the sidebar's archive-view per-row restore button. `AgentApi.conv` bundles the per-conversation helpers driving the composer attachment chips and the Sessions modal: `uploadFile(convId, file, onProgress?, onXhr?)` (XHR-based single-file upload to `POST /api/chat/conversations/:id/upload` — `onProgress(loaded, total)` feeds the chip progress bar, `onXhr(xhr)` hands the caller the raw XHR so it can abort in-flight uploads when the user removes the chip, and the promise resolves with the first entry from the server's `{ files: [{name, path, size}] }` payload), `deleteUpload(convId, filename)` (`DELETE /api/chat/conversations/:id/upload/:filename` — fired when the user removes an already-uploaded chip), `getSessions(convId)` (`GET /api/chat/conversations/:id/sessions` → `{ sessions: SessionHistoryItem[] }` — list of past sessions with `number` / `startedAt` / `endedAt` / `messageCount` / `summary` / `isCurrent`; drives the Sessions modal's list view), `getSessionMessages(convId, sessionNumber)` (`GET /api/chat/conversations/:id/sessions/:num/messages` → `{ messages: Message[] }` — persisted messages for a past session; the Sessions modal detail view skips this fetch and reuses the in-memory `currentMessages` when the user opens the session they're already viewing), and `sessionDownloadUrl(convId, sessionNumber)` (URL builder only — pointed at `GET /api/chat/conversations/:id/sessions/:num/download` for `window.open`, the server returns markdown with `Content-Disposition: attachment`). `getClaudePlanUsage()` fires `GET /api/chat/plan-usage` and resolves to the cached snapshot `{ fetchedAt, planTier, subscriptionType, rateLimits, lastError, stale }` — consumed by `window.PlanUsageStore` (see below). The endpoint never forces a refresh; the server throttles the actual Anthropic `/api/oauth/usage` fetch at one every 10 min behind the route. `getKiroPlanUsage()` fires `GET /api/chat/kiro-plan-usage` and resolves to the parallel cached snapshot `{ fetchedAt, usage, lastError, stale }` — consumed by `window.KiroPlanUsageStore` (see below). Same 10-min server-side throttle and same never-force-refresh contract, but the underlying call is a direct JSON-1.0 POST to `AmazonCodeWhispererService.GetUsageLimits` using the bearer token the server reads read-only from the kiro-cli SQLite auth store. `getCodexPlanUsage()` fires `GET /api/chat/codex-plan-usage` and resolves to the third cached snapshot `{ fetchedAt, account, rateLimits, lastError, stale }` — consumed by `window.CodexPlanUsageStore` (see below). Same 10-min server-side throttle and same never-force-refresh contract; the underlying refresh spawns a one-shot `codex app-server` and calls `account/read` + `account/rateLimits/read` over JSON-RPC, then kills the process. `getBackendsCached()` resolves to the `BackendMetadata[]` array from `GET /api/chat/backends` (the registry is effectively static so one fetch per session is plenty; every `<ChatLive>` mount consults it for the composer pickers). `chatWsUrl(convId)` derives the WebSocket URL from the same base as REST (same protocol swap ws↔wss), so reverse-proxy deployments that serve `/v2/` under a subpath pick up the prefix automatically. `AgentApi.kb` bundles the KB endpoint helpers consumed by the v2 KB Browser. Reads: `getState(hash, {folder, limit, offset})` (extra params optional — defaults to root folder, 500-row page), `getEntries(hash, {search, tags, limit, offset, folder, rawId, ...})`, `getEntry(hash, entryId)`, `getTags(hash)`, `getSynthesis(hash)`, `getTopic(hash, topicId)`, `getReflections(hash)`, `getReflection(hash, reflectionId)`, `rawDownloadUrl(hash, rawId)` (URL builder only, no fetch), `rawMediaUrl(hash, rawId, mediaPath)` (URL builder for the per-raw media endpoint — splits `mediaPath` on `/`, `encodeURIComponent`s each segment, and rejoins so embedded slashes stay structural), and `pandocStatus()` (top-level `/api/chat/kb/pandoc-status` — not workspace-scoped — drives the install banner above the Raw tab), and `libreOfficeStatus()` (top-level `/api/chat/kb/libreoffice-status` → `{ available: boolean, version?: string }`; consumed by the Settings screen's KB tab to gate the "Convert PPTX slides to images" toggle and to surface a status pill). Mutations: `setAutoDigest(hash, enabled)` (`PUT /kb/auto-digest`), `digestRaw(hash, rawId)` (`POST /kb/raw/:rawId/digest`), `digestAll(hash)` (`POST /kb/digest-all`), `deleteRaw(hash, rawId, folder?, filename?)` (when both `folder` and `filename` are passed it deletes only that location, else fully purges the raw), and `uploadRaw(hash, file, folder?, onProgress?)` (XHR-based single-file upload that resolves with the server's `{ entry, deduped, addedLocation }` payload and surfaces progress via the `onProgress(loaded, total)` callback; mirrors the `explorer.upload` pattern), `createFolder(hash, folderPath)` (`POST /kb/folders`, idempotent), `renameFolder(hash, fromPath, toPath)` (`PUT /kb/folders`), `deleteFolder(hash, folderPath, cascade)` (`DELETE /kb/folders?folder=…&cascade=…`; `cascade` is always passed `true` from the V2 UI because the confirm copy already warns the user about nested content), `getEmbeddingConfig(hash)` (`GET /kb/embedding-config` → `{ embeddingConfig: {model?, ollamaHost?, dimensions?} | null }`), `setEmbeddingConfig(hash, cfg)` (`PUT /kb/embedding-config` with `{ model, ollamaHost, dimensions }` → `{ embeddingConfig: <result> }`), and `embeddingHealth(hash)` (`POST /kb/embedding-health` → `{ ok: boolean, error?: string }`; runs server-side via `checkOllamaHealth(cfg)`). `AgentApi.explorer` bundles the workspace file explorer helpers consumed by the v2 Files Browser: `tree(hash, relPath)`, `preview(hash, relPath)` (always `mode=view`), `rawUrl(hash, relPath)` / `downloadUrl(hash, relPath)` (URL builders for `mode=raw` image tags and `mode=download` links), `mkdir(hash, parent, name)`, `createFile(hash, parent, name, content)`, `saveFile(hash, relPath, content)`, `rename(hash, from, to, overwrite)`, `deleteEntry(hash, relPath)`, and `upload(hash, destPath, file, overwrite, onProgress)` (XHR-based single-file upload that resolves with the server's `{ ok, entry }` payload and surfaces progress via the `onProgress(loaded, total)` callback). `AgentApi.settings` bundles the global app-settings helpers consumed by the v2 Settings screen: `get()` (`GET /settings` → full `Settings` object with all defaults filled in), `save(settings)` (`PUT /settings` with the full merged shape — server returns the persisted result), `backends()` (`GET /backends` → `{ backends: BackendMetadata[] }` with nested `models[].supportedEffortLevels`), `usageStats()` (`GET /usage-stats` → `{ days: [{ date, records: [{ backend, model, usage }] }] }`), `clearUsageStats()` (`DELETE /usage-stats`), and `restartServer()` (`POST /server/restart` — server replies 200 on success or 409 if any conversation stream is currently active). Errors include a `.status` field on the thrown `Error` so the UI can branch on 409 conflicts without re-parsing the response body; the 401 path still triggers the session-expired handler. Each JSON helper JSON-parses the response. `AgentApi.workspace` bundles the per-workspace settings helpers consumed by the Workspace Settings modal: `getInstructions(hash)` (`GET /workspaces/:hash/instructions` → `{ instructions }`), `saveInstructions(hash, instructions)` (`PUT /workspaces/:hash/instructions`), `getMemory(hash)` (`GET /workspaces/:hash/memory` → `{ enabled, snapshot: { capturedAt, files: [{ filename, type, name, description, content }] } | null }`), `setMemoryEnabled(hash, enabled)` (`PUT /workspaces/:hash/memory/enabled` with `{ enabled }`), `deleteMemoryEntry(hash, relPath)` (`DELETE /workspaces/:hash/memory/entries/:relPath` — server broadcasts a `memory_update` WS event to any connected streams so open memory panels refresh in lockstep), `clearMemory(hash)` (`DELETE /workspaces/:hash/memory/entries` — same WS broadcast, but with `changedFiles: []`), `getKb(hash)` (`GET /workspaces/:hash/kb` → `{ enabled, state }` — the modal only reads `enabled`, the full snapshot drives the KB Browser), and `setKbEnabled(hash, enabled)` (`PUT /workspaces/:hash/kb/enabled`). All helpers go through `chatFetch` so CSRF + session-expired handling stays consistent.
- `public/v2/src/streamStore.js` — per-conversation stream store exposed as `window.StreamStore`. Holds one `ConvState` entry per conversation the user has visited in the current tab (`{ convId, conv, messages, input, sending, streaming, streamError, streamingMsgId, usage, loaded, loadError, ws, wsOpening, uiState, pendingInteraction, respondPending, composerBackend, composerModel, composerEffort, pendingAttachments, queue, resetting }`) with its own WebSocket, accumulated messages, and streaming flags. `uiState` is one of `'streaming' | 'awaiting' | 'error' | null`; `pendingInteraction` is either `{ type: 'planApproval', planContent }` or `{ type: 'userQuestion', question, options }` or `null`. The three `composer*` fields hold the user's picker selections — hydrated from the conv record on first load (`data.backend/model/effort`) and sticky once the user touches them, so flipping convs and back doesn't rewind the pickers. `pendingAttachments` is an array of `PendingAttachment` entries (`{ id, file, status: 'uploading'|'done'|'error', progress, result: AttachmentMeta | null, xhr, error }`) driving the composer's attachment tray — each entry tracks one file's lifecycle from user-drop through server-upload to final send. `result` holds the typed `AttachmentMeta` returned by `POST /upload` (including server-inferred `kind` and optional `meta` sublabel like "12 pages") so the chip can render without a second fetch. The tray clears on successful POST `/message` and is restored on POST failure. `queue` is the client mirror of the persisted `messageQueue` (array of `QueuedMessage` — see `spec-data-models.md`); the store auto-drains the queue head on every `done` frame when the conversation is idle (no `pendingInteraction`, no `streamError`). Public surface: `getState(convId)`, `load(convId)`, `ensureWsOpen(convId)`, `send(convId, text)`, `respond(convId, text)` (delivers an answer to the pending plan/question interaction — see *Plan approval & clarifying questions* below), `setInput(convId, value)`, `setComposerBackend(convId, value)` / `setComposerModel(convId, value)` / `setComposerEffort(convId, value)` (composer picker setters — values flush to the server with the next POST `/message`, not immediately), `addAttachments(convId, files)` / `removeAttachment(convId, attachmentId)` / `clearPendingAttachments(convId)` (tray setters — `addAttachments` kicks off one XHR upload per file, `removeAttachment` aborts in-flight uploads and `DELETE`s already-uploaded ones from the artifacts dir, `clearPendingAttachments` empties the tray without deleting the uploads — used when the user enqueues a message and the attachments are detached onto the queue entry). Queue helpers: `loadQueue(convId)` (GET `/queue`), `enqueue(convId, content, attachments)` (optimistic append + PUT), `removeFromQueue(convId, index)`, `reorderQueue(convId, fromIndex, toIndex)`, `updateQueueItem(convId, index, patch)`, `clearQueue(convId)` (DELETE). Backwards-compat helpers exposed for legacy transcript rendering: `parseUploadedFilesTag(content)` (returns `{ content, attachments } | null`), `attachmentKindFromPath(path)`. Lifecycle: `reset(convId) → Promise<boolean>` (sets `state.resetting = true` for the duration of the call so `ChatLive` can surface an in-feed "Archiving session…" progress bubble — the server-side reset archives the session, captures memory, and re-initialises which can take several seconds; resolves `true` on success so callers can fire a success toast, `false` when the conv is streaming/sending/already-resetting or the POST fails), `archive(convId)`, `closeWs(convId)`, `subscribe(convId, listener)` (returns unsubscribe), `subscribeGlobal(listener)` (fires whenever any conv's `uiState` transitions — used by the sidebar), `convStates()` (returns the compact `{ [convId]: 'streaming' | 'awaiting' | 'error' }` map consumed by `<Sidebar>`). React views read these through the `useConversationState(convId)` / `useConvStates()` hooks in `shell.jsx`. Because state lives *above* the `<ChatLive>` view, switching conversations does **not** abort an in-flight stream: the prior conv keeps collecting frames, the sidebar lights up every row whose `uiState` is non-null, and switching back to the conv immediately shows the accumulated messages. WebSockets are opened lazily on first visit and left open for the session (the server's 60 s grace buffer + replay handles transient drops); `archive(convId)` is the only path that closes the WS and removes the entry.
- `public/v2/src/planUsageStore.js` — Claude Code account plan usage singleton exposed as `window.PlanUsageStore`. Plain JS (not JSX), loaded after `streamStore.js` and before `tabIndicator.js`. Holds one `cached` snapshot (`{ fetchedAt, planTier, subscriptionType, rateLimits, lastError, stale } | null`) and a `Set<Fn>` of subscribers. Public surface: `get()` (returns the cached snapshot or `null` before the first fetch), `refresh()` (coalesces concurrent calls — returns the shared in-flight promise; on resolution updates `cached` and fans out to every subscriber via `subscriber(cached)`; preserves `cached` on fetch failure so the UI can keep rendering the prior snapshot), `subscribe(fn)` (returns an unsubscribe). The fetch goes through `AgentApi.getClaudePlanUsage()`; the server-side 10-min throttle behind `/api/chat/plan-usage` means calling `refresh()` on every chip mount + every Claude Code `done` frame is safe and cheap. `streamStore.js` calls `window.PlanUsageStore.refresh()` from the `done` frame handler whenever the completing conversation's backend is `claude-code`; other backends never trigger a refresh. The `ContextChip` in `shell.jsx` is the only UI consumer — it subscribes on mount (only for Claude Code conversations) and forwards the snapshot into `ClaudeCodeTokenCard` via the renderer registry's opts bag.
- `public/v2/src/kiroPlanUsageStore.js` — Kiro account plan usage singleton exposed as `window.KiroPlanUsageStore`. Plain JS (not JSX), loaded right after `planUsageStore.js` and before `tabIndicator.js`. Mirrors `planUsageStore.js` shape: holds one `cached` snapshot (`{ fetchedAt, usage, lastError, stale } | null`) and a `Set<Fn>` of subscribers. Public surface: `get()` (returns the cached snapshot or `null` before the first fetch), `refresh()` (coalesces concurrent calls — returns the shared in-flight promise; on resolution updates `cached` and fans out to every subscriber via `subscriber(cached)`; preserves `cached` on fetch failure so the UI keeps rendering the prior snapshot), `subscribe(fn)` (returns an unsubscribe). The fetch goes through `AgentApi.getKiroPlanUsage()`; the server-side 10-min throttle behind `/api/chat/kiro-plan-usage` means calling `refresh()` on every chip mount + every Kiro `done` frame is safe and cheap. `streamStore.js` calls `window.KiroPlanUsageStore.refresh()` from the `done` frame handler whenever the completing conversation's backend is `kiro`; other backends never trigger a refresh. The `ContextChip` in `shell.jsx` subscribes on mount (only for Kiro conversations) and forwards the snapshot into `KiroTokenCard` via the renderer registry's opts bag.
- `public/v2/src/codexPlanUsageStore.js` — OpenAI Codex account plan usage singleton exposed as `window.CodexPlanUsageStore`. Plain JS (not JSX), loaded right after `kiroPlanUsageStore.js` and before `tabIndicator.js`. Mirrors `planUsageStore.js` / `kiroPlanUsageStore.js` shape: holds one `cached` snapshot (`{ fetchedAt, account, rateLimits, lastError, stale } | null`) and a `Set<Fn>` of subscribers. Public surface: `get()` (returns the cached snapshot or `null` before the first fetch), `refresh()` (coalesces concurrent calls — returns the shared in-flight promise; on resolution updates `cached` and fans out to every subscriber via `subscriber(cached)`; preserves `cached` on fetch failure so the UI keeps rendering the prior snapshot), `subscribe(fn)` (returns an unsubscribe). The fetch goes through `AgentApi.getCodexPlanUsage()`; the server-side 10-min throttle behind `/api/chat/codex-plan-usage` means calling `refresh()` on every chip mount + every Codex `done` frame is safe and cheap. `streamStore.js` calls `window.CodexPlanUsageStore.refresh()` from the `done` frame handler whenever the completing conversation's backend is `codex`; other backends never trigger a refresh. The `ContextChip` in `shell.jsx` subscribes on mount (only for Codex conversations) and forwards the snapshot into `CodexTokenCard` via the renderer registry's opts bag.
- `public/v2/src/tabIndicator.js` — browser-tab favicon-dot indicator for v2. Plain `<script>` (not JSX) loaded after `streamStore.js` and before the Babel-transpiled scripts; exposes `window.TabIndicator.init()` and auto-initialises in its IIFE. Reads state from `StreamStore.convStates()` via `StreamStore.subscribeGlobal`; the semantics match the V1 module described under *Browser Tab Status Indicator* below (identical four-state machine, identical favicon canvas rendering — 64 px canvas, 22 % dot radius, white halo, same blue/green/red palette — with the same sticky error flag and `visibilitychange` auto-clear). The only source-level difference is the data source: instead of V1's `state.chatStreamingConvs` + explicit `chatTabOnStreamChange({ error })` calls, the v2 module diffs the previous and current error-ID sets from `convStates()` to detect newly-errored conversations and recomputes from scratch on every global subscription fire.
- `public/v2/src/icons.jsx` — shared icon functions on `window.Ico` (each takes a size, returns an SVG).
- `public/v2/src/dialog.jsx` — reusable anchored-popover dialog system. Replaces native `window.alert` / `window.confirm` / `window.prompt` across the v2 app. Exports `window.Dialog` (low-level primitive), `window.DialogProvider` (wraps the React tree, hosts the single active dialog), `window.useDialog` (hook returning `{ confirm, prompt, alert, choice }`), and a `window.DialogApi` global escape hatch for non-React modules. Each hook method returns a Promise. **Anchoring:** every method accepts an optional `anchor` HTMLElement (typically `e.currentTarget` from the trigger button); the dialog computes `getBoundingClientRect()` placement above or below with viewport-edge clamping (32 px margin) and renders a small `.dlg-arrow` pointing at the anchor. When `anchor` is `null`/`undefined`, the dialog falls back to viewport-center with `data-anchored="false"` and the arrow is omitted. Window resize/scroll re-runs the position computation. **Variants** (each picks an eyebrow label, accent color, and icon — overridable via `eyebrow`): `confirm` (default neutral), `warn` (red, "Are you sure?"), `yesno` (accent, "Question"), `text` (neutral, prompt input), `error` (red, alert glyph), `info` (accent), `choice` (accent, radio list). Panel widths: 380 (text/info), 420 (error), 360 (choice), 400 (default). **Hook API & cancel sentinels:**
  - `dialog.confirm({ anchor, title, body, confirmLabel, cancelLabel, destructive })` → `Promise<boolean>`. Cancel resolves `false`. `destructive: true` auto-maps to variant `'warn'` (unless an explicit `variant` is given) and renders the primary button red (`.dlg-btn.primary.danger`).
  - `dialog.prompt({ anchor, title, inputLabel, inputDefault, placeholder, confirmLabel, cancelLabel })` → `Promise<string | null>`. Cancel resolves `null`; the input is auto-focused and pre-selected.
  - `dialog.alert({ anchor, variant, title, body, confirmLabel })` → `Promise<undefined>`. Single-action (no Cancel button); default `confirmLabel` is `'Got it'`. Use `variant: 'error'` for failures, `'info'` (default) for advisories.
  - `dialog.choice({ anchor, title, body, choices, defaultChoice, confirmLabel })` → `Promise<string | null>`. `choices` is `[{ id, label, hint? }]`; the resolved value is the picked `id`.
  Cancel paths: clicking the scrim, Escape, or the Cancel button. Enter (without Shift) confirms. Only one dialog is shown at a time — opening a new one resolves the existing one with its `cancelValue` and replaces it. The `useDialog` hook throws `Error('useDialog must be used within a DialogProvider')` when called outside the provider, so `<App/>` must be wrapped in `<DialogProvider>` at the React root.
- `public/v2/src/dialog.css` — styles for the dialog system. `.dlg-scrim` is a fixed full-viewport blurred backdrop at `z-index: 2000` with a fade-in animation; `.dlg-panel` sits on top at `z-index: 2001` with a slide-and-fade animation (`top` for below-anchor, `bottom` for above-anchor — origin tracks placement). `.dlg-arrow` is a 12 × 12 rotated square positioned 6 px outside the panel's top or bottom edge depending on `data-placement`; only rendered when `data-anchored="true"`. Component classes follow the `.dlg-*` prefix (`.dlg-head`, `.dlg-eyebrow`, `.dlg-esc`, `.dlg-title`, `.dlg-body`, `.dlg-detail`, `.dlg-input-wrap`, `.dlg-label`, `.dlg-input`, `.dlg-choices`, `.dlg-choice`, `.dlg-radio`, `.dlg-foot`, `.dlg-hint`, `.dlg-btn` with `ghost` / `primary` / `primary danger` modifiers).
- `public/v2/src/toast.jsx` — non-blocking toast notification system. Complements the anchored-popover `Dialog` system: blocking confirms/errors use dialogs, transient confirmations (save-success, reset-success, etc.) use toasts. Exports `window.ToastProvider` (wraps the React tree below `DialogProvider`; hosts the toast queue + auto-dismiss timers), `window.useToasts` (hook returning `{ info, success, warn, error, dismiss }`), and `window.ToastApi` (global escape hatch mirrored onto `window` via `useEffect` so non-React modules can fire toasts without wiring context). **Hook API:** each level method accepts either a string (becomes the toast title) or an object `{ title, message, duration }`; default `duration` is 4000 ms (pass `0` to disable auto-dismiss). Returns the numeric toast id so the caller can dismiss it early via `toast.dismiss(id)`. Toasts render as a fixed stack in the bottom-right corner (`.toast-stack` at `z-index: 1900`, `role="status"` + `aria-live="polite"`); each card has a `.toast-close` (×) button and slides/fades in via the `toast-in` keyframe. Variants `info | success | warn | error` recolor the 3 px left border via the status tokens (`--status-running` / `--status-done` / `--status-awaiting` / `--status-error`). **Wired call sites (success path):** `ChatLive.handleReset` in `shell.jsx` fires `toast.success('Session reset')` after `StreamStore.reset` resolves truthy; `SettingsScreen.save` fires `toast.success('Settings saved')`; `KbSettingsTab.save` fires `toast.success('Embedding configuration saved')`. Error paths in all three still use `dialog.alert` because errors warrant the blocking anchored popover with the failing button as the anchor. The CSS block lives in `app.css` at the tail (before the mobile media query); toasts sit below the dialog scrim/panel (2000/2001) so dialogs always cover them, and above the mobile hamburger (1850) and sidebar (1800).
- `public/v2/src/updateModal.jsx` — `window.UpdateModal` + `window.RestartOverlay`. `UpdateModal` is the Update-Now dialog triggered from the sidebar version indicator's amber "v{remoteVersion} available" badge. Accepts `{ open, localVersion, remoteVersion, onClose, onRestarting }`. Reuses the `.fp-scrim` / `.fp-panel` / `.fp-head` / `.fp-foot` / `.fp-btn-*` visual vocabulary so it matches FolderPicker. Body shows a two-row `.um-row` key/value block (Current / Available), an explanatory `.um-note` line ("This will pull the latest code from `main`, install dependencies, and restart the server. The page will reload automatically."), and — on error — a `.um-error` callout with the server's `error` string plus a per-step `.um-steps` list (tinted green for `success: true`, red otherwise). The primary button toggles between `Update now` / `Updating…` / `Retry update` based on busy/error state; clicking it fires `AgentApi.triggerUpdate()`. Success (`result.success`) calls `onRestarting()` which flips `App` into restart mode (closes the modal, shows the overlay). Failure keeps the modal open so the user can inspect the step list and retry. A `TypeError` / `"Failed to fetch"` rejection is treated as success (same as V1 — the update script kills pm2 mid-request). Escape closes the modal unless busy. `RestartOverlay` is the full-screen curtain shown while pm2 cycles: fixed-position, `z-index: 1200`, scrim background, centered `.restart-dialog` card with "Restarting server…" title and "The page will reload automatically." subtitle. On mount it schedules `window.location.reload()` at 6000 ms (cleared on unmount). Mirrors V1's `chatShowRestartOverlay`.
- `public/v2/src/folderPicker.jsx` — `window.FolderPicker`. A modal folder browser used by the sidebar's **New conversation** action. Accepts `{ open, onClose, onSelect, onUseDefault, busy }`; renders a scrim (`z-index: 1950` — below the dialog scrim at 2000/2001 and above toasts at 1900) + centered panel (`max-width: 560px`, `max-height: 80vh`). On `open`, fetches the default directory via `AgentApi.browseDir('', showHidden)` (the server defaults to the user's home dir) and renders the response: current absolute path + Up button (if `parent != null`) + delete-current-folder button (if `parent != null`) + "Show hidden folders" toggle + "+ New folder" inline-input button + scrollable list of subdirectories + "Use default (workspace)" ghost button + "Select this folder" primary button. Clicking a folder row navigates into it (re-fetches via `browseDir(fullPath, showHidden)`); clicking **Select this folder** calls `onSelect(currentPath)`; clicking **Use default (workspace)** calls `onUseDefault()`. The "+ New folder" button flips to an inline input — Enter calls `AgentApi.mkdirDir(parentPath, name)` + navigates into the newly-created folder; Escape cancels. The delete button flips the list pane to a confirm "Delete `<folder>` and all its contents?" prompt; confirming calls `AgentApi.rmdirDir(currentPath)` + navigates to parent. All API failures surface inline as `⚠ <message>` in the path row. `busy={true}` (set by `App` while the create-conversation POST is in flight) disables every action button + the scrim click-to-dismiss so the user can't accidentally trigger a duplicate create. Escape closes the modal (when not busy). Matches V1's `chatShowFolderPicker` feature set (navigate, up, delete, show hidden, new folder, select/default/cancel) in a React-idiomatic form.
- `public/v2/src/workspaceSettings.jsx` — `window.WorkspaceSettingsModal`. The per-workspace settings dialog triggered from the gear button next to each workspace group in the sidebar. Accepts `{ open, hash, label, onClose }`. Reuses the `.fp-scrim` / `.fp-panel` / `.fp-head` shell (with a `.ws-panel` wider override: `max-width: 640px`, `max-height: 80vh`). Three tabs (`.ws-tabs` / `.ws-tab` — underlined active state, no full tab strip chrome). On open, fires three `AgentApi.workspace` reads in parallel (`getInstructions`, `getMemory`, `getKb`) and caches their results in component state. Tabs: **Instructions** — multi-line textarea bound to `instructions`; Save button is enabled only while the textarea is dirty; clicking Save calls `AgentApi.workspace.saveInstructions(hash, instructions)` and toasts success. **Memory** — enable toggle (reuses the global `.toggle` / `.tgl` styling) that immediately persists via `AgentApi.workspace.setMemoryEnabled(hash, enabled)` and refetches the snapshot on success; when enabled + non-empty, renders a `.ws-mem-groups` browser grouping entries by `type` (user / feedback / project / reference / unknown) with a capitalised group header + count badge, a `.ws-mem-item` per entry showing the derived `name` + optional `description` + relative `filename`, an expand-on-click body (`.ws-mem-item-body` — monospace, wrapped, capped at 280 px tall), a red trash icon that confirms via `dialog.confirm` and calls `AgentApi.workspace.deleteMemoryEntry(hash, relPath)`, and a footer `.ws-mem-clear` with a red-bordered "Clear all memory" button that confirms and calls `AgentApi.workspace.clearMemory(hash)`. Any mutation refetches `AgentApi.workspace.getMemory(hash)` so the list reflects the new state. **Knowledge Base** — enable toggle only; immediate-save via `AgentApi.workspace.setKbEnabled(hash, enabled)`. The toggle does not refresh the sidebar itself — closing the modal triggers `StreamStore.refreshConvList()` in `App.onCloseWorkspaceSettings`, which re-fetches `listConversations` and notifies all subscribers so the per-workspace book icon appears/disappears without a manual reload. Escape closes the modal unless `saving` is true. Mirrors V1's `chatShowWorkspaceSettings` (`public/js/main.js:1253`) in React-idiomatic form.
- `public/v2/src/sessionsModal.jsx` — `window.SessionsModal`. The per-conversation session-history browser triggered from the **Sessions** button in the `ChatLive` topbar (between the Reset and Archive buttons). Accepts `{ open, convId, currentSessionNumber, currentMessages, onClose }`. Reuses the `.fp-scrim` / `.fp-panel` / `.fp-head` shell (with a `.sh-panel` wider override: `max-width: 720px`, `max-height: 80vh`). Two internal views flipped by a component-local `view` state: `'list'` (default) renders `<SessionListView>`, `'detail'` renders `<SessionDetailView>`; the title in `.fp-head` swaps between `Session History` and `Session <N>` accordingly. **List view** — on `open`, fires `AgentApi.conv.getSessions(convId)` and caches the sessions; each row (`.sh-item`) shows `Session N` + a `Current` pill when `isCurrent: true` (tinted accent via `color-mix`), a `Download` ghost button (window-opens `AgentApi.conv.sessionDownloadUrl` into `_blank` with `noopener`) and a `View` primary-style button, the optional session summary, and a `Started: … · Ended: … · N messages` meta line. **Detail view** — on row-click, flips to `'detail'`; if `sessionNumber === currentSessionNumber` and `currentMessages` is an array, reuses it in-place to avoid a round-trip; otherwise fires `AgentApi.conv.getSessionMessages(convId, sessionNumber)`. Renders a `.sh-detail-head` strip with a `← Back to Session List` button plus the first message's localized date and a Download button, then a scrollable `.sh-detail-body` of `<SessionMessageRow>` cards. Each row carries a role label (`You` / `Assistant`), optional backend badge when `message.backend` is set, and the message body rendered via a local `sessionRenderMd` helper (`marked.parse` + `DOMPurify.sanitize`, same config as `shell.jsx`'s `renderMarkdown`). Unlike the live chat, detail rows **do not** render thinking blocks, tool activity, streaming indicators, or queue badges — this is a read-only history browser, not a parity with `<MessageBubble>`. User rows get an accent-tinted background (`color-mix(var(--accent), transparent 94%)`) to distinguish them from assistant rows. Escape closes the modal from either view. Mirrors V1's `chatShowSessions` / `chatViewSession` (`public/js/main.js:391`) in React-idiomatic form.
- `public/v2/src/tooltip.jsx` — reusable tooltip primitive (not yet wired into any surface; available for future use). Exports `window.Tip` (a `React.forwardRef` component that wraps a single child and attaches hover/focus handlers via `React.cloneElement`) and `window.useTip` (convenience `() => React.useRef(null)` for grabbing an imperative handle). **Usage patterns:** (A) declarative hover — `<Tip content="Copy link" kb="⌘."><button>…</button></Tip>`; (B) programmatic — `const ref = useTip(); <Tip ref={ref} …>…</Tip>` then call `ref.current.show() / hide() / toggle() / pin() / unpin() / anchor()`; (C) rich content — `<Tip variant="stat" rich={<Card/>}>…</Tip>`. **Props:** `content` (simple string/node for `label`/`shortcut` variants), `rich` (ReactNode for rich variants), `variant: 'label' | 'shortcut' | 'rich' | 'stat' | 'entity' | 'explain' | 'code'` (defaults to `label`), `kb` (keyboard-shortcut string — on `label` rendered as a muted `.tt-kb` chip; on `shortcut` split by space into `.kbd` pills with a `then` separator between each), `delay` (open delay, default 300 ms), `closeDelay` (default 80 ms), `pinned` (initial pinned state). **Positioning:** `useAnchorPos` internal hook measures anchor + panel `getBoundingClientRect()` on open + resize + scroll (capture) and picks below/above based on available space (flips when below has less room than above), then horizontally centers and clamps with a 16 px viewport margin and computes an `arrowX` so the arrow tracks the anchor center even after clamping. Renders a fixed-position `.tt` panel (`z-index: 200`, `pointer-events: none` unless pinned) with `role="tooltip"` + `aria-describedby="tt-active"` on the anchor; pop animation via the `tt-pop` keyframe. **Pinning:** call `ref.current.pin()` (or pass `pinned={true}` as the initial prop) to set `data-pinned="true"` — re-enables pointer events so links inside rich tooltips work, and adds a `mousedown` click-outside listener that unpins when the user clicks outside both the anchor and the panel. The primitive does **not** auto-pin on anchor click; the "click to pin" hint used in some rich-card footers is a caller-supplied string, not built-in behavior. **Variants & content classes (styled in `tooltip.css`):** `label` is a tiny nowrap pill (`.tt-kb` for the optional shortcut chip); `shortcut` shows a `.kbd` / `.sep` sequence; `rich` / `stat` / `entity` / `explain` / `code` all render a 320 px card where the caller composes content using `.tt-header` + `.tt-eye` + `.tt-status-dot`, `.tt-h` (title, `.u-dim` for muted inline text), `.tt-section` + `.tt-section-label`, `.tt-rows` + `.tt-kv` (stat grid — `<span>` label + `<b>` value with optional `<em>` suffix), `.tt-bar-wrap` + `.tt-bar` + `.tt-bar i` (progress bar), `.tt-models` + `.tt-model` (swatch + name + value rows), `.tt-foot` (hint + link footer), `.tt-code` + `.ln` + `.add` + `.del` (code/diff preview with tail-fade gradient for `variant="code"`), `.tt-entity-head` + `.tt-ent-sw` + `.tt-ent-name` + `.tt-ent-type` + `.tt-chips` + `.tt-chip` (entity card for `variant="entity"`), `.tt-body-text` (long-form paragraph for `variant="explain"`). All variants share the same auto-flip arrow (`.tt-arrow` styled per `data-placement="above"|"below"`).
- `public/v2/src/chip-renderers.jsx` — per-CLI registry for the top-bar token chip + its rich tooltip card. Exports `window.getChipRenderer(backendId)` → `{ renderChipText(usage), renderTooltipCard(usage, opts?) }`. The map `CHIP_RENDERERS` is keyed by backend id (`'claude-code'`, `'kiro'`, `'codex'`, …); unknown ids fall through to `DEFAULT_CHIP_RENDERER` so a new CLI gets a safe generic chip until it registers its own. `renderChipText(usage)` returns the string shown inside the chip **or `null` to hide the chip entirely**; `renderTooltipCard(usage, opts?)` returns a `ReactNode` rendered inside `<Tip variant="stat">`. The `opts` bag is renderer-specific — both the `claude-code` and `kiro` renderers read `opts.planUsage` to render an additional Account plan-usage section below the session breakdown (see below); other renderers ignore it. Internal components `ClaudeCodeTokenCard` / `KiroTokenCard` / `CodexTokenCard` / `DefaultTokenCard` compose the card body using the Tip primitive's stat-variant classes (`.tt-header`, `.tt-eye`, `.tt-h`, `.u-dim`, `.tt-section`, `.tt-section-label`, `.tt-rows`, `.tt-kv`, `.tt-bar-wrap`, `.tt-bar`). `ClaudeCodeTokenCard` renders the session-usage Breakdown section and, when `planUsage` is passed, a nested `<PlanUsageSection data={planUsage}/>` after it. `KiroTokenCard` similarly renders the Kiro session-tokens section and, when `planUsage` is passed, a nested `<KiroPlanUsageSection data={planUsage}/>` after it (see `KiroPlanUsageSection` below). `CodexTokenCard` is modeled on `KiroTokenCard` (Codex emits a `contextUsagePercentage` for the active turn so the session section uses the same context-window progress bar + Input/Output Tokens layout) and, when `planUsage` is passed, renders a nested `<CodexPlanUsageSection data={planUsage}/>` after it (see `CodexPlanUsageSection` below). `PlanUsageSection` renders: a section header `Account · {humanTier}` (where `_humanizeTier` strips the `default_claude_` prefix and title-cases the remainder — e.g. `default_claude_max_20x` → `Max 20x`, `default_claude_pro` → `Pro`, fallback `Plan`); one `.tt-bar-wrap` per non-null rate-limit bucket (label → utilization % → reset-time hint) built by `_buildRateLimitBars(rateLimits)`; when `rateLimits.extra_usage.is_enabled` is true, an `Extra credits` `.tt-kv` row showing `$used / $limit` formatted as dollars with 2 decimals (the Anthropic API ships `used_credits` and `monthly_limit` as **integer cents** — `18734` means `$187.34`, so the renderer divides by 100 before formatting) and an `<em>` currency suffix; a trailing `.tt-section` footer showing `Updated {Xm ago}` (or `Last update failed · {timestamp}` with `--status-error` color when `lastError` is set, else `--status-warning` when `stale: true`, else the default `--text-3`). **Rate-limit labels:** `RATE_LIMIT_LABELS` provides canonical labels for the known keys — `five_hour: '5h session'`, `seven_day: 'Weekly total'`, `seven_day_opus: 'Weekly Opus'`, `seven_day_sonnet: 'Weekly Sonnet'`, `seven_day_oauth_apps: 'Weekly API apps'`. Unknown keys are handled by `_deriveRateLimitLabel`, which strips the `seven_day_` / `five_hour_` prefix, title-cases the remainder, and prepends `Weekly ` or `5h ` accordingly — this keeps the UI forward-compatible with Anthropic's unreleased codename buckets (`seven_day_omelette`, `iguana_necktie`, etc.). The bucket iterator skips the `extra_usage` key (rendered separately as the credits row) and any bucket whose `utilization` is not a number. **Kiro `KiroPlanUsageSection`** renders: a section header `Account · {humanTitle}` where `_humanizeSubscriptionTitle(subscription.subscriptionTitle)` title-cases the upstream label (so `"free"` / `"PRO"` render as `Free` / `Pro`; falls back to `Plan` when missing); a single credits `.tt-bar-wrap` with the bar filled to `min(used/limit, 1) * 100%` and a head reading `{unitLabel}` (on the left, sourced from `breakdown.displayNamePlural` — `Credits` when missing) + `{usedFmt} / {limitFmt}` (on the right, via `_fmtCredits(n)` — 2 decimals below 10, integer for 10–999, `toLocaleString` for ≥ 1000) with an optional `· resets {Mon D}` suffix sourced from `_formatResetDate(breakdown.nextDateReset ?? usage.nextDateReset)` (`nextDateReset` is epoch **seconds**, not ms — multiplied by 1000 before `new Date()`); the renderer prefers the `*WithPrecision` variants (`currentUsageWithPrecision`, `usageLimitWithPrecision`, `currentOveragesWithPrecision`) when present, falling back to `currentUsage` / `usageLimit` / `currentOverages`; an `Overage` `.tt-kv` row when `overageStatus === 'ENABLED'` reading `{count} {unitLabel.toLowerCase()} · ${charges}` (count via `_fmtCredits`, charges via `overageCharges.toFixed(2)`) when `currentOveragesWithPrecision > 0`, else `Enabled · none used` (suppressed entirely when `overageStatus` is any other value); a `Bonus credits` `.tt-kv` row when `breakdown.bonuses.length > 0` showing the **count** of bonus entries (`<b>{bonuses.length}</b>` — each entry is an opaque object, so the renderer surfaces how many were granted rather than trying to extract a total); a fallback `No usage data yet.` monospace line when `breakdown` is missing; and a trailing `.tt-section` footer `Updated {Xm ago}` (or `Last update failed · {timestamp}` with `--status-error` color when `lastError` is set, else `--status-warning` when `stale: true`, else the default `--text-3`). **Codex `CodexPlanUsageSection`** renders: a section header `Account · {humanPlan}` where `_humanizeCodexPlan(planType)` maps the verbose tier strings to short labels (`self_serve_business_usage_based` → `Business (PAYG)`, `enterprise_cbp_usage_based` → `Enterprise (PAYG)`, `prolite` → `Pro Lite`) and otherwise title-cases the underscore-separated tier (so `pro` → `Pro`, `plus` → `Plus`, `team` → `Team`); one `.tt-bar-wrap` per non-null window built by `_buildCodexBars(rateLimits)` — Codex returns `primary` (5h) and `secondary` (weekly), and the renderer keys labels off `windowDurationMins` via `_codexWindowLabel(mins)` (`300 → '5h session'`, `10080 → 'Weekly'`, fallback `{Nh window}` / `{Nd window}` derived from the duration) so a future API change that swaps slots can't mislabel the bars; each bar shows `usedPercent`% utilization with a `· resets in …` suffix sourced from `_formatResetAtEpoch(resetsAt)` (Codex's `resetsAt` is **epoch seconds**, not ms — so this helper differs from Claude's ISO-string `_formatResetAt` and Kiro's `_formatResetDate`); a `Credits` `.tt-kv` row showing `{balance}` (or `Unlimited` when `credits.unlimited === true`) is rendered **only when `credits.hasCredits === true`** — ChatGPT subscribers without an API credit balance get no row at all, keeping the tooltip tight; a fallback `No usage data yet.` line when both windows and credits are missing; and a trailing `.tt-section` footer `Updated {Xm ago}` (or `Last update failed · {timestamp}` with `--status-error` color when `lastError` is set, else `--status-warning` when `stale: true`, else the default `--text-3`). Formatter helpers (file-local): `_fmtTokensShort(n)` (→ `20.4k` / `3.1M` / raw), `_fmtCost(c)` (2 decimals ≥ 1 USD, 4 decimals below), `_fmtInt(n)` (locale-separated integers), `_fmtCredits(n)` (Kiro credit counts — 2 decimals below 10, integer for 10–999, `toLocaleString` for ≥ 1000), `_formatResetAt(iso)` (→ `resets in {Nm|Nh|Nd}` / `resets now` when past — used by Claude's `PlanUsageSection`), `_formatResetDate(epochSeconds)` (→ locale `Mon D` string; empty when the number isn't finite — used by Kiro's `KiroPlanUsageSection`), `_formatResetAtEpoch(epochSeconds)` (→ `resets in {Nm|Nh|Nd}` / `resets now` when past — used by Codex's `CodexPlanUsageSection`; differs from `_formatResetAt` in that the Codex API ships epoch seconds rather than an ISO string), `_humanizeSubscriptionTitle(title)` (Kiro plan header — title-cases each word of `subscriptionTitle`, or `Plan` when missing), `_humanizeCodexPlan(planType)` (Codex plan header — special-cases verbose business / enterprise tiers, otherwise title-cases the underscore-separated tier; falls back to `Plan` when missing), `_codexWindowLabel(windowDurationMins)` (Codex bar label — `5h session` for 300, `Weekly` for 10080, otherwise `{Nh window}` / `{Nd window}`), `_buildCodexBars(rateLimits)` (extracts the `primary`/`secondary` window pair into the `[{ key, label, percent, resetsAt }]` shape `CodexPlanUsageSection` iterates), `_formatFetchedAt(iso)` (→ `just now` / `{N}m ago` / `{N}h ago` / `{N}d ago` / `never` when null). Load order: must come after `tooltip.jsx` (it renders `<Tip>` indirectly via the caller) and before `shell.jsx` (which calls `getChipRenderer` from `ContextChip`).
- `public/v2/src/primitives.jsx` — `VersionIndicator`, `Sidebar`, `ToolRow`, `Breadcrumb`. `VersionIndicator` is self-contained: owns its own `{ version, remoteVersion, updateAvailable, checking }` state, fires `AgentApi.getVersion()` once on mount, runs a 5-minute `setInterval` calling `AgentApi.getUpdateStatus()` for background update detection, and on click flips into a "checking…" label while `AgentApi.checkVersion()` is in flight. Takes a single `onShowUpdate({ localVersion, remoteVersion })` callback — fired when the user clicks the amber "v{remoteVersion} available" badge so `App` can open the Update modal. Renders nothing until the first `getVersion()` resolves (so the sidebar doesn't briefly show a missing version). The `Sidebar` accepts `{ activeId, onSelect, convStates, onOpenKb, onOpenFiles, onOpenSettings, onOpenWorkspaceSettings, onNewConversation, viewingArchive, onToggleArchive, onRestore, onSignOut, onShowUpdate }` — `onShowUpdate` is forwarded to the embedded `<VersionIndicator/>` at the bottom of the sidebar (below `.sb-footer`) — when `onSelect` is provided, rows become keyboard- and pointer-clickable and fire with the conversation id. `convStates` is an optional `{ [convId]: 'streaming' | 'awaiting' | 'error' }` map; matching rows get the corresponding state class (`.streaming` / `.awaiting` / `.error`) so the leading `.dot` lights up pulsing-blue, amber, or red per the tokens. `onOpenKb(hash, label)` fires when the user clicks the per-workspace KB button (book icon); the button is only rendered when the workspace has `kbEnabled: true` (derived from `workspaceKbEnabled` on any of its conversations). The KB button carries a `.persist` class so it opts out of the hover-only fade applied to its siblings (Files, Workspace settings). `onOpenFiles(hash, label)` fires when the user clicks the per-workspace Files button (folder icon) — the button is always rendered (inside the hover-only `.actions` span) but is `disabled` when the workspace group has no `hash` (pre-merge drafts). `onOpenSettings()` fires when the user clicks the global **Settings gear** in the sidebar footer (`.sb-footer-icon`) and triggers the modal-swap Settings screen in `App` (no per-workspace context — the screen handles app-wide settings only). `onOpenWorkspaceSettings(hash, label)` fires when the user clicks the per-workspace gear button (settings icon in the `.actions` span next to Files) — the button is always rendered but is `disabled` when the workspace group has no `hash`; `App` routes the callback to its `<WorkspaceSettingsModal>` state. `onNewConversation()` fires when the user clicks the **New conversation** button and opens `App`'s `<FolderPicker>` modal (the `⌘N` hint on the button is decorative — browsers reserve ⌘N / Ctrl+N for "new window", and V1 never bound it either); when omitted the button renders disabled. The Sidebar no longer takes a `refreshToken` prop — it's a pure subscriber to `StreamStore.getConvList()` / `subscribeConvList()` and any caller that needs the list to mutate (rename / archive / delete / restore / create / KB toggle) updates the store directly via `patchConvListItem` / `removeConvListItem` / `prependConvListItem` / `refreshConvList`, which fan out to all subscribers including the Sidebar. **Resizable sidebar** — `.cockpit` uses `grid-template-columns: var(--cockpit-sb-w, 260px) 1fr` (and `var(--cockpit-sb-w, 260px) 1fr 320px` in `.cockpit.with-rail`), with the sidebar width state-owned by the `Sidebar` component. A `<div class="sb-resizer" role="separator">` absolutely-positioned on the right edge of `.sb` (5px wide, `cursor: col-resize`, `background: var(--accent)` while hovered or `.dragging`) captures `mousedown` and attaches window-level `mousemove` / `mouseup` listeners. Drag updates `--cockpit-sb-w` on `document.documentElement` (clamped 200–600px); mouseup persists the final value under `localStorage['ac:v2:sb-width']`. Double-clicking the resizer resets to the 260px default and saves. `public/v2/index.html` carries a second pre-paint IIFE (alongside the existing theme pre-paint) that reads the saved width and sets `--cockpit-sb-w` before the cockpit grid paints, avoiding a width-flicker from 260px on refresh. The resizer is hidden in the ≤ 768px breakpoint (sidebar becomes a fixed overlay with its own `width: 85vw`).
- `public/v2/src/shell.jsx` — real app root. `<ChatLive>` is wrapped in a `ChatErrorBoundary` with `key={activeConvId}` so a render/effect crash inside one conversation renders a local "Something went wrong" panel (with error message + Reload button) instead of taking down the whole shell; switching to another conversation remounts the boundary and gives the user a fresh attempt. Mounts `<DialogProvider>` → `<ToastProvider>` → `<BackendsProvider>` → `<App/>` → `<Sidebar/>` + one of `<ChatLive convId=…/>`, the static empty-main placeholder, `<KbBrowser hash=… label=… onClose=…/>` (modal-swap), `<FilesBrowser hash=… label=… onClose=…/>` (modal-swap), or `<SettingsScreen onClose=…/>` (modal-swap) based on `App`'s `activeConvId`, `kbView`, `filesView`, and `settingsView` state. Wrapping the whole tree in `<DialogProvider>` makes `useDialog()` available to every descendant; `App` itself uses it to surface the session-expired dialog (the handler registered with `AgentApi.setSessionExpiredHandler` calls `dialog.alert({ variant: 'error', title: 'Session expired', body: 'Refresh and sign in again.' })` instead of `window.alert`). Clicking the sidebar KB button sets `kbView = { hash, label }` which renders the browser in a `.main.main-kb` wrapper; clicking the Files button sets `filesView = { hash, label }` which renders the explorer in a `.main.main-files` wrapper; clicking the footer Settings gear sets `settingsView = true` which renders the settings screen in a `.main.main-settings` wrapper. The three modal-swap states are mutually exclusive — opening one clears the others. Selecting a conversation clears all three so the chat view takes over again. `settingsView` is a plain boolean (no per-workspace context) since the settings screen edits app-wide preferences only. `<ChatLive>` is **not** keyed by `convId` — it is a thin view on `StreamStore` state, subscribed via the `useConversationState(convId)` hook, so switching conversations rebinds the view to a different store entry instead of remounting. The hook ensures the conv is loaded (`StreamStore.load(convId)`) and its WebSocket is open (`StreamStore.ensureWsOpen(convId)`) on every switch; the store itself is idempotent for both. Persisted messages come from `GET /api/chat/conversations/:id` (no separate `/sessions/:num/messages` round-trip), and the WebSocket URL is `/api/chat/conversations/:convId/ws`. Because the WS is owned by the store (not the view), it stays open across conversation switches and in-flight streams continue in the background. The sidebar brand slot (`.sb-logo` next to the "Agent Cockpit" wordmark in `.sb-top`) renders the full, text-free cockpit mark (`/logo-full-no-text.svg`) at 36×36; the assistant-avatar fallback in `AssistantAvatar` uses the small mark (`/logo-small.svg`) inside the 26×26 `.avatar.avatar-cockpit` tile. **Mobile responsive breakpoint** — `App` carries a `sbOpen: boolean` state (default `false`) used only when the viewport is ≤ 768 px. When true, `.cockpit` gets a `.sb-open` modifier class. Two siblings sit after `<Sidebar>` inside `.cockpit`: `<div className="sb-backdrop"/>` (a fixed-position click-to-dismiss scrim at `z-index: 1700`) and `<button className="nav-hamb">` (a fixed 40×40 hamburger at `left/top: 10px`, `z-index: 1850`, with an inline three-line SVG glyph). Both are `display: none` on desktop. Inside `@media (max-width: 768px)` in `app.css`, `.cockpit` collapses to `grid-template-columns: 1fr`, `.sb` becomes a fixed-position slide-in drawer (`width: 85vw` capped at 320 px, `transform: translateX(-100%)` → `translateX(0)` when `.sb-open`), and the hamburger/backdrop are flipped visible. The hamburger fades to opacity 0 while the sidebar is open (overlap would cover the brand) — the backdrop tap is the primary dismiss. Every content swap (`onSelectConv`, `onOpenKb`, `onOpenFiles`, `onOpenSettings`) auto-closes the drawer via `setSbOpen(false)`. All four topbars (`.topbar`, `.kb-top`, `.fx-top`, `.settings-top`) gain `padding-left: 58px` to clear the hamburger, crumbs truncate with ellipsis, and right-side buttons shrink. Composer reflow: `.composer-foot` and its `.picks` row both `flex-wrap: wrap` so the Backend/Model/Effort chips flow onto a second row when needed; padding compresses. Files Browser stack: `.fx.fx-live` re-declares its grid to a single column with `top` / `tree` / `edit` stacked vertically, `.fx-resizer` is hidden, `.fx-tree` caps at `max-height: 40vh`, and `.fx-tree-row` / `.fx-tree .row` get `padding: 10px 10px` + `min-height: 40px` for touch targets (with always-visible `.fx-tree-actions` at 32×32 since hover-reveal doesn't work on touch). KB Browser Raw stack: `.kb-raw-body` becomes `flex-direction: column`, `.kb-raw-tree-rail` goes full-width capped at `max-height: 35vh`, `.kb-raw-resizer` is hidden. The dialog system (z-index 2000/2001) still wins over everything. `BackendsProvider` wraps the app in a `BackendsContext` whose value is the `BackendMetadata[]` registry (fetched once per session via `useBackends()` → `AgentApi.getBackendsCached()`); `useBackendList()` exposes the list to any descendant without re-fetching. The main consumer is `AssistantAvatar({ backend })`, which looks up the `BackendMetadata.icon` inline SVG for the message's `backend` id and renders it into the agent bubble's avatar slot (`.avatar.avatar-svg` with `dangerouslySetInnerHTML` — safe because the SVG strings originate in our own backend adapter TypeScript files, not user input). When the backend id is unknown or carries no icon (legacy messages, third-party backends), the avatar falls back to the Agent Cockpit logo (`/logo-small.svg` inside `.avatar.avatar-cockpit`). Both `MessageBubble` (for assistant final messages) and `ProgressBreadcrumbBubble` (for progress-trailing runs — takes `backend` from the first progress message) consume `<AssistantAvatar>`; user bubbles still render the fixed `DY` monogram in the same slot.
- `public/v2/src/screens/kbBrowser.jsx` — real KB Browser (`window.KbBrowser`). Rendered by `shell.jsx` when the sidebar's KB button is clicked; takes `{ hash, label, onClose }`. Tab strip: **Raw** (wired — pandoc-availability banner, toolbar with auto-digest toggle / "Digest All Pending (N)" button / unified digest-progress chip / **Upload Files** (multi-file `<input multiple>`) and **Upload Folder** (`<input webkitdirectory>` — `webkitdirectory` / `directory` / `mozdirectory` are set on the DOM node via a callback ref so React doesn't filter them out); whole-pane **drag-and-drop** with a centered "Drop to upload" overlay (`onDragEnter` / `onDragOver` / `onDragLeave` / `onDrop`, with a counter ref so child re-enters don't flicker the overlay) — dropped folders are walked recursively via `DataTransferItem.webkitGetAsEntry()` + `FileSystemDirectoryReader.readEntries()` (loops until an empty batch since Chrome caps at 100/call) so nested folders preserve their structure. All three input paths produce a flat `[{ file, folderPath }]` array fed into a client-side **upload queue**: `setQueue(prev => prev.concat(...))` enqueues new items; a drain `useEffect` watching `queue` picks the next `'queued'` item whenever `inFlight < KB_UPLOAD_CONCURRENCY = 3`, flips it to `'uploading'`, and fires `AgentApi.kb.uploadRaw(hash, file, folderPath, onProgress)`. Per-item **progress bars** are sourced from the `(loaded, total) → loaded/total` callback. Successful uploads flip to `'done'` (or `'deduped'` when `res.deduped === true`) and auto-dismiss after `KB_UPLOAD_DISMISS_MS = 1500ms`; the constants are intentionally KB-prefixed because `filesBrowser.jsx` already declares its own top-level `const UPLOAD_CONCURRENCY` — both files load as classic scripts in the same realm so a duplicate top-level `const` would throw `SyntaxError: Identifier 'UPLOAD_CONCURRENCY' has already been declared`, leave `window.FilesBrowser` undefined, and crash on the next sidebar Files-button click. errors stick at `'error'` with a `×` button to dismiss manually. The queue header shows `Uploads · N in progress / M total` and a "Clear finished" button when any non-active rows remain. Per-row file list (root folder only — folder tree lands later) with status pill; the meta line under the filename appends `· N entries` when `status === 'digested' && entryCount > 0` (sourced from the new `KbRawEntry.entryCount` field on `GET /kb`) — kept inline next to size/timestamp rather than rendered as a separate pill so it reads as document metadata, not a status. "Digest" button on `ingested`/`pending-delete`/`failed` rows, and "Delete" that removes that single `(folder, filename)` location via `DELETE /kb/raw/:rawId?folder=&filename=`. Smart polling: `getState` is refetched every 1500 ms only while something is in-flight (`digestProgress != null` OR any `ingesting`/`digesting` in `rawByStatus`); on idle the timer stops. **Folder tree + breadcrumb + folder CRUD:** the Raw tab is a two-column split — a **resizable** left rail (`.kb-raw-tree-rail`) renders `KbRawFolderTree` from `kbState.folders` (built into a nested map by `buildFolderTree(folders)` so siblings sort alphabetically and ancestors auto-open when a deep child is active), and a right column hosts a breadcrumb (`KbRawBreadcrumb`) above the upload queue + file list. The rail's width is driven by the inline CSS variable `--kb-tree-w` set on the `.kb-raw-body` wrapper; a 5px `<div class="kb-raw-resizer">` between the rail and the right column is draggable (window-level `mousemove`/`mouseup` listeners reading from a `treeWidthRef` to avoid stale closures) to resize between `KB_TREE_WIDTH_MIN = 140px` and `KB_TREE_WIDTH_MAX = 600px`, double-clicking the resizer resets to `KB_TREE_WIDTH_DEFAULT = 220px`, and the chosen width is persisted in `localStorage` under `ac:v2:kb-tree-width:<hash>` so each workspace remembers its own preference (a `useEffect` watching `hash` reloads the stored width on workspace switch). The rail no longer renders its own right border — the visible resizer (`background: var(--border)`, accent on hover/dragging) provides the column separation. Clicking a folder sets `currentFolder` (component state inside `KbRawTab`) and a `useEffect` re-runs `getState(hash, {folder: currentFolder})` to repopulate the right column — the snapshot's `counters`/`folders` are workspace-wide so the header's `{rawTotal} files · {entryCount} entries · {folderCount} folders` line stays accurate when navigating folders. Each non-root tree row has hover-revealed **rename** (`✎`, prompts via `useDialog`) and **delete** (`×`, cascade-confirms) icons — these are the **only** rename/delete affordances; the breadcrumb no longer carries per-folder action buttons. Renaming `a/b` to `a/b2` rewrites `currentFolder` in place if the renamed prefix matches; deleting the active folder (or one of its ancestors) navigates back to root. All three CRUD actions go through `AgentApi.kb.createFolder(hash, folderPath)` / `renameFolder(hash, fromPath, toPath)` / `deleteFolder(hash, folderPath, cascade)` (cascade is always `true` from the UI — the confirm copy makes the destructive scope explicit). The toolbar's **+ New folder** button (plain `btn` style, sitting to the **left of Upload Files**) creates a folder under `currentFolder` and auto-navigates into it. All three upload pathways (multi-file picker, folder picker, DnD) prepend `currentFolder` via the `joinFolder(base, child)` helper so files always land under the active folder; the drop overlay surfaces the target as `Uploads land in /currentFolder.` **Status filter (breadcrumb pills):** `KbRawBreadcrumb` renders a single-select pill group (`.kb-raw-status-filter` / `.kb-raw-filter-pill`) sourced from the `KB_STATUS_FILTERS = [{id:'all'}, {id:'digested'}, {id:'pending'}]` constant — `All` (default), `Digested` (`raw.status === 'digested'`, fully processed end state), and `Pending digestion` (`raw.status === 'ingested' || raw.status === 'ingesting' || raw.status === 'digesting'`, covers everything that hasn't reached the final digested state — both queued and in-flight). The selected pill carries an `on` modifier (accent border + accent-tinted background). Filtering happens client-side via `rawMatchesStatusFilter(raw, filterId)` against the folder-scoped `kbState.raw` list before the alphabetical sort. `failed` and `pending-delete` rows only surface under `All`. When a filter narrows the list to empty (but the unfiltered list is non-empty), the empty state reads `No files match this filter. Try All.` with `All` rendered as a click-to-clear `kb-raw-crumb-link` button that resets `statusFilter` to `'all'`. The filter is component-local state (not persisted), so it resets to `All` on browser close/reopen. **Active-row autoscroll:** each `KbRawTreeNode` holds a `rowRef` and runs a `useEffect` keyed on `isActive` that calls `rowRef.current.scrollIntoView({ block: 'nearest', behavior: 'smooth' })` whenever the row becomes the active selection. This covers both manual navigation (clicking a row scrolls it into view if it was offscreen) and the create-folder flow (`onCreateFolder` does `setCurrentFolder(newPath)` after `createFolder` resolves; once the next `refetch` lands the new node mounts with `isActive=true` and the effect scrolls it into view). `block: 'nearest'` is a no-op when the row is already visible, so navigation between visible rows doesn't re-scroll), **Entries** (wired — search + tag-chip filter + paginated list, click to open a modal with full markdown body + source locations), **Synthesis** (wired — left-pane topic list with status pills, click a topic to render its entries/connections/synthesis markdown in the right pane), **Reflections** (wired — list of reflection cards with a stale badge when `isStale: true`; click to open a modal with content + cited entries), **Settings** (wired — `KbSettingsTab` renders the per-workspace embedding configuration form: **Model** (text, blank → backend default `nomic-embed-text`), **Ollama Host** (text, blank → `http://localhost:11434`), **Dimensions** (number 1–4096, blank/invalid → `768`); **Test Connection** button fires `AgentApi.kb.embeddingHealth(hash)` and surfaces an inline pill (`.kb-settings-health` with `checking | ok | err` modifier — green "Connected", red error message, or grey "Checking…"); **Save** button fires `AgentApi.kb.setEmbeddingConfig(hash, payload)`, refreshes form state from the server's echoed `embeddingConfig`, and surfaces success via `toast.success('Embedding configuration saved')` (non-blocking); failures still route through `dialog.alert({ anchor, variant: 'error' })`. Form values hydrate from `AgentApi.kb.getEmbeddingConfig(hash)` on mount; `null` config falls back to empty inputs so placeholders show the defaults. Saving the same model+dimensions repeatedly is safe; changing either evicts the cached PGLite vector store on the server and rebuilds with the new dimensions on next access — see backend spec). The `KbRawTab` reuses the parent `KbBrowser`'s `kbState` and is given `onStateUpdate(state)` so its polling/mutations bubble back into the header counters — the `{rawTotal} files · {entryCount} entries · {folderCount} folders` line stays live as users upload, digest, or delete. All confirms/error toasts go through `useDialog()`; no native `window.alert` / `confirm` / `prompt`. Top bar displays the workspace label plus `{rawTotal} files · {entryCount} entries · {folderCount} folders` from `counters` in the initial state snapshot, and a Close button that calls `onClose`. All reads go through `AgentApi.kb.*`. Inside the entry modal, `rewriteEntryMediaPaths(body, hash, rawId)` runs before `marked.parse` so any relative markdown image (`![alt](media/Slide123.jpg)`, `![alt](slides/slide-001.png)`, `![alt](pages/page-0001.png)`, etc.) becomes an absolute URL to `GET /workspaces/:hash/kb/raw/:rawId/media/*`. The regex matches `![alt](url "title")` and skips anything that is already absolute (`http:`, `https:`, `//`, `/`, `#`, or any `scheme:` prefix), so external URLs and in-page anchors render unchanged. **Image sizing & lightbox:** `.prose img` is constrained to `max-width: 100%` so embedded slides/pages don't overflow the modal; clicking any image opens `KbImageLightbox` (a fixed-position fullscreen scrim with the image at `max-width: 95vw / max-height: 95vh`) — Esc or backdrop click closes it. Wired into both the entry modal and the reflection modal via the shared `interceptImageClick(event, onOpen)` helper, which short-circuits when the click target sits inside an `<a>` (so a linked image lets the link handler win).
- `public/v2/src/screens/filesBrowser.jsx` — real Files Browser (`window.FilesBrowser`). Rendered by `shell.jsx` when the sidebar's Files button is clicked; takes `{ hash, label, onClose }`. Full-screen `.fx.fx-live` grid (resizable tree column + 5px resizer + 1fr preview on top of a top-bar row). The tree column width is driven by the inline CSS variable `--fx-tree-w` (set on the root `.fx` element); a `<div class="fx-resizer">` between the tree and preview panes is draggable (window-level `mousemove`/`mouseup` listeners) to resize between `FX_WIDTH_MIN = 140px` and `FX_WIDTH_MAX = 600px`, double-clicking the resizer resets to `FX_WIDTH_DEFAULT = 220px`, and the chosen width is persisted in `localStorage` under `ac:v2:fx-tree-width:<hash>` so each workspace remembers its own preference. Top bar carries a clickable breadcrumb (workspace root → each folder segment), Refresh, New folder, New file, Upload, and Close buttons. Tree pane is a recursive `<FxTreeBranch>` driven by a cached `childrenMap: Map<rel, entry[]>`; clicking a folder row toggles its expansion (lazy-loading children via `AgentApi.explorer.tree` on first expand), clicking a file row sets `selected` and fetches a preview via `AgentApi.explorer.preview`. Row hover exposes per-row action buttons: Rename, Download (files only), and Delete — all prompts and error notifications go through `useDialog()` (anchored popovers — see *Dialog system* above), never `window.prompt` / `window.confirm` / `window.alert`. Each trigger button captures `e.currentTarget` synchronously and threads it down (toolbar buttons → `doMkdir` / `doNewFile`; row icon buttons → `FxTreeRow.onRename(currentTarget)` / `onDelete(currentTarget)` → `doRename(rel, name, anchor)` / `doDelete(rel, type, name, anchor)`; preview pane Cancel/Save → `cancelEdit(currentTarget)` / `saveEdit(currentTarget)`) so the popover arrow points at the button that triggered it. Validation errors (invalid characters, 409 destination-exists overwrite confirms, save/upload failures) reuse the same anchor so a single trigger can chain prompt → error alert. The upload-overwrite confirm has no natural anchor (it fires from the background upload worker) and falls back to viewport-center. Preview pane (`<FxPreviewPane>`) branches on `previewKind(name, size)`: `image` renders an `<img>` against `rawUrl` capped at `max-width: 100% / max-height: 70vh`, `markdown` runs the same `marked + DOMPurify` pipeline, `text` renders a monospace `<pre><code>…</code></pre>`, `oversize-image` / `oversize-text` / `unsupported` show a dimmed message with a Download action. Text/markdown previews are editable — `Edit` flips into a `<textarea>` (via `fx-editor` class); `Save` calls `AgentApi.explorer.saveFile`, `Cancel` prompts for confirmation when the draft differs from the saved content. **Uploads:** the toolbar's Upload button and drag-and-drop onto the tree pane (either the whole pane for the current folder or onto a specific directory row for that row's path) push items into an uploads queue; `drainUploads` maintains up to `UPLOAD_CONCURRENCY = 3` concurrent XHRs via `AgentApi.explorer.upload`, each with a progress bar rendered in the `<FxUploadPanel>` at the bottom of the tree pane. Once no upload is queued or in progress, a `useEffect` auto-dismisses the `done` rows after a 1 s delay (keeping `error`/`skipped` rows visible so the user can see what failed); if the filtered list becomes empty the panel disappears entirely. 409 conflicts prompt to overwrite and retry; 413 / other errors surface inline on the row. Each upload refreshes its own target folder's branch the moment it completes — mutation side-effects go through `refreshBranch(rel)`, a helper that re-fetches a single folder's children and patches them in place in `childrenMap` (also updating the main-pane `entries` when the refreshed folder matches `currentFolder`, tracked via a `currentFolderRef`). **Rename/delete/save/create side-effects:** all call `refreshBranch(parent)` for the affected folder instead of clearing the whole `childrenMap` — this means a file created, saved, or uploaded into a subfolder shows up in that subfolder's expanded branch immediately, not only after a manual re-expand. Rename retries with `overwrite: true` on 409 after a confirm. **Preview state shape:** `{ path, name, size, kind, loading, content, language, editing, draft, saving, error }` — `content` is the file body, `draft` the in-progress edit, `kind` is one of `image | oversize-image | markdown | text | oversize-text | unsupported`.
- `public/v2/src/screens/settingsScreen.jsx` — real Settings screen (`window.SettingsScreen`). Rendered by `shell.jsx` when the user clicks the sidebar footer's gear icon; takes `{ onClose }` (no per-workspace context — settings are app-wide). Top bar carries the title block ("Settings" + "Global app preferences" subtitle) and a Close button. Tab strip mirrors the V1 5-tab layout sourced from the `SETTINGS_TABS` constant: **General**, **Memory**, **Knowledge Base**, **Usage**, **Server**. The selected tab carries an `active` modifier (accent underline). On mount runs `Promise.all([AgentApi.settings.get(), AgentApi.settings.backends()])` to load the full settings object plus the backend registry; while loading, the body shows a dimmed "Loading…" placeholder. All form tabs share a single in-memory `settings` object via the `patch(updater)` helper; clicking **Save** on any of the form tabs (General / Memory / KB) POSTs the **full** merged shape via `AgentApi.settings.save(settings)` so the user doesn't have to remember to save from one specific tab (V1 only had Save on General). Save success fires `toast.success('Settings saved')` (non-blocking, auto-dismisses); save failure still uses `dialog.alert({ anchor: e.currentTarget, variant: 'error' })` anchored to the Save button so the error is inspectable. **Controls reuse the design mock-up's primitives** (`screens/settings.jsx` in the deck): the file declares two reusable helpers — `<Seg value onChange options={[{id,label}]} />` renders the deck's `.seg.seg-inline` pill row (active option marked with `aria-pressed="true"`), and `<Toggle checked onChange label />` renders the deck's `.toggle/.tgl` pill switch (hidden checkbox + visual `.tgl` span). Backend / model selectors stay as native `<select>` elements (long lists) but Theme, Send behavior, every effort selector, and the convert-slides toggle use the deck primitives so the visual language matches the mock-up. The base `.seg button[aria-pressed="true"]` rule hardcodes the deck shell's colors; `app.css` adds a `.settings-shell .seg button[aria-pressed="true"]` override that maps the active pill to `var(--text) / var(--bg)` so the contrast is correct in both light and dark themes.
  - **General tab** — Theme `<Seg>` (`system | light | dark`; selecting any value calls `applyThemePreview(theme)` which sets `data-theme` on `#root` synchronously so the user sees a live preview before saving — `system` resolves to the OS preference via `window.matchMedia('(prefers-color-scheme: dark)')`); Send behavior `<Seg>` (`enter` for "Enter to send", `ctrlEnter` for "⌘/Ctrl+Enter to send"); Default backend `<select>` (driven by `backends[]`); Default model `<select>` (only rendered when the backend has models); Default effort `<Seg>` (only rendered when the model exposes `supportedEffortLevels` — typically 2–4 entries like `low | medium | high`); System prompt (textarea, 6 rows, monospace). Switching the backend resets `defaultModel` + `defaultEffort` to that backend's defaults via `defaultModelId(models)` / `defaultEffortFor(levels)` so we never carry a model id into a backend that doesn't support it; switching the model resets `defaultEffort` similarly.
  - **Memory tab** — `memory.cliBackend` / `memory.cliModel` `<select>` plus `memory.cliEffort` `<Seg>` (drives the workspace-memory CLI used to generate `CLAUDE.md`). When unset, falls back to the General tab's default backend / model / effort. Same backend → model → effort cascade as the General tab.
  - **Knowledge Base tab** — Pandoc + LibreOffice availability pills (`AgentApi.kb.pandocStatus()` and `AgentApi.kb.libreOfficeStatus()` fire on mount; pill carries `ok` / `err` modifier and shows the version when available). **Digestion** section: `knowledgeBase.digestionCliBackend` / `digestionCliModel` `<select>` + `digestionCliEffort` `<Seg>` (cascade-resetting like General). **Dreaming** section: `dreamingCliBackend` / `dreamingCliModel` `<select>` + `dreamingCliEffort` `<Seg>` plus `dreamingConcurrency` (number input clamped 1–10). **Conversion** section: a `convertSlidesToImages` `<Toggle>`; flipping it on while LibreOffice is unavailable rejects the toggle and surfaces an inline `.settings-warning` so the user isn't silently saving an unrunnable preference.
  - **Usage tab** — Mirrors the deck mock-up's "Usage & cost" view (`screens/settings.jsx`'s `SettingsUsage`). Loads `AgentApi.settings.usageStats()` on mount. Renders three stacked blocks inside `.settings-usage` (a wider wrapper than `.settings-form` so the chart + table get room to breathe): (1) `.stat-grid` of four `.stat` cards — **Today**, **Last 7 days**, **Last 30 days**, **All time** — each showing `$cost` as the prominent number plus a token sub (in/out for Today, total tokens for the others); the day buckets are computed by `trailingDays(days, n)` (most-recent N days relative to the latest date in the ledger, not wallclock today, so sparse data still aggregates sensibly), then summed via `totalsFor(days)`. (2) `.pane-block` "Last 14 days" containing a `.bars` vertical-bar chart and a `<Seg>` metric toggle in the head (`Cost | Tokens` — the chart re-renders when toggled). `buildBars(days, metric)` returns up to 14 bars sorted oldest → newest; bar height is `value / max * 100%`; each bar carries a `title` showing `${date} · ${metric value}` for hover. Empty/zero-value windows show a dimmed "No usage recorded yet." placeholder instead of the chart. (3) `.pane-block` "By backend & model" containing a `.tbl` aggregated all-time per (backend, model) with right-aligned numeric columns (Input / Output / Cache R / Cache W / Cost) sorted by descending cost. Below the blocks, a destructive **Clear all usage data** button (ghost style) fires `dialog.confirm({ destructive: true })` and on confirm calls `AgentApi.settings.clearUsageStats()` then reloads — explicitly framed in the confirm copy as a purely local ledger purge that doesn't affect backend billing. Token formatting: `fmtTokensShort(n)` produces `1.5k` / `2.3M`; `fmtCostShort(n)` produces `$3.42` (or `$94` for ≥ $100); the table uses full-precision `fmtNum` / `fmtCost` (`$0.0000`).
  - **Server tab** — Single **Restart server** button. `dialog.confirm({ destructive: true })` then `AgentApi.settings.restartServer()`. A live `.settings-status-line` mirrors the request lifecycle. On success (`result.success === true`) the status flips to "Restart launched. Reconnecting…", a full-viewport `.restart-overlay` fades in after 500 ms ("Restarting server… The page will reload automatically."), and `window.location.reload()` fires at 6000 ms. The fetch itself usually resolves cleanly because the restart script sleeps briefly before killing pm2, but if timing races and the process dies first the promise rejects with `TypeError: Failed to fetch` — `ServerTab` catches that specific shape and treats it as success too (overlay + 5000 ms reload). 409 responses (active stream in progress) surface as a friendly "Restart blocked: an active stream is in progress. Stop or wait for it, then try again." line; other failures show the raw error and re-enable the button. The overlay sits at `z-index: 2100` (above dialogs at 2000/2001, toasts at 1900) so it can't be dismissed. Mirrors V1's `chatTriggerServerRestart` behavior.
- `public/v2/src/screens/*.jsx` + `public/v2/src/app2.jsx` — slide-deck-only mock screens plus the deck scaffold. Not loaded by `index.html` (except `kbBrowser.jsx`, `filesBrowser.jsx`, and `settingsScreen.jsx`, which are); only `deck.html` references the rest. The mock `KbScreen` (graph-look force-layout) still lives in `screens/kb.jsx` for the deck; the mock `FilesScreen` lives in `screens/files.jsx`.

**Wiring progress (real app at `/v2/`):**
- **Sidebar** — subscribes to `StreamStore.getConvList()` / `StreamStore.subscribeConvList()` (the store is the single source of truth for the conversation list) and triggers `StreamStore.loadConvList({ query, archived })` whenever the debounced search query or archive view flag changes. The store fires `GET /api/chat/conversations` via `AgentApi.listConversations({ q, archived })` and notifies all subscribers when the result arrives. Sidebar groups items by last-two-segments of `workingDir` and renders rows sorted by `updatedAt` (server order) with a relative-time meta. Clicking a row (or pressing Enter/Space when focused) calls `onSelect(convId)` which sets the app's `activeConvId` state. Rows consult the `convStates` map to decide whether to apply `.streaming` (pulsing blue dot, row text un-dimmed, meta coloured `--status-running`), `.awaiting` (amber halo dot, meta coloured `--status-awaiting` — a plan approval or clarifying question is pending for that conv), `.error` (red dot), or `.unread` (theme-adaptive ink dot — see *Unread state* below). Per-workspace group header: the **KB button** (book icon) is rendered only when the group has `kbEnabled: true` (true if any conversation in the group carries `workspaceKbEnabled`); it is **always visible** (bypasses the hover-only `.actions` fade) and fires `onOpenKb(hash, label)` which triggers the modal-swap KB view in `App`. The **Files** icon button sits in the hover-only `.actions` span alongside it — hover on `.sb-ws` reveals it, and clicking fires `onOpenFiles(hash, label)` which triggers the modal-swap Files view in `App`. The **Workspace settings** icon in the same span is still visual-only. The footer `Settings` gear is wired — clicking it fires `onOpenSettings()` which triggers the modal-swap Settings screen in `App` (no per-workspace context). The `New conversation` button is wired — clicking it fires the `onNewConversation` prop, which `App` hooks up to open a `<FolderPicker>` modal (see `public/v2/src/folderPicker.jsx`). The picker navigates directories via `GET /browse`, creates/deletes folders via `POST /mkdir` and `POST /rmdir`, and calls `App.createConv(path)` on "Select this folder" or `createConv(null)` on "Use default (workspace)" — `createConv` fetches the user's `defaultBackend` via `AgentApi.settings.get()`, POSTs `/conversations` with `{ workingDir, backend }`, calls `StreamStore.prependConvListItem(conv)` so `<Sidebar>` shows the new row immediately, clears any open modal-swap views, and selects the new conv. Errors surface through `dialog.alert({ variant: 'error' })`. The button is disabled when no `onNewConversation` is provided (e.g., inside the slide deck). The `⌘K` **conversation search** is wired — the `.sb-search` block renders a real `<input ref={searchInputRef}>` bound to local `query`/`setQuery` state with a **300 ms debounce** useEffect that copies `query` into `debouncedQuery`; a second useEffect watches `debouncedQuery` and `viewingArchive` and calls `StreamStore.loadConvList({ query: debouncedQuery, archived: viewingArchive })`, which itself invokes `AgentApi.listConversations({ q, archived })` (non-empty `q` triggers a server search that matches against title, lastMessage, and full message `content` case-insensitively via `chatService.searchConversations`). Rapid typing is safe: the store stamps each load with an internal `reqGen` counter and discards stale responses before they overwrite newer state, so the Sidebar's subscriber always sees the latest matching result. A window-level `keydown` listener handles `⌘K` / `Ctrl+K` (ignoring Shift/Alt modifiers) by calling `preventDefault()` then `searchInputRef.current.focus() + select()` so the shortcut re-focuses and re-selects any existing text. In-input **Escape** clears `query` and blurs the input. The right side of the input shows either a `<kbd className="sb-search-kbd">⌘K</kbd>` hint when the query is empty or a `<button className="sb-search-clear">` (× icon) that clears the query and re-focuses the input when the query is non-empty. The search-results empty state ("No matches for `<query>`.") replaces the default "No conversations yet…" text whenever `debouncedQuery` is set and `convs.length === 0`, so a search miss reads as a search miss instead of an empty account. The **archive view** is wired — `App` owns a `viewingArchive: boolean` state and passes it plus `onToggleArchive()` / `onRestore(convId)` down to `<Sidebar>`. A full-width `.sb-archive-toggle` button sits between the scrollable conversation list and the footer; its label flips between `<Ico.archive/> Archive` (active view) and `← Back to conversations` (archive view). Clicking the toggle flips the boolean — entering archive mode also clears `activeConvId`, `kbView`, `filesView`, and `settingsView` so the empty-main placeholder replaces any open conversation/modal-swap view (matches V1's `chatToggleArchiveView`). The same Sidebar effect that watches `debouncedQuery` includes `viewingArchive` in its deps, so flipping the toggle re-runs `StreamStore.loadConvList({ query, archived })` with the new flag (search still works inside the archive). Each archived row swaps its trailing `.meta` timestamp for an `.sb-row-restore` icon button (`Ico.reset`) — clicking it calls `AgentApi.restoreConversation(id)` (`PATCH /conversations/:id/restore`) and `StreamStore.removeConvListItem(id)` so the row disappears from the archive view immediately without a refetch. Errors surface via `dialog.alert({ variant: 'error' })`. The empty-state copy reads "No archived conversations." when `viewingArchive && !debouncedQuery && convs.length === 0`; search misses still say "No matches for `<query>`.". Clicking **New conversation** while viewing archive flips `viewingArchive` back to `false` before opening the folder picker (matches V1 behavior); `createConv` also resets the flag after a successful POST so the new conv shows up in the active list immediately. The sidebar footer's left side renders a **user badge** (`SidebarUser` in `primitives.jsx`) showing the signed-in identity. `App` fires `AgentApi.getMe()` on mount (`GET /api/me` → `{ displayName, email, provider }`) and passes the result down as the `user` prop. The avatar circle shows the **identity provider's logo** (`ProviderLogo.google` — multi-color Google "G"; `ProviderLogo.github` — dark octocat, `#24292f`) on a neutral white background (`.avatar-provider` CSS modifier adds `background: #fff` + a 1px inset border so the brand colors read against the sidebar surface) with a `Signed in with Google` / `Signed in with GitHub` native `title` tooltip. The label next to the avatar renders `user.displayName` (falling back to the local-part of `user.email`, then `"User"`). While the fetch is in flight (or when `<Sidebar>` is rendered from the test screens with no prop), the avatar is an empty `.avatar-empty` placeholder (neutral surface + border, no content) to avoid layout shift. Local-dev sessions that bypass OAuth receive `{ displayName: null, email: null, provider: null }` from the server and also fall through to the placeholder. A **Sign out** button (`Ico.logout`) sits to the right of the Settings gear in the sidebar footer (both are `.sb-footer-icon` buttons); clicking it fires `onSignOut(e.currentTarget)` which `App` handles by prompting `dialog.confirm({ anchor, title: 'Sign out?', body: 'You will be redirected to the sign-in page.', confirmLabel: 'Sign out' })` and on confirm redirects the browser to `/auth/logout` (mirrors V1's `chat-signout-btn` handler in `public/js/main.js` — the server destroys the session and redirects to `/`). The **version indicator** (`<VersionIndicator/>`) sits as a final `.sb-version` row below `.sb-footer`, rendered by the `Sidebar` itself. On mount it fires `AgentApi.getVersion()` to populate the `vX.Y.Z` text (a clickable `.sb-version-text` button — click triggers a manual `AgentApi.checkVersion()` that flips the label to "checking…" until the POST resolves). A 5-minute `setInterval` calls `AgentApi.getUpdateStatus()` for background update detection. When the server reports `updateAvailable && remoteVersion`, an amber `.sb-update-badge` ("v{remoteVersion} available") appears next to the version text — clicking it calls `onShowUpdate({ localVersion, remoteVersion })` which `App` handles by setting `updateTarget` state, rendering `<UpdateModal open={true}/>`. Confirming the update in the modal fires `AgentApi.triggerUpdate()`; on success the modal closes and `App` flips into `restarting: true` which swaps the modal for `<RestartOverlay/>` (full-screen curtain that auto-reloads the page at 6000 ms). The badge uses `--status-awaiting` (amber) via `color-mix` rather than the accent color so it reads as a "pay attention" affordance without alarming the user. Matches V1's `public/js/main.js` version-indicator init (`chatManualVersionCheck` / `chatCheckUpdateIndicator` / `chatShowUpdateModal` / `chatTriggerUpdate` / `chatShowRestartOverlay`) end-to-end.
- **ChatLive (PR 3a + 3b + 4a + 4b)** — on `convId` change the `useConversationState` hook asks `StreamStore` to load the conv (which fetches `/api/chat/conversations/:id` into the entry's `conv` + `messages` + `usage` once per session) and to open the WebSocket (`StreamStore.ensureWsOpen(convId)`; no-op if already open). The view renders the topbar (workspace label · title on the left; context chip + Download + Reset + Archive on the right — see *Topbar actions* below) and a scrollable feed of bubbles. Because `<ChatLive>` has no streaming state of its own, every send/reset/archive action delegates to the store (`StreamStore.send`, `StreamStore.reset`, `StreamStore.archive`) and the view simply re-renders from the new snapshot.

  **Topbar actions:**
  - **Context chip** (`ContextChip`) — routes through `getChipRenderer(backendId)` (see `public/v2/src/chip-renderers.jsx`) so each CLI picks its own chip text + tooltip card. The chip is a `.u-mono` span wrapped in `<Tip variant="stat" rich={card}>` when the renderer returns a card, else a bare span; hidden entirely when `renderChipText(usage)` returns `null`. On mount, `ContextChip` picks the matching plan-usage singleton for the current backend (`claude-code` → `window.PlanUsageStore`, `kiro` → `window.KiroPlanUsageStore`, `codex` → `window.CodexPlanUsageStore`, else `null`), subscribes to it, and fires its `refresh()` once so the tooltip reflects the freshest cached account plan snapshot; the subscription updates the component's local `planUsage` state whenever the store's cache changes (refreshed by the `streamStore` `done` frame handler on every completing turn for that backend). The `planUsage` value is forwarded to the renderer as `renderTooltipCard(usage, { planUsage })` — renderers that don't read `opts.planUsage` simply ignore it. Switching backends tears down the subscription cleanly via the effect cleanup (the `store` reference is an effect dep, so changing `backendId` fires the old unsubscribe and rebinds to the new store). Current renderers: **`claude-code`** — chip reads `{tokensK}` or `{tokensK} · ${cost}` (hidden when `input+output === 0` — which keeps the account plan usage hidden behind the chip until there's at least one turn of session activity; this is intentional per the scoping decision to keep visible chip text unchanged); tooltip card shows total tokens (+ `$cost` when > 0) in the `.tt-h` title, a Breakdown section with Input / Output / Cache read / Cache write rows (cache rows hidden when 0), and — when `planUsage` is non-null — a nested `PlanUsageSection` rendering the account-wide plan tier, per-bucket utilization bars (5-hour session, weekly total, weekly Opus/Sonnet, plus any codename buckets upstream ships before they land with stable names), an `Extra credits` row when `extra_usage.is_enabled`, and an `Updated Xm ago` / `Last update failed` footer coloured by the `stale` / `lastError` state. Cost is taken straight from `usage.costUsd` — the Claude CLI already factors cache pricing. **`kiro`** — chip reads `{pct}% context · {tokensK}` when both are present, `{pct}% context` when tokens are 0, `{tokensK}` when pct is null (hidden when both missing); tooltip card shows `{pct}% context` or `{tokensK} tokens` in the title, a Context window progress bar section (only when pct is set), an Input/Output Tokens section (only when total > 0), and — when `planUsage` is passed — a nested `KiroPlanUsageSection` rendering the account-wide plan header (title-cased `subscription.subscriptionTitle` — `Free`, `Pro`, etc.), a credits-used progress bar (`{displayNamePlural || 'Credits'}` on the left, `{used} / {limit}` on the right with an optional inline `· resets {Mon D}` suffix), an Overage row when `overageStatus === 'ENABLED'` (credit count + dollar charges when `currentOveragesWithPrecision > 0`, or `Enabled · none used` when zero), a Bonus credits row showing the count of entries in `breakdown.bonuses` when any exist, and an `Updated Xm ago` / `Last update failed` footer colored by the `stale` / `lastError` state. Kiro doesn't surface a dollar cost for the session, so none is shown in the title. **`codex`** — chip mirrors Kiro's shape: `{pct}% context · {tokensK}` when both are present, `{pct}% context` when tokens are 0, `{tokensK}` when pct is null (hidden when both missing — Codex emits `contextUsagePercentage` on every assistant turn so the chip lights up after the first response); tooltip card shows `{pct}% context` or `{tokensK} tokens` in the title, a Context window progress bar section (only when pct is set), an Input/Output Tokens section (only when total > 0), and — when `planUsage` is passed — a nested `CodexPlanUsageSection` rendering the account-wide plan header (humanized via `_humanizeCodexPlan` — `Plus`, `Pro`, `Business (PAYG)`, etc.), one progress bar per active rate-limit window (`5h session` → `windowDurationMins: 300`, `Weekly` → `10080`), an optional `Credits` row (only when `credits.hasCredits === true` — ChatGPT subscribers with no API credits don't see it), and an `Updated Xm ago` / `Last update failed` footer colored by the `stale` / `lastError` state. Codex doesn't surface a dollar cost for the session, so none is shown in the title. **Default renderer** — catches unknown backend IDs; chip is `{tokensK}` or `{tokensK} · ${cost}`; tooltip card is the same stat layout as Claude Code minus the cache rows. Extending the registry for a new CLI is a one-line addition to `CHIP_RENDERERS` in `chip-renderers.jsx`. `tokensK` is total tokens (in + out) formatted as `20.4k` for ≥ 1000, `3.1M` for ≥ 1M, else raw. `$cost` is formatted with 2 decimals at ≥ 1 USD, 4 decimals below.
  - **↓ Download** — direct GET to `/api/chat/conversations/:id/download` (opens in a new tab; no CSRF needed; server streams the markdown file with `Content-Disposition: attachment`).
  - **↺ Reset** — `POST /api/chat/conversations/:id/reset` with an empty body. Prompts via `dialog.confirm({ anchor: <button>, title: 'Reset this conversation?', body: '…', confirmLabel: 'Reset' })` (anchor captured from the button's `e.currentTarget`). While the POST is in flight, `state.resetting` flips to `true` and ChatLive renders a `<ResetProgressBubble>` at the foot of the feed — an assistant bubble with the Agent Cockpit logo avatar, three staggered `.typing-dot` circles, and the label "Archiving session…" (the server-side reset archives the session, captures memory, runs post-session extraction, and re-initialises the backend, which can take several seconds). The button label flips to "↺ Resetting…" and both the button and the composer-side buttons that gate on `streaming/sending` stay disabled for the duration. On success: `StreamStore.reset` invalidates the conversations cache and refetches the conversation so the feed reflects the new empty session, copies the freshly-fetched `sessionUsage` into the entry's `usage` (so the topbar chip zeroes immediately for the new session), clears `resetting`, returns `true`, and `ChatLive` fires `toast.success('Session reset')` for confirmation. On failure `StreamStore.reset` clears `resetting`, sets `streamError`, returns `false`, and no toast fires. Mirrors V1's `chatResetSession` in-feed progress indicator.
  - **Archive** (danger style) — `PATCH /api/chat/conversations/:id/archive`. Prompts via `dialog.confirm({ anchor, title, body, confirmLabel: 'Archive', destructive: true })` — `destructive: true` auto-maps the variant to `'warn'` and renders the primary button in red. On success: calls `onArchived()` which clears `activeConvId` in `App` (switching to the empty-main placeholder) and invalidates the conversations cache so the row disappears from the sidebar on next mount. Disabled while a stream is active.

  User-message bodies run through `UserMessageBody`, which calls `extractUploadedFiles(content)` to strip any trailing `[Uploaded files: /abs1, /abs2, …]` tag from the persisted text. The cleaned text renders as plain `white-space: pre-wrap`; each extracted path then renders below the text inside a `.file-cards` column — images (by extension: `.png|.jpe?g|.gif|.webp|.svg|.bmp`) become inline `.user-image-thumb` thumbnails (`<button>` wrapping an `<img src={GET /conversations/:id/files/:filename}>`, click opens `ImageLightbox` via `openLightbox(src, alt)`) and non-images become `UploadedFileCard`s with View + Download buttons (endpoint: `GET /conversations/:id/files/:filename?mode=view|download`; View opens `FileViewerPanel` with the descriptor `{ filename, viewPath, imageUrl: null, displayPath }`). `FileViewerContext` threads both `wsHash` (workspace-scoped FILE_DELIVERY) and `convId` (conv-scoped uploaded files) plus `openFileViewer(descriptor)` + `openLightbox(src, alt)` down to the nested components. Assistant bodies walk the message's ordered content blocks (see *Assistant body rendering* below). The composer is a `<textarea>` with Enter-to-send (Shift+Enter inserts a newline) and a round send button; the composer is disabled while a stream is active. Below the textarea, a chip row (`ComposerPicks`) exposes three cascading pickers — **Backend**, **Model**, **Effort** — that control which CLI to spawn on the next send (see *Composer pickers* below). A paperclip button (`.attach`) sits to the left of the pickers and a `<FileChips>` row sits above the textarea; together they drive per-conversation file attachments (drag-and-drop onto the chat pane or click-paperclip-to-pick; sent as multipart via `POST /conversations/:id/upload` and embedded in the next message body as `[Uploaded files: …]`; see *File attachments* below).

  **Per-conversation state lift (PR 4b)** — `App` reads the sidebar's per-row indicator map from `useConvStates()`, which is a thin subscription over `StreamStore.subscribeGlobal` (fires when any conv's `uiState`, `unread`, or first-touch transitions). Every visited conv has an entry in the store and contributes to `StreamStore.convStates()`, so multiple rows can light up simultaneously — e.g. the user can kick off a stream in conv A, switch to conv B, kick off a second stream there, and both rows show `.streaming` in the sidebar while only one `<ChatLive>` is mounted. The store flips the entry's `uiState` to `'streaming'` on send, `'error'` on stream error, and `null` on `done`. `convStates()` also surfaces `'unread'` for entries with the unread flag set, and `'idle'` as a sentinel for any touched entry that is currently neither active-state nor unread (used by the sidebar to override the stale `c.unread` it may still be holding from the cached server list — see *Unread state* below).

  **Refresh-time hydration (`StreamStore.hydrateActiveStreams`)** — a page refresh wipes the whole in-memory `StreamStore` (new JS context), but the CLI streams themselves keep running on the server and their WS event buffers are held for 60 s of grace. Without intervention the sidebar would show gray dots on those live conversations until the user clicked into each one (which triggers `ensureWsOpen` → buffer replay → `uiState` rebuilt). To close that gap, `Sidebar` calls `StreamStore.hydrateActiveStreams()` once on mount: it fetches `GET /api/chat/active-streams` (returns `{ ids: string[] }` of every convId currently registered in the router's in-memory `activeStreams` map) and seeds `uiState: 'streaming'` on each ID that doesn't already have an open WS. For seeded convs the user never opens, the dot stays blue until the next hydrate (the Sidebar effect runs once per app mount — not on search/archive-toggle re-fetches). The first WS replay corrects the state to `'awaiting'`/`null`/`'error'` as appropriate. Non-fatal on network/auth errors — hydration just silently no-ops so the sidebar still renders with what's in the server-cached `conversations` list.

  **Unread state** — a conv is "unread" when a response stream completed on it while the user was viewing a different conv. Tracked in three places that all stay in sync:
  - **Server (authoritative)** — `unread: boolean | undefined` on the workspace-index entry. Toggled via `PATCH /api/chat/conversations/:id/unread` (see API spec). Persisted across reload.
  - **Server-cached list** — every `conversations` summary returned by `AgentApi.listConversations()` carries the boolean as `c.unread`. The sidebar uses this for cold rows (rows the user hasn't visited this session, so there's no `StreamStore` entry for them).
  - **`StreamStore` (per-conv, in-memory)** — `ConvState.unread` (`boolean`) on every visited conv. The sidebar uses `StreamStore.convStates()` for any conv that has an entry, falling back to `c.unread` only when no entry exists. This split is what lets the sidebar clear the dot **immediately** on selection without waiting for a `/conversations` refetch.

  **Auto-mark on done** — `StreamStore.handleFrame`'s `done` branch flags `unread = true` on the target conv whenever `convId !== activeConvId` and the stream landed in idle (no `pendingInteraction`, no `streamError`). The shell pushes the active id into the store via `StreamStore.setActiveConvId(id)` from a `useEffect` on `activeConvId`, so the store always knows which conv (if any) is currently visible. The flag is mirrored to the server via `AgentApi.markConversationUnread(convId, true)` (fire-and-forget — failures are intentionally swallowed; the next `/conversations` fetch reconciles).

  **Manual mark / clear** — selecting a conv (`onSelectConv` in `shell.jsx`) calls `StreamStore.markRead(id)`, which always touches local state (creating a blank entry on cold convs so `convStates()` returns `'idle'` to override `c.unread`) and POSTs `unread: false`. The user can left-click the dot on any non-active, non-active-state row to manually re-flag the conv as unread; the sidebar passes the click through `onMarkUnread` → `StreamStore.markUnread(id)`. The dot click uses `event.stopPropagation()` so it doesn't also select the row.

  **Sidebar render priority** — for each row, `Sidebar` resolves the dot like this: live state (`'streaming'` / `'awaiting'` / `'error'`) wins; otherwise unread is derived from `state === 'unread'` (touched, currently unread) or `state == null && c.unread === true` (untouched fallback to server). `state === 'idle'` (touched, currently read) wipes the dot to neutral. The unread dot is rendered as a `<button class="dot dot-btn">` only when the row is non-active and not in any other state — otherwise it's a non-interactive `<span class="dot">`. The CSS rule `.sb-row.unread .dot { background: var(--text); }` paints a theme-adaptive ink dot (near-black on light themes, near-white on dark).

  **Send flow (all logic lives in `StreamStore.send(convId, text)`; the view just invokes it):**
  1. Ensure the conv's WS is open (`ensureWsOpen` resolves the pending open promise or opens a new socket; a 5 s connect timeout is surfaced as an error).
  2. Optimistically append a pending user message and an empty pending-assistant placeholder (with an empty `contentBlocks` array); mark the placeholder's id as the entry's `streamingMsgId`; flip `uiState` to `'streaming'`.
  3. Before building the POST body, gather all completed file attachments (`pendingFiles` entries whose `status === 'done'`); when any exist, append `[Uploaded files: <abs-path-1>, <abs-path-2>, …]` to the content (on a fresh line separated by `\n\n`; the tag alone if the textarea was empty). The attachments were already uploaded to the conv's artifacts dir by `addFiles` — the server paths are what the LLM sees, and the message body is their only channel. Snapshot `pendingFiles` before clearing so a POST failure can restore the chips intact (the files are still on disk server-side, so there's no re-upload). `POST /api/chat/conversations/:id/message` with `{ content, backend?, model?, effort? }` via `AgentApi.fetch` (CSRF-token header is attached automatically; a 401 triggers the registered session-expired handler). The optional `backend/model/effort` fields ride along only when the composer pickers have a value (either hydrated from the conv record or touched by the user). The server-side route (see Chat Routes spec) conditionally updates the conversation row when any of the three differs from what's currently stored, so the chosen picker values become the new defaults for the next turn. On success, swap the pending user message for the server-authoritative `userMessage` returned in the response and mirror the picker values back onto the entry's `conv.backend/model/effort` so future renders see the persisted truth.
  4. WebSocket frame handling runs inside `StreamStore.handleFrame(convId, frame)` — every frame updates the target conv's entry regardless of which view is currently mounted:
     - `text` — `{ content }` delta appended to the streaming placeholder's ordered `contentBlocks`. Consecutive `text` deltas merge into the last text block so one contiguous paragraph produces one block.
     - `thinking` — `{ content }` delta appended as a `thinking` block (same merge rule as text).
     - `tool_activity` — pushes a new `{ type: 'tool', activity: { tool, description, id, duration: null, startTime, isAgent?, subagentType?, parentAgentId? } }` block onto `contentBlocks`. Plan-mode markers (`isPlanMode`) and clarifying-question frames (`isQuestion`) are *not* pushed as tool blocks; instead they update `pendingInteraction` + `uiState='awaiting'` (see *Plan approval & clarifying questions* below).
     - `tool_outcomes` — patches the matching tool block(s) in the streaming placeholder by `activity.id === toolUseId`, setting `outcome` and `status`.
     - `assistant_message` — authoritative persisted message; replaces the placeholder **and clears `streamingMsgId`** so the next delta opens a fresh placeholder for the next segment. This is the *real* segment boundary on the server (the server only emits `assistant_message` when it just persisted an intermediate/final message). Intermediate segments land with `turn: 'progress'`, the final segment with `turn: 'final'`. The feed render collapses runs of consecutive `turn: 'progress'` assistant messages into a progress breadcrumb (see *Progress-breadcrumb collapse* below).
     - `turn_complete` — **explicit no-op** (documented branch in `handleFrame`, not a silent drop). The server emits `turn_complete` right after every `turn_boundary` CLI event, which V1 uses to archive `activeTools`→`toolHistory` and stop a shared activity timer. V2 doesn't need that archival: tools live as `contentBlocks[]` on the streaming assistant message and `tool_outcomes` mutates them in place, turn splits already happen on `assistant_message` (placeholder swap + `streamingMsgId = null`), and live activity timers are per-block and self-clear when `duration` lands. Kiro never emits `turn_boundary`, so this frame is Claude-Code-only; the Kiro adapter yields `done` directly from its `turn_end` handler.
     - `replay_start` — wipes the streaming placeholder's `contentBlocks` (and `content`, and any `pendingInteraction`/`planModeActive`) so the server's buffered frames can rebuild state cleanly on reconnection. Keeps `streamingMsgId` intact so replayed `text`/`tool_activity` frames land on the same placeholder message id. No-op when `streamingMsgId` is null (nothing to wipe). Backend-agnostic: the server's per-conv ring buffer (`ws.ts`, 1000-event cap, 60 s disconnect grace) captures every frame regardless of adapter, so replay affects both Claude Code and Kiro conversations identically. Without this wipe, navigating away from and back to an in-progress conversation would duplicate text + tool rows in the placeholder until a live `assistant_message` overwrote it.
     - `replay_end` — explicit no-op. State was rebuilt by the normal handlers during replay; React subscribers already re-render on each update, so there's no additional reconcile step to run. V1 uses this frame to trigger a DOM re-render and re-show pending interaction dialogs, but V2's declarative view layer handles that implicitly.
     - `title_updated` — updates the entry's `conv.title` in-place and invalidates the cached conversations list so the sidebar re-fetches on next mount.
     - `usage` — refreshes the entry's `usage` from the frame's `sessionUsage` field (session-scoped totals; conversation-level totals on the same frame are intentionally ignored here so the topbar chip reflects only the active session and zeroes on reset).
     - `assistant_message` — also calls `bumpConvListActivity(convId, frame.message.timestamp)` so the sidebar row's relative-time label and list ordering reflect the just-persisted server timestamp without a full list re-fetch (see *Sidebar live activity* below).
     - `error` — flips `streamError` and `uiState` to `'error'`, clears `pendingInteraction` (a dead stream can't accept an answer).
     - `done` — clears `streaming` and `streamingMsgId`; `uiState` resolves to `'error'` if one already landed, `'awaiting'` if `pendingInteraction` is still set, else `null`. `pendingInteraction` itself is preserved across `done` so the user can still answer after the stream's finally block runs. When the stream lands in idle (no `streamError`, no `pendingInteraction`) and `convId !== activeConvId`, the entry's `unread` is also flipped to `true` and `AgentApi.markConversationUnread(convId, true)` fires (see *Unread state* below).
  5. On POST failure, the pending user and assistant placeholders are rolled back and the error is shown.

  **Assistant body rendering (`AssistantBody` → `groupBlocksForRender` → `renderSegment`):** `contentBlocks` is authoritative when present. For legacy messages saved before the field existed, a fallback in `deriveBlocks` synthesizes blocks from the message's `thinking`/`toolActivity`/`content` fields (thinking first, then tools, then the text body) so older conversations render without special cases. `groupBlocksForRender` then walks the blocks and splits them into three kinds of segments: `text`, `thinking`, and `tool-run` (consecutive tool blocks merge into one run so they can be grouped as a unit). Each segment renders as:
     - `text` → `.prose` container with the markdown pipeline (`marked.parse` + `DOMPurify.sanitize`). Fenced code blocks land as `<pre><code>…</code></pre>`; `.prose pre` renders them as a monospace panel with `overflow-x: auto`, `white-space: pre`, and the `--surface-2` background so the block is visually distinct and selectable/copy-able.
     - `thinking` → `ThinkingBlock` — a dashed-border pill button that, when clicked, toggles a below-pill panel showing the raw thinking text (`white-space: pre-wrap`, dimmed). The collapsed label shows a short preview (first 80 chars of line 1) so the user can tell segments apart at a glance.
     - `tool-run` → `ToolRun`, which feeds its activities into `partitionToolRun` and then renders each segment via `SubagentCard` (for agents) or `ToolGroup` (for parallel/sequential clusters of non-agent tools). See *Tool-run grouping* below.

  **Tool-run grouping (`partitionToolRun` + `partitionParallel`):**
  - Cross-message subagent index (`buildAgentIndex` + `AgentIndexContext`): a subagent's `Agent` tool_use and its internal children often end up persisted on *different* messages (the Agent's accompanying "Now spawning..." text triggers an early save, then the subagent's child tool_uses accumulate on the next saved message). To reassemble the subagent card, the feed-level `AgentIndexProvider` walks the entire `messages` list before rendering and produces `{ agentIds, childrenByAgent }`: `agentIds` is the set of every `isAgent` tool ID seen, and `childrenByAgent` maps each agent ID to the flat list of child activities (from *any* message) whose `parentAgentId` points at it, in chronological order. The provider wraps the whole feed so every `ToolRun` down the tree reads the same index via `React.useContext(AgentIndexContext)`.
  - `partitionToolRun(tools, agentIndex)` walks the run in order. Activities whose `isAgent === true` become `{type:'agent'}` segments whose `children` come from `agentIndex.childrenByAgent.get(activity.id)` (partitioned themselves via `partitionParallel`). Activities whose `parentAgentId` appears in `agentIndex.agentIds` are skipped at the top level — they render inside the parent card wherever that card lives, even if it's on a different message. Everything else accumulates into a buffer that flushes through `partitionParallel` whenever an agent interrupts (preserving the source order).
  - `partitionParallel(tools)` merges consecutive activities that share a server-assigned `batchIndex` (see Backend Services spec) into `{type:'parallel'}` clusters; activities across distinct `batchIndex` values become separate `{type:'sequential'}` clusters. The server bumps `batchIndex` on every CLI `turn_boundary` event, so consecutive tools tagged with the same index are exactly the parallel tool_uses of a single LLM assistant turn — this is authoritative regardless of how tool execution time spaces the events. **Legacy fallback:** tools persisted before the server started tagging `batchIndex` lack the field; for those, `partitionParallel` falls back to the 500ms `PARALLEL_THRESHOLD_MS` startTime-proximity rule used by the original `chatGroupParallelItems()` helper. Single activities become a single-item `sequential` group.
  - `ToolGroup` renders the cluster as a `.tools` card. Parallel clusters add the `.parallel` modifier (left rail) and always show a `.tools-head` with a `PARALLEL` tag plus step count; sequential clusters only show the header when they have two or more items (a lone tool renders as a bare row inside a plain `.tools` card, per the mock).
  - `SubagentCard` renders the agent activity as a `.subagent` card. The header shows `.chip` (subagentType), `.title` (the agent's description/tool), and `.elapsed` (duration or `…`). The body contains nested `ToolGroup` cards for the agent's partitioned children; CSS strips the child cards' outer border/rounding so the parent card supplies the chrome.
  - `ToolRow` renders an individual activity. The leading marker is `.run` (pulsing amber) while the tool is in flight, `.done` (green) once `outcome` or `duration` lands, and `.err` (red) when `status === 'error'`. The right-hand cell shows the outcome text when available, else the duration in `ms`, else `…` while running. When the server sends `tool_outcomes` without a `duration` (some backends don't), `patchToolOutcomes` computes elapsed from the activity's local `startTime` so the row flips to `.done` with a usable timing.

  **Progress-breadcrumb collapse (`collapseProgressRuns`):** the feed render walks `messages` through `collapseProgressRuns()` before mapping into bubbles. The helper produces three kinds of entries:
  - `plain` — any message that isn't an assistant `turn: 'progress'` and isn't preceded by a progress run. Rendered as a normal `MessageBubble`.
  - `final-with-progress` — a run of consecutive assistant `turn: 'progress'` messages immediately followed by any non-progress assistant bubble: either a persisted `turn: 'final'` message, or the live streaming placeholder (which has no `turn` field). In the streaming-placeholder case, the in-flight multi-segment turn stays in a single agent presence — the breadcrumb of already-saved segments on top, the currently streaming content below — instead of splitting into a standalone breadcrumb bubble above a separate streaming bubble. Rendered as the trailing bubble with the progress run passed in as `attachedProgress`, which renders inside the bubble (below the head, above the streaming/final text) as a collapsed `ProgressBreadcrumb` dashed pill: summary shows `N segment{s} · <first-text-preview>` and a chevron; clicking expands a body that renders each progress message's full content (text / thinking / tool-runs) as a `.progress-row`. When the trailing bubble is the streaming placeholder, `isStreaming` is forwarded so the head reads `streaming…` and copy actions stay hidden.
  - `progress-trailing` — a run with no following assistant bubble at all (stream aborted mid-turn before a new placeholder/final was attached). Rendered as its own `ProgressBreadcrumbBubble` — a stand-alone agent bubble whose head reads `progress · N segment{s}` and whose body is the `ProgressBreadcrumb`.

  **Streaming-target indicator:** the active streaming placeholder's bubble header shows `streaming…` in place of the relative timestamp.

  **Plan approval & clarifying questions (PR 4c)** — the server emits two interactive `tool_activity` frames that require a user response rather than being executed like normal tools:
  - `{ tool: 'ExitPlanMode', isPlanMode: true, planAction: 'exit', planContent }` — the LLM has finished drafting a plan and is asking for approval to proceed. `planContent` is the markdown body of the plan; the server falls back to the accumulated assistant text when no explicit plan file was captured.
  - `{ tool: 'AskUserQuestion', isQuestion: true, questions: [{ question, options: [{label, description?}] }], description }` — the LLM needs a clarifying answer before continuing. Only the first question in the array is surfaced; `options` may be empty (free-form answer only) or contain labelled choices.

  `StreamStore.handleFrame` catches both and updates the target conv's entry with:
  - `pendingInteraction = { type: 'planApproval', planContent }` or `{ type: 'userQuestion', question, options }`
  - `uiState = 'awaiting'` — the sidebar row flips to the amber halo dot (`.sb-row.awaiting`) and every visible conv shows its own pending state independently.

  `tool_activity` frames with `planAction: 'enter'` are ignored (they mark the *start* of plan mode, which isn't actionable). Neither interactive frame is pushed onto the streaming message's `contentBlocks` — they are presented entirely through the dedicated card UI below.

  **Stream error card:** when `state.streamError` is non-null (set by a WS `error` frame, a failed `/message` POST, or an `onerror`/`onclose` on the socket itself — the literal string `"WebSocket error"`), `<ChatLive>` renders a `<StreamErrorCard>` at the foot of the feed (after `InteractionCard`). The card uses the mock's `.err-card` / `.err-head` / `.err-actions` classes: accent-red left border, uppercase mono head with the error message in dim mono on the right, a prose body that says how many queued messages are paused, and two action buttons — **Dismiss** (always shown, clears the error) and **Resume queue** (only rendered when `queue.length > 0`). Both buttons call `StreamStore.clearStreamError(convId, opts)` which nulls `streamError`, recomputes `uiState` (`'awaiting'` if a `pendingInteraction` is still set, else `null`), and — when `opts.resumeQueue === true` — immediately calls `drainQueueIfReady(convId)` so the head of the queue fires as a fresh `/message` POST. Because `drainQueueIfReady` checks `streamError` before acting, the error must be cleared first.

  **Card rendering (inside `<ChatLive>`):** when `state.pendingInteraction` is set, `<ChatLive>` renders an `<InteractionCard>` at the bottom of the feed (after messages, before `streamError`). The card dispatches on interaction type:
  - `PlanApprovalCard` — amber-top `.plan` card with a `NEEDS YOU` chip, the rendered `planContent` inside a `.prose` container (via the same `marked + DOMPurify` pipeline used for message bodies), and `.plan-actions` with `Reject` (danger) / `Approve & run` (primary) buttons. Clicking either calls `StreamStore.respond(convId, 'yes' | 'no')`.
  - `QuestionCard` — accent-top `.plan` card with a `Clarifying question` header, the question text as `.plan-title`, an optional row of option buttons (each pre-fills the answer input when clicked; `title` attribute carries the option description), a focused `<input>` for free-form typing, and a `Send` button. Enter submits. The submit calls `StreamStore.respond(convId, text)`.

  While `pendingInteraction` is set, the composer's textarea is disabled with placeholder `"Answer the prompt above to continue…"` and the send button is disabled (`awaiting` joins `sending`/`streaming` in `canSend`'s guard). Users cannot send an ordinary composer message while an interaction is pending — they must click Approve/Reject or answer the question.

  **Answer delivery (`StreamStore.respond`):**
  1. Set `respondPending = true` (disables card buttons; shows `Sending…` in the primary button).
  2. `POST /api/chat/conversations/:id/input` with `{ text, streamActive: streaming || sending }`. The `streamActive` hint tells the server whether the client still considers the CLI stream live, so the server can avoid racing a just-finished stream.
  3. On `{ mode: 'stdin' }` — the server piped the answer straight into the CLI's stdin; the existing stream will produce more frames. Clear `pendingInteraction`, reset `respondPending`, and set `uiState` back to `'streaming'` (if still streaming), `'error'` (if already errored), or `null`.
  4. On `{ mode: 'message' }` — the stream has already ended. Clear `pendingInteraction`, reset `respondPending`, then delegate to `StreamStore.send(convId, text)` which runs the normal send flow (optimistic user message, POST `/message`, fresh stream via the existing WS).
  5. On network/HTTP error — surface via `streamError` and flip `uiState` to `'error'`.

  **Lifecycle edges:**
  - `reset(convId)` clears `pendingInteraction` and `uiState` along with the rest of the session.
  - `send(convId, text)` clears `pendingInteraction` as part of the optimistic update — if the user bypasses the card by typing into the composer (which only happens when we're not correctly guarding — see above), we don't leave a stale interaction behind.
  - `archive(convId)` deletes the whole entry so `pendingInteraction` naturally goes away.
  - A new `error` frame mid-interaction clears `pendingInteraction` (a dead stream can't accept the answer).
  - A `done` frame *preserves* `pendingInteraction` — the stream's `finally` block runs but the user can still answer; the next `respond()` call will get `mode: 'message'` and transparently start a new send.

  **Composer pickers (`ComposerPicks` + `PickChip`):** the chip row below the textarea renders three cascading pickers — **Backend**, **Model**, **Effort** — sourced from `AgentApi.getBackendsCached()` (one fetch per session for the static `BackendMetadata[]` registry). Each chip is a `PickChip` that visually matches the legacy deck styling (label + bold current value + chevron glyph) but layers a transparent native `<select>` on top via absolute positioning so a single click opens the OS-native dropdown — keeping the full keyboard/a11y behavior of `<select>` without reimplementing a popover. Options read off the registry:
  - **Backend** — `backends[].label` as visible label, `backends[].id` as value. Disabled (no options) until the fetch resolves.
  - **Model** — only the models of the currently selected backend (`backend.models[]`). Disabled when the backend exposes no models (some backends are model-less). Each option renders `costTierDot(tier)` (● for `high`, ◐ for `mid`, ○ for `low`/unknown) followed by `m.label` so users can gauge cost at a glance.
  - **Effort** — only the `supportedEffortLevels` of the currently selected model. Disabled when the model doesn't expose effort levels. Human labels (`low` → "Low", `xhigh` → "Extra high", `max` → "Max") come from a local map.

  **Cascade reconciliation** — a `useEffect` inside `ComposerPicks` watches `(backends, composerBackend, composerModel, composerEffort)` and pushes reconciled values to the store whenever the catalog shifts under the user's selection:
  - If `composerBackend` isn't in the registry (e.g. a deleted backend still persisted on the conv record), snap to the first backend.
  - If the current `composerModel` isn't in the selected backend's `models[]`, reset to the backend's `default` model (falling back to the first model).
  - If the current `composerEffort` isn't in the selected model's `supportedEffortLevels`, reset to `'high'` when supported, else the first level.
  The reconciler only writes to the store when a value actually needs to change — it doesn't re-fire on every render. Because `streamStore.send` reads the reconciled values out of the same store fields, the POST body always carries a self-consistent `(backend, model, effort)` triple even when the user hadn't touched the chips since the catalog last shifted.

  **State lifecycle** — the pickers live in `StreamStore`'s per-conv entry (`composerBackend / composerModel / composerEffort`), not in component state, so:
  - First load of a conv hydrates the chips from `conv.backend/model/effort` via `StreamStore.load`.
  - Once the user picks a value, the `composer*` fields become sticky — switching to another conv and back doesn't rewind them.
  - On successful `POST /message`, `StreamStore.send` mirrors the sent values back onto `conv.backend/model/effort` so the next hydration (or a new subscriber) sees the persisted truth.
  - `reset(convId)` / `archive(convId)` wipe the entry entirely; the next mount starts fresh from the conv record.

  Composer-level setters (`setComposerBackend` / `setComposerModel` / `setComposerEffort`) only mutate the store and never POST directly — picker changes flush to the server with the **next** `/message`, by design. This avoids chatty PATCHes for a user who flips through the chips before typing, and leans on the `/message` endpoint's existing conditional-update behavior.

  **Composer attachment tray + typed chips (`AttTray` / `AttChip` / `PendingAttChip` + whole-view drop overlay):** the composer supports per-message file attachments via two entry points:
  - **Paperclip button** (`.composer-foot .attach`) — inline in the picker row. Clicking it triggers a hidden `<input type="file" multiple>` (stashed below the textarea). Selected files go directly through `StreamStore.addAttachments(convId, files)`.
  - **Drag-and-drop** — the entire `<section className="main">` wrapper attaches `onDragEnter` / `onDragOver` / `onDragLeave` / `onDrop`. The drag-state is tracked with a counter ref so child re-enters don't flicker the overlay. While dragging files over the pane, a `.drop-overlay` absolute-positioned overlay (dashed accent border, "Drop files to attach" label) renders on top of the feed and composer. `dataTransfer.types.includes('Files')` gates the handlers so text/URL drags don't trigger the overlay.
  - **Clipboard paste** — the composer textarea has an `onPaste` handler (v1 parity). If `clipboardData.items` contains any file entries (screenshots, copied images, dragged-in files), the handler calls `preventDefault()`, rewrites each file's name to `<basename>-<ts>-<n>.<ext>` so concurrent pastes don't collide, and forwards them via `StreamStore.addAttachments`. Otherwise, if `clipboardData.getData('text/plain')` is ≥ 1000 characters, the handler synthesises a `File` named `pasted-text-YYYYMMDD-HHMMSS.txt` (mime `text/plain`) and forwards that as an attachment instead of letting the textarea absorb a wall of text. Shorter text pastes fall through to the default textarea behavior.
  - **Dissolve (reverse paste-to-attachment)** — `PendingAttChip` exposes an extra "inline back into message" button (`.att-dissolve`, `Ico.up` glyph, positioned to the left of the existing `×` remove button) when the chip represents a *fresh* paste-text attachment that still has its in-memory `Blob` — specifically `entry.file instanceof Blob` AND `entry.file.name.startsWith('pasted-text-')` AND `!entry.restored`. Clicking it reads the blob via `file.text()`, splices the recovered text into the composer textarea at the current cursor via the shared `insertAtComposerCursor(text)` helper (uses the textarea's `selectionStart` / `selectionEnd` through `composerTextRef`; falls back to appending if no selection range is available), then calls `StreamStore.removeAttachment` to drop the pending attachment (which aborts a still-in-flight upload or DELETEs a completed one — same path as the regular `×`). After the splice, focus returns to the textarea with the caret positioned at the end of the inserted text via `requestAnimationFrame` + `setSelectionRange`. **Confirm threshold** — when the recovered text is > 50,000 characters the handler calls `dialog.confirm` first ("Inline this text into the message?" with the character count) so a huge dump doesn't surprise the user; below the threshold the splice happens silently. **Why restored entries don't dissolve:** entries rehydrated from `localStorage` after a session-expired full reload no longer have a `Blob` in memory (only a `{ name, size }` stub plus the server-side `result`), so client-side text recovery is impossible without re-fetching from the server. Dissolve is intentionally scoped to within-session pastes — the typical "wait, undo that paste I just did" affordance — rather than a permanent feature of the attachment.
  - **OCR (image → Markdown at cursor)** — `PendingAttChip` exposes an "OCR image to Markdown" button (`.att-ocr`, `Ico.fileText` glyph; sits in the same right-of-`×` slot as `.att-dissolve` since the two never co-exist — dissolve is paste-text-only, OCR is image-only) when the chip is an image attachment whose upload has landed (`status === 'done'` AND `result.path` present). Restored entries qualify too since the file still lives on the server. Clicking it calls `ocrAttachment(entry)` in `shell.jsx`, which delegates to `StreamStore.ocrAttachment(convId, attachmentId)`. The store flips the entry's `ocrStatus` to `'running'` (the chip swaps the icon for a small spinner via `.att-ocr-spin` and disables the button) and POSTs to `/conversations/:id/attachments/ocr` with the attachment's absolute `path`. On success the returned Markdown is cached on the entry as `ocrMarkdown` (with `ocrStatus: 'done'`); on failure `ocrStatus: 'error'` + `ocrError: msg` and a red `toast.error('OCR failed: ' + msg)` surfaces — the composer is left untouched. Either way the result Markdown (or cached value on a re-click) is spliced into the composer at the cursor through the same shared `insertAtComposerCursor(text)` helper that dissolve uses, with caret positioned at the end of the insert. **Cache semantics** — the cache lives on the `PendingAttachment` (composer-state, not server-persisted), so a re-click is instant with no second CLI spin-up. The button title flips to "Insert OCR Markdown (cached)" once the cache is populated. The cache is dropped with the rest of the composer state when the message ships or the conversation is left. **Original image stays put** — OCR never removes the attachment; the user decides whether to keep the image alongside the inlined text (mixed text+diagram screenshots) or remove it (text-only screenshots) before sending. **Re-entrancy guard** — clicking again while `ocrStatus === 'running'` throws inside the store (the button is also `disabled` in the running state, so this is defense-in-depth for keyboard activations / programmatic callers).

  `StreamStore.addAttachments(convId, files)` creates one `PendingAttachment` entry per file with a unique `id`, initial `status: 'uploading'`, `progress: 0`, and an `xhr: null` slot. Each entry then fires `AgentApi.conv.uploadFile(convId, file, onProgress, onXhr)`:
  - `onXhr(xhr)` is called synchronously at XHR creation — the store patches `xhr` onto the entry so `removeAttachment` can abort it cleanly.
  - `onProgress(loaded, total)` updates the entry's `progress` percentage (0–100) so the chip's spinner ring fills.
  - On resolve (server returns `{ files: AttachmentMeta[] }` — the helper picks the first entry since we send one file per request), the store flips the entry to `status: 'done'`, sets `result` to the full typed `AttachmentMeta` (including `kind` + optional `meta` like "12 pages"), and clears `xhr`.
  - On reject with `err.aborted === true`, the store does nothing (the chip was already removed). Other errors flip the entry to `status: 'error'` with the error message stashed for the chip's `title`.

  `StreamStore.removeAttachment(convId, attachmentId)` branches on status: aborts the XHR when `uploading`, fires `AgentApi.conv.deleteUpload(convId, result.name)` when `done` (fire-and-forget — the chip disappears from the UI regardless of the DELETE outcome), else drops the entry directly. `StreamStore.clearPendingAttachments(convId)` empties the tray without deleting the uploads — used when the user enqueues a message so the attachments detach onto the queue entry and the server copies remain on disk.

  **AttChip component** (two size variants):
  - `size: 'md'` — composer tray card. A ~120 px tall tile with: a typed-kind icon badge (color-coded per `AttachmentKind` — see the hint row below the composer for the legend), filename (ellipsized, 2-line clamp), and a `meta` sublabel underneath (e.g. "PDF · 12 pages", "TS · 142 lines", "MD · 84 lines"). For `kind: 'image'` the card shows the `blob:` thumbnail (from `URL.createObjectURL(file)`, revoked on unmount) edge-to-edge with a dark overlay for the filename. During upload, a spinner ring with the percent fills from 0 → 100; on error, a red status strip + the error message.
  - `size: 'sm'` — queue-row pill. A compact ~36 px tile with just the kind-colored icon + truncated filename. Used inside expanded queue rows.

  `AttTray` renders a CSS grid (`.att-tray`, `grid-template-columns: repeat(auto-fill, minmax(160px, 1fr))`) above the textarea when `pendingAttachments.length > 0`, one `PendingAttChip` (md) per entry, plus a trailing `.att-add` tile (dashed border, plus icon) that opens the file picker. `PendingAttChip` is a thin wrapper that derives `kind` from the file extension via `StreamStore.attachmentKindFromPath`, builds the blob thumbnail for images, and passes the right props into `AttChip`. The tray receives `convId` from `ChatLive` and forwards it to each `PendingAttChip` so the chip can rebuild thumbnails for **restored** entries (see below).

  **Thumbnail rehydration safety.** `PendingAttChip`'s image-thumb effect branches on `entry.restored`. For live entries it calls `URL.createObjectURL(file)` on the in-memory `File` blob (revoked on unmount). For entries rehydrated from `localStorage` by `hydrateAttachmentsFromDraft` — where `file` is a `{ name, size }` stub, not a `Blob` — the effect skips `createObjectURL` entirely and falls back to the server-side URL `AgentApi.chatUrl('conversations/:id/files/:filename')` so the thumb still renders after a session-expired full reload. A `file instanceof Blob` guard covers the edge case of a restored entry that lacks the `restored` flag for any reason (defense-in-depth; never reached in practice). V1's composer got this right implicitly by omitting the `type` field on restored files, which short-circuited the legacy `f.type && f.type.startsWith('image/')` check; V2's refactor into `kindFromFile` dropped that implicit guard, so V2 uses explicit branching instead.

  **Send-time behavior (`StreamStore.send`):**
  - **Guard** — when any `pendingAttachments` entry is `uploading`, `send()` returns early. `canSend` computes the same guard so the send button is disabled while uploads are in flight.
  - **Content mutation** — completed attachments' absolute server paths are joined with `", "` and appended as `[Uploaded files: path1, path2, …]` via `composeWireContent(queuedMessage)`. If the textarea is empty but attachments are present, the tag becomes the full content (and `canSend` allows this path). The CLI still reads the files from disk — the typed chips are a UI concern only and the wire format is unchanged.
  - **Snapshot / rollback** — `send()` captures both the current `pendingAttachments` array AND the `input` string before the optimistic update; it clears both immediately (so the UI repaints with an empty composer and no tray) and restores both if the POST `/message` fails (including the 401/`Session expired` path). No re-upload is needed on rollback — the server still holds the files under the conv's artifacts dir. Restoring `input` is what keeps the user's typed message visible after a mid-send session-expired 401, so they don't have to retype.
  - **Persisted draft flush before wipe** — `send()` calls `flushDraftNow(convId)` right before the optimistic state wipe. This cancels any pending debounced `scheduleDraftSave` timer and writes the current (pre-wipe) state to `localStorage` synchronously. Without this, a timer firing between wipe and POST resolution would see `input: ''` + `pendingAttachments: []` and delete the `ac:v2:draft:<convId>` key — which would leave the full-reload path (popup-blocked fallback for silent re-auth) with no draft to rehydrate. On POST failure `clearDraft` is *not* called (only on server-confirmed success), so the flushed copy remains the source of truth for a reload.
  - **`clearAllStreamErrors()`** — iterates every tracked ConvState and calls `clearStreamError(convId)` on any with a non-null `streamError`. Called by the shell after a successful silent re-auth (see `spec-server-security.md` § "V2 frontend silent re-auth") so stale "session expired" error cards disappear across every open conv the moment the session is back.

  The 50 MB per-file / 10-files-per-request limits come from the server's multer config; the client does not enforce them up front — errors surface as `status: 'error'` chips with the server's message pinned to the title tooltip. Below the tray the `.ca-hint-row` shows a legend of typed kinds with their accent colors so the user can correlate chips to types at a glance.

  **Composer notification icon (`ComposerNotifIcon`):** a 26×26 dashboard-style icon button (`.composer-notif`) rendered inside `.composer-foot` immediately to the left of the Send button. The component is a general-purpose composer-level notification surface; today the only notification source is Knowledge Base state, but the component is named and structured so additional sources can be added without renaming. **Visibility rule** — the icon is hidden when (a) the conversation has no `kb` object or `kb.enabled === false`, OR (b) KB is enabled but idle (no pending digestions, no pending synthesis entries, no dream in progress). It only renders when there is something to notify about. Two visible states: `pending` (accent border + small accent dot via `.composer-notif-pulse`) when there is queued KB work but no active dream, and `running` (accent border + accent-tinted background + animated pulse via the `composer-notif-pulse` keyframes, 1.6s cycle) when `kb.dreamingStatus === 'running'`. The state class lives on both the button and the pulse span (`state-pending` / `state-running`). The icon glyph is `Ico.book(14)` since KB is the only current source — when a second source lands, the glyph (and label) become source-aware. **Tooltip** — hover surfaces a `<Tip variant="stat">` rich card built from the standard tooltip template classes (`.tt-header` / `.tt-eye` / `.tt-h` / `.tt-section` / `.tt-rows` / `.tt-kv`): header `Knowledge Base`, title `Dreaming…` when running else `{N} pending`, then a key/value section with `Digestion: {N files}` (or `—`), `Synthesis: {N entries}` (or `—`), `Auto-digest: on/off`. While running, a second section appends `Dreaming in progress` and a `<DreamStepper progress={kb._dreamProgress}/>` showing the five phases (routing → verification → synthesis → discovery → reflection) with active/done/pending modifiers. **Aria label** mirrors the tooltip title (`KB: dreaming in progress` or `KB: {N} pending`). **Data sources** — `conv.kb` is hydrated from `GET /conversations/:id` on conv load and patched live via `kb_state_update` WS frames (handled in `streamStore.js`); a 2-second poll on `GET /conversations/:id` runs only while `dreamingStatus === 'running'` to backstop dream-progress transitions when WS frames lag. Replaces the V1 `#chat-dream-banner` in-flow panel.

  **Message queue (`QueueStack` / `QueueRow`):** the v2 composer supports queueing follow-up messages during a live run. State mirrors the server-persisted `messageQueue` (typed `QueuedMessage[]`) via `StreamStore.queue`; the store auto-drains the head on every `done` frame when the conversation is idle (see `drainQueueIfReady` in `streamStore.js`).
  - **Enqueue paths** — three ways to enqueue: (a) ⌘/Ctrl + Enter in the composer textarea always enqueues, even while idle; (b) plain Enter with content while streaming enqueues; (c) clicking the stop/enqueue button (`.send.stop`, red with the octagon-stop glyph) during streaming enqueues the current draft and clears it. `doEnqueue()` in `shell.jsx` snapshots the composer's done `pendingAttachments` onto the queue entry's `attachments[]`, calls `StreamStore.enqueue`, then `setInput('')` + `clearPendingAttachments()`. The uploads stay on disk — the queue entry owns their `AttachmentMeta` references.
  - **`canSend` / `canEnqueue` guards** — `canSend = hasContent && !sending && !streaming && !awaiting && !hasUploadingFiles`, `canEnqueue = hasContent && !sending && !awaiting && !hasUploadingFiles && streaming`. "Content" means non-empty textarea **or** at least one done attachment.
  - **`QueueStack`** renders below the tray (and above `.composer-foot`) when `queue.length > 0`. Header row shows "Queued (N)" + a "Clear all" link. Each entry maps to a `QueueRow`.
  - **`QueueRow` (collapsed)** — compact two-line layout: top row shows the 1-based position badge (`.qrow-num`), the message text truncated to 2 lines, and the action buttons (↑/↓ reorder, ✎ edit, × remove). When the row has attachments, a second row (`.qrow-atts`) renders horizontally-scrollable `AttChip size="sm"` pills. First/last rows get their up/down buttons disabled via `first` / `last` props.
  - **`QueueRowExpanded` (inline edit)** — clicking a row's pencil (`Ico.edit`) button swaps it for an expanded editor (`.qexp` surface). The header repeats the `#N` badge with a `· in N positions` suffix when the row is not at the head. The body is a pre-filled `<textarea className="qexp-text">` (with a `textRef` for cursor-position splicing during dissolve) and `autoFocus`. Attachments render as full-width `AttChip size="md"` cards with per-chip remove (`onRemove` → drops from the local draft). The footer has **Add attachment** (opens a hidden file input, uploads via `AgentApi.conv.uploadFile` per file, appends the `AttachmentMeta` result to the draft), **Move up** (if not first), **Cancel** (discards draft, collapses), **Save** (calls `StreamStore.updateQueueItem(convId, index, { content, attachments })` — disabled unless dirty and not uploading), and a destructive **Remove** (calls the stack's remove handler, which collapses automatically). Upload failures render inline above the footer. Only one row may be expanded at a time; if the auto-drain pops the expanded index, the `QueueStack` collapses back to the list.
    - **Paste-to-attachment (parity with composer)** — the editor's textarea has its own `onPaste` handler that mirrors the main composer's behavior: file-kind clipboard items are renamed `<basename>-<ts>-<n>.<ext>` and uploaded; text pastes ≥ 1000 chars synthesise `pasted-text-YYYYMMDD-HHMMSS.txt` and upload it. Shorter text pastes fall through. Uploads route through the same `uploadFiles` helper that the file picker uses, so a successful upload appends the returned `AttachmentMeta` to the local `atts` draft and the chip appears in the editor's attachment row immediately. Failures surface in the inline error area like file-picker uploads.
    - **Dissolve (within-session)** — when paste-to-attachment fires inside the editor, the original pasted text is also stashed in a `dissolveMapRef` (`Map<attachmentName, originalText>`) keyed by the server-returned `result.name`. Chips whose name appears in that map render the same `.att-dissolve` button as the composer; clicking it splices the recovered text into the editor's textarea at the current cursor (or appends if no selection), removes the chip from `atts` (and from the dissolve map), and refocuses the textarea with the caret at the end of the splice. The same > 50,000-char `dialog.confirm` gate applies. **Lifetime** — the dissolve map is component state, so it lasts only until the editor row is closed (Cancel / Save / collapse). Reopening the same row later restores the attachment but not the dissolve affordance: by design, dissolve is the immediate-undo path for a paste that just happened, not a permanent property of the attachment. The `removeAtt` helper clears the matching dissolve-map entry whenever an attachment is dropped, dissolve or otherwise.
  - **Persistence** — every mutator (`enqueue`, `removeFromQueue`, `reorderQueue`, `updateQueueItem`) optimistically updates `state.queue` then PUTs the full queue; `clearQueue` DELETEs. A stale update is overwritten by the next PUT; there's no coalescing layer.
  - **Auto-drain on `done`** — when a stream completes with no `pendingInteraction` and no `streamError`, the store pops `queue[0]`, PUTs the shortened queue, and composes the popped entry's wire-format content via `composeWireContent` (content + `[Uploaded files: …]` tag rebuilt from `attachments[]`). The content is sent via the internal `sendWireContent(convId, content)` helper (bypasses the pendingAttachments pipeline since attachments were already uploaded at enqueue time).

  **Backwards compatibility (historical transcripts):** messages saved before the typed queue existed may carry `[Uploaded files: /abs/path1, /abs/path2]` trailing text on `Message.content` (user messages). The renderer feeds these through `StreamStore.parseUploadedFilesTag(content)`, which strips the tag and returns `{ content, attachments }` where each `AttachmentMeta` has `name` + `path` + `kind` (inferred from extension via `attachmentKindFromPath`). `size`/`meta` are unavailable for legacy entries — the `md` chip shows only the filename and kind badge in that case. Users receive the same typed-chip UX for old transcripts at read-time; the on-disk bytes are untouched.
- **Everything else** — not in the real app at `/v2/`. Right rail, tab indicator, and the other mock screens remain accessible only via `/v2/deck.html` and will be wired in future PRs.

## Legacy UI (V1)

The sections below describe the legacy vanilla-JS UI at `public/index.html` + `public/js/*.js` + `public/styles.css`. It is reachable at `/legacy/` (redirects to `/index.html`) as a fallback during the V2 cutover and will be removed once V2 proves itself. All content below documents V1's behavior as of the cutover and is not being actively extended.

## Layout

- Flexbox: sidebar (fixed 280px) + main area (flex: 1)
- Sidebar: new chat button, search, conversation list grouped by workspace, settings, sign out, version label
- Main area: header with title + usage indicator + action buttons, messages container, input area with backend selector + model selector + effort selector + file chips + textarea
- Responsive: below ~768px sidebar overlays content

## Conversation Management

- **New conversation:** folder picker modal (via `/browse` API) → user selects directory → POST creates conversation with the user's `defaultBackend` and `defaultModel` from settings
- **Sidebar list:** grouped by workspace (last 2 path segments of `workingDir`), sorted by `updatedAt` desc. Groups are collapsible (state in localStorage). Each group header has a gear icon (`ICON_SETTINGS`) for workspace settings.
- **Sidebar live activity:** `StreamStore.bumpConvListActivity(convId, iso?)` is the single entry point that keeps a row's `updatedAt` (and therefore the `timeAgo()` label and the list's sort position) fresh between full list re-fetches. The store calls it from three places: (1) the `assistant_message` WebSocket frame handler (passes `frame.message.timestamp`), (2) `send()` after the server returns `userMessage` (passes `userMessage.timestamp`), and (3) `sendWireContent()` after the server returns `userMessage`. Each call patches the matching item's `updatedAt` to the supplied ISO string (falling back to `new Date().toISOString()` when omitted), re-sorts `convList.items` descending by `updatedAt` so the active conv bubbles to the top, and notifies subscribers. No-op when the list isn't loaded yet or when no row matches `convId`. Without these calls the sidebar would hold the timestamp from whenever the list was last GET'd — V2's list only re-fetches on initial mount, search-query changes, and archive toggles, so the label would otherwise stay frozen for the entire duration of an active chat session.
- **Context menu:** right-click on conversation items for rename/archive/delete (active view) or restore/delete (archive view)
- **Archive:** conversations can be archived via context menu. Archived conversations are hidden from the main sidebar but all files (sessions, artifacts) remain on disk. A toggle at the bottom of the sidebar switches between active and archived views. Archived conversations can be browsed, searched, restored, or permanently deleted.
- **Search:** debounced, case-insensitive search across titles, last messages, and full content. Respects active/archive view filter.

## Messaging & Streaming

- `chatSendMessage()` gathers completed file paths from pending uploads, appends `[Uploaded files: ...]` to content, opens WebSocket, POSTs message (with `backend`, `model`, and `effort`), receives stream events via WS. When the selected backend, model, or effort differs from the stored defaults, it auto-saves the new choice via `PUT /settings` (fire-and-forget) so future new conversations use it. `defaultEffort` only persists to settings when the chosen model matches the stored `defaultModel`, preventing a mid-flight model swap from clobbering the settings-level default.
- Streaming uses `fetch` with manual ReadableStream parsing (not EventSource API)
- **Streaming state persistence:** `chatStreamingState` Map stores per-conversation state (accumulated text, thinking, tools, agents, tool/agent history, pending interactions). State survives conversation switches — on return, the streaming bubble is recreated and restored. A separate `chatPendingInteractions` Map (keyed by convId) mirrors the `pendingInteraction` data and acts as a fallback for `chatRenderMessages()` — if the streaming state is missing or its `pendingInteraction` is cleared during navigation, the fallback Map ensures plan approval / user question UI is still rendered.
- **WebSocket auto-reconnect:** On unexpected WS close during streaming, the client automatically attempts reconnection with exponential backoff (1s base, up to 5 attempts). On reconnect, the server replays buffered events wrapped in `replay_start`/`replay_end`. The client resets streaming state on `replay_start` (clears accumulated text/thinking/tools) and reprocesses replayed events from scratch. `assistant_message` events are deduplicated by message ID. `done` events during replay are ignored to prevent stale streams from destroying the current streaming state. After max attempts exhausted, `_doneResolve` is called to clean up. `chatDisconnectWs()` clears reconnect attempts to prevent auto-reconnect on deliberate close. Session reset clears the server-side event buffer to prevent stale events from replaying into the new session.
- **Streaming avatar:** The streaming message bubble reads the backend from the `chat-backend-select` dropdown (not from `state.chatActiveConv.backend`) to ensure the correct icon is shown immediately when the user switches backends, even before the conversation object is updated.
- **Elapsed timer:** live timer in streaming bubble header, self-cleans on DOM disconnect and nulls out the interval reference so it can restart on conversation switch-back
- **Unified streaming content:** A single `chatUpdateStreamingContent()` function renders all streaming state (thinking, text, tool history, active tools, agents, plan mode) together in one stacked view. Text content and tool activity accumulate and remain visible simultaneously — new progress updates stack below previous content rather than replacing it. Items are grouped by agent via `chatGroupItemsByAgent()`: standalone tools render flat, while each agent card is followed by a scrollable sub-activity panel showing its child tools. Completed items show checkmarks and elapsed durations; running agents show animated spinners with live timers that count up in real-time.
- **Tool activity on completed messages:** When a message has a `toolActivity` array, `chatRenderToolActivityBlock()` renders a collapsible `<details>/<summary>` block (same pattern as thinking blocks) with a summary line (e.g. "15 ops · 2 agents · 5 read, 2 edited") generated by `chatBuildActivitySummary()`. Collapsed by default; expands to show the full chronological tool/agent list. Agent entries render as agent cards, tool entries as history items.
- **Ordered content blocks on completed messages:** When a message carries a `contentBlocks` array (new format — see Data Models → ContentBlock), `chatRenderContentBlocks()` walks the blocks in source order and emits: text blocks as markdown, thinking blocks via `chatRenderThinkingBlock()`, and **runs of consecutive tool blocks** as a single `chatRenderToolActivityBlock()` (so agent nesting and parallel grouping still apply within each run). This preserves the interleaving the CLI emitted — e.g. "text → activity breadcrumb → text → activity breadcrumb → text" instead of the legacy "all thinking + all tools + all text stacked". `chatRenderMessage()` uses `contentBlocks` when present and falls back to the legacy `thinkingHtml + toolActivityHtml + rendered` stack when absent, so session files written before this field existed render unchanged. The progress-breadcrumb row renderer (`chatRenderProgressBreadcrumb`) uses the same helper with a `textWrapClass: 'chat-progress-row-text'` option so text blocks keep the row's compact typography. The live streaming bubble (`chatUpdateStreamingContent`) continues to use the existing progressive renderer — ordered blocks take effect once the stream-final `assistant_message` is persisted and rendered.
- **Progress group collapsing:** `chatRenderMessages()` walks the session messages and collapses runs of consecutive assistant messages with `turn === 'progress'` (same `backend`, not interrupted by a user/system/memory_update message) into a **single compact breadcrumb pill** via `chatRenderProgressBreadcrumb()` — a `<details>` element with no avatar, no role header, and no action buttons. Collapsed by default: the summary shows a caret, the step count (e.g. "4 steps"), the elapsed time computed against the preceding user message using the *last* progress row's timestamp (so it keeps climbing as more rows land), and a truncated single-line preview of the last row's text. Expanded: reveals a vertical timeline (`.chat-progress-timeline`) where each row holds a per-row timestamp, the row's `chatRenderThinkingBlock`/`chatRenderToolActivityBlock` output, and the row's markdown body. **Placement rules** — three cases:
  1. **Run followed by a persisted final assistant message (same backend):** the breadcrumb is rendered *inside* that final message's `.chat-msg-content` (above its Thinking block + body) so the whole turn is one visual block — breadcrumb → Thinking → activity → final text.
  2. **Run is the trailing tail of the session AND a stream is live for this conversation** (`state.chatStreamingState.has(convId)`): the feed walk skips emitting the breadcrumb; instead `chatUpdateStreamingContent()` calls `chatBuildTrailingProgressBreadcrumb()` on every update and prepends the result inside the streaming bubble's own `.chat-msg-content` (under the Assistant header, above thinking / tool activity / accumulating text). This keeps past progress steps visible under the Assistant header while the agent works on the next segment, and transitions seamlessly into case (1) once the final message lands (the breadcrumb helper returns empty once the tail message is no longer `turn: 'progress'`).
  3. **Run is the trailing tail with no live stream** (stream crashed, session ended mid-turn): the breadcrumb renders standalone as a feed-level sibling with a left offset (`#chat-messages > .chat-progress-breadcrumb`) to align with the avatar column.
  Assistant messages with `turn === 'final'` (or legacy messages without `turn` — treated as final for back-compat) always render as their own full card via `chatRenderSingleMessage()`. Rationale: the final message is what matters; the intermediate chatter should take minimal vertical space but remain accessible on demand, and should always appear tucked under a single Assistant header rather than as a full-width feed item.
- **Tool outcome indicators:** Each tool/agent in the activity log shows a colored outcome badge when outcome data is available. `chatRenderStatusCheck()` renders status-colored checkmarks (green ✓ for success, red ✗ for error, amber ✓ for warning). `chatRenderOutcomeBadge()` renders a small colored badge with the outcome text (e.g. "exit 0", "4 matches", "not found"). Outcomes are extracted from CLI `tool_result` blocks by `extractToolOutcome()` in the backend, correlated by `tool_use_id`, and persisted on the `toolActivity` entries.
- **Sticky active section:** During streaming, when both completed and running tools exist, a `chat-activity-panel` container wraps them: completed items scroll in a bounded area while running items with spinners stay pinned at the bottom, always visible.
- **Parallel group indicator:** `chatGroupParallelItems()` detects consecutive agent entries whose `startTime` values are within 500ms (`PARALLEL_THRESHOLD_MS`) and wraps them in a `chat-parallel-group` container with a "parallel" label and a left accent border. Works in both persisted activity blocks and streaming display.
- **Agent detail expansion:** Agent cards with long descriptions or outcome data render as expandable `<details>` elements (`chatRenderAgentCard()`). Summary shows agent type, description, outcome badge, and elapsed time; expanding reveals full outcome details.
- **Turn boundaries:** intermediate assistant messages saved, content reset. `turn_complete` event archives active tools/agents to history so spinners stop. On `assistant_message`, tool/agent history is cleared after archiving — the saved message's `toolActivity` now owns those entries, preventing duplicates when the next turn adds new agents to the streaming bubble. Agents are only archived when they have received their `tool_outcomes` (outcome/status set) — sub-tool `turn_complete` events within an agent do NOT prematurely archive the parent agent. This ensures agents show spinners and live timers throughout their full execution.
- **Post-completion processing indicator:** When all tools/agents have completed but the model is still working (no text content yet), a "Processing..." indicator with typing dots is shown below the completed activity log. This fills the gap between agent completion and text output, so users always see ongoing work.
- **Thinking events:** do NOT archive active tool/agent state — `turn_complete` handles archiving. This prevents premature archiving that would kill agent spinners and timers.
- **Plan approval / user input:** renders plan as markdown with approve/reject buttons (and user-question prompts) and submits responses through `POST /conversations/:id/input`, including a `streamActive` hint from the client. The server uses that hint to avoid racing a just-finished stream: it only forwards to `sendInput()` (`mode: 'stdin'`) when the client still considers the stream live; otherwise it returns `mode: 'message'` and the client resumes the conversation by sending the response as a normal follow-up message in the same session. On plan exit, if no explicit plan file was captured, the server falls back to the accumulated assistant text for `planContent` so the approval card still shows the full plan body. Once a plan approval or user question is pending (`pendingInteraction` is set), all DOM-updating event handlers (`thinking`, `tool_outcomes`, `text`, `tool_activity`, `turn_complete`, `replay_end`, and the activity timer) must guard against calling `chatUpdateStreamingContent()`, which would overwrite the pending interaction UI. Additionally, `chatUpdateStreamingContent()` itself contains a belt-and-suspenders guard that returns immediately when `pendingInteraction` is set, preventing any unguarded call site from overwriting the interaction UI. The `replay_end` handler re-shows the pending interaction instead of rendering regular streaming content. The `finally` block in `chatSendMessage` does **not** disconnect the WebSocket when `pendingInteraction` is set — the WS remains available for continued stream updates even though interaction submission itself no longer depends on the browser WS.
- **Auto title update:** handles `title_updated` event by updating the active conversation title, the header, and the sidebar list in-place (no full reload needed).
- **Usage display:** a small indicator in the conversation header shows **session-level** token count (with `ICON_TOKEN`) and USD cost. Updated in real-time when `usage` events arrive during streaming. Displays on hover a tooltip with session input/output/cache token breakdown and cost, plus conversation-level totals. Hidden when no usage data exists (e.g. new conversation). For **Kiro** conversations, shows credits consumed and context usage percentage instead of tokens/cost. The token icon replaces the text label — no "tokens" or "credits" word in the display.
- **Stream cleanup:** `chatCleanupStreamState()` accepts `{ force }` option. When the stream ends (`finally` block) while a `pendingInteraction` is active, force cleanup is **skipped** and the WebSocket is kept open — the streaming state and message element are preserved so the user can still interact and send their response. The interaction response handler checks `!chatStreamingConvs.has(convId)` and force-cleans after the user responds. Content updates in the interaction handlers (plan approval, user question) are applied **before** cleanup so they remain visible. When no interaction is pending, the `finally` block uses `force: true` as before.
- **Send button state:** shows `ICON_STOP` (octagon) when streaming with no text input, `ICON_SEND` (paper plane) when idle or when streaming with text input (to queue). Toggled via `innerHTML`. Disabled during uploads or session resets.
- **Message queue:** Users can compose and submit messages while the CLI is actively responding. Queued messages are stored client-side in `chatMessageQueue` (Map of convId → array of `{ id, content, inFlight }`) and **persisted server-side** as `messageQueue` (array of content strings) on the conversation entry. On every queue mutation (add, edit, delete, shift, clear), a sequential coalescing PUT syncs the current state to the server — at most one PUT in flight at a time, with a follow-up if mutations occur during the request. Queued messages appear inline in the chat after the streaming bubble, styled as user messages with reduced opacity and an accent left border. Each shows a "Queued" badge and has Edit and Delete buttons. In-flight messages show "Sending..." and cannot be edited or deleted. When a response completes successfully, the next queued message is automatically sent (FIFO). Queue has three states: **Active** (streaming, auto-execute on completion), **Paused** (error, banner with Resume/Clear), and **Suspended** (restored from server after page load). The `chatQueuePaused` Set tracks paused conversations; `chatQueueSuspended` tracks restored conversations. On loading a conversation with a non-empty persisted queue and no active stream, the queue is restored into client state and marked suspended. A banner reads "N queued messages from a previous session" with Resume and Clear buttons. Suspended queues do not auto-execute — the user must explicitly resume. Queue is automatically cleared on session reset and archive.
- **Per-message actions:** Each rendered message has a `.chat-msg-actions` row (visible on hover) with two buttons: **Copy** (copies the rendered plain text via `content.textContent`) and **Copy MD** (copies the original markdown source from the `data-raw-content` attribute on the `.chat-msg` element). Both buttons show a "Copied!" confirmation for 1.5 seconds after clicking.

## Browser Tab Status Indicator

Implemented in `public/js/tab-indicator.js`. Overlays a colored dot-badge on the favicon so users can monitor task progress while working in other tabs. `document.title` is never modified — the signal lives entirely on the favicon. Four states:

| State | Favicon dot color | Condition |
|-------|-------------------|-----------|
| `idle` | (no dot — base favicon) | No conversations streaming |
| `running` | Blue `#3b82f6` | Any conversation is streaming (`state.chatStreamingConvs.size > 0`) |
| `done` | Green `#22c55e` | Last stream ended successfully **while the tab was hidden** |
| `error` | Red `#ef4444` | Last stream ended with an error **while the tab was hidden** |

- **Any-conversation semantics:** A single indicator reflects the aggregate — if multiple conversations stream concurrently, the indicator is `running` until all finish.
- **Hidden-only completion:** If the tab is visible when a stream ends, the indicator returns directly to `idle` — the user already sees the completion in the UI, so no notification is needed. `done` and `error` are purely passive-notification states.
- **Error flag sticky across concurrent streams:** If any of multiple concurrent streams errors, the error flag persists; the indicator stays `running` until the *last* stream ends, at which point (if still hidden) it shows `error`. The flag is cleared once consumed.
- **Auto-clear on focus:** A `visibilitychange` listener clears `done` / `error` back to `idle` when the tab becomes visible. `running` is unaffected by visibility changes.
- **Pending interactions do not end the run:** When a stream ends with `pendingInteraction` set (plan approval / user question), the tab indicator is **not** updated — the task is logically still active, awaiting user input, so the `running` state persists.
- **Favicon rendering:** The base SVG is loaded once, drawn onto a 64×64 canvas, and overlaid with a colored dot (22% radius, bottom-right, white halo for contrast). The result is exported as a PNG data URL and assigned to `<link rel="icon">.href`. Reverting to `idle` restores the original `favicon.svg` href.
- **Hook points:**
  - `chatStartMessageRequest()` in `streaming.js` calls `chatTabOnStreamChange()` immediately after adding to `chatStreamingConvs`.
  - The `finally` block in `chatStartMessageRequest()` calls `chatTabOnStreamChange({ error: !!hadError })` in the non-pending-interaction path, after deleting from `chatStreamingConvs`.
- **Initialization:** `chatInitTabIndicator()` is called from `chatInit()` in `main.js`. Wires the visibility listener and preloads the base favicon image. Idempotent.

## File Handling

- Files upload **immediately on attach** (not at send time) via XHR with per-file progress tracking
- Sources: drag-and-drop, clipboard paste (images + large text ≥1000 chars), file picker button
- `chatEnsureConversation()` auto-creates conversation on first file attach (promise-cached for concurrent calls)
- File chips show progress bar, checkmark on completion, error indicator on failure
- Remove button: aborts in-progress upload or DELETEs completed file
- At send time: completed file paths embedded as `[Uploaded files: /path/to/file1, ...]`
- **Inline images:** `chatRenderUploadedFiles()` replaces `[Uploaded files: ...]` with `<img>` tags for image extensions (`.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`, `.svg`, `.bmp`). Click opens lightbox overlay.
- **Non-image files:** `chatRenderUploadedFiles()` renders non-image uploaded files as file badge cards (same `.chat-file-card` component used by File Delivery), with **View** and **Download** buttons hitting `GET /conversations/:id/files/:filename?mode=view|download`.

## File Delivery

- **Trigger:** The CLI outputs `<!-- FILE_DELIVERY:/path/to/file -->` markers when the user explicitly requests a deliverable file (e.g. "give me a CSV", "create a report"). The system prompt instructs all backends to use this marker only for user-requested deliverables, not for routine file edits.
- **Marker extraction:** `chatExtractFileDeliveries(text)` runs as a pre-processing step before markdown rendering. Strips all `FILE_DELIVERY` HTML comments from the text and returns the list of file paths. Runs in both streaming and static message rendering paths (via `chatRenderMarkdown`).
- **File cards:** `chatRenderFileCards(files)` renders inline cards for each delivered file, showing a file icon, filename, and two buttons: **View** (opens the side panel) and **Download** (direct browser download via `Content-Disposition: attachment`). Both buttons hit `GET /workspaces/:hash/files?path=...&mode=view|download`.
- **Viewer panel:** A right-side panel (`.chat-file-viewer`) that slides in when the user clicks View. Shows the file's text content with syntax highlighting (via highlight.js). Capped at 2 MB on the server side. Closed via the X button or automatically on conversation switch. On mobile (≤768px), the panel overlays the full chat area.
- **Multi-file support:** Multiple `FILE_DELIVERY` markers in one message render multiple file cards. The View panel swaps content when opening a different file.

## Draft Persistence

- **In-memory layer:** `state.chatDraftState` is a `Map` keyed by convId (or `'__new__'` for the welcome-screen buffer) that holds `{ text, pendingFiles }`. Source of truth inside a single tab.
- **localStorage mirror:** every draft is also written under `chat:draft:<convId|__new__>` so drafts survive the sign-in-redirect page reload triggered by the session-expired overlay. The persisted shape is `{ text, files: [{ path, name, size }] }` — only completed uploads are serialized. In-progress `File` bytes cannot be persisted and are dropped.
- **Helpers (`public/js/conversations.js`):**
  - `chatSaveDraft()` — mirrors the textarea + `state.chatPendingFiles` into both layers under the active convId (or `'__new__'`). Empty drafts trigger `chatDeleteDraft` rather than writing `{ text: '', files: [] }`.
  - `chatRestoreDraft(convId)` — reads from `state.chatDraftState` and paints the textarea + pending-files UI. Clears both when no draft is present.
  - `chatDeleteDraft(key)` — removes the draft from both layers. Used by `chatDeleteConversation`, `chatArchiveConversation`, the empty-draft branch of `chatSaveDraft`, and the success paths of `chatSendMessage` / `chatStartMessageRequest`.
  - `chatMigrateDraft(fromKey, toKey)` — moves a draft between keys in both layers. Invoked by `chatEnsureConversation` when a `'__new__'` draft is promoted to a real convId after an upload-triggered conversation creation.
  - `chatHydrateDraftsFromStorage()` — called once from `chatInit` on boot. Iterates `localStorage` for `chat:draft:*` entries, rebuilds `state.chatDraftState`, and reconstructs file-entry objects with `{ name, size }` (type is intentionally omitted so the chip renderer's `f.type &&` guard skips `URL.createObjectURL` on the non-`Blob` restored file). Corrupted entries are dropped.
- **Save sites:**
  - On conversation switch — `chatSelectConversation` (saves the outgoing conv's draft before loading the new one).
  - On opening the folder picker for a new conversation — `chatCreateConversationWithDir`.
  - At the start of `chatSendMessage` — immediately before the textarea is cleared, so the draft reaches localStorage even if the function throws synchronously before the fetch's catch runs.
- **Restore sites:**
  - On conversation switch — `chatSelectConversation`.
  - After folder-picker creation — `chatCreateConversationWithDir` (restores any `'__new__'` draft migrated to the new convId).
  - On boot — `chatInit` calls `chatHydrateDraftsFromStorage()` then `chatRestoreDraft(null)` to paint the welcome-screen textarea with any pre-reload `'__new__'` draft.
  - On failed send — `chatSendMessage` installs a `restoreFailedSend()` closure that repaints the captured `originalText` + `originalFiles` into the UI and calls `chatSaveDraft()` again. Wired into both the conversation-create catch block and `chatStartMessageRequest`'s `onSendFailed` callback.
- **Clear-on-send ordering:** the per-conv delete in `chatStartMessageRequest` runs **after** `await response.json()` succeeds, not before the POST. The `'__new__'` delete in `chatSendMessage` runs after the conversation-create POST succeeds. This keeps the draft intact when the message POST returns 401 (session expired mid-send) so the overlay's "your draft is preserved" promise holds.
- **Session-expired overlay:** `chatShowSessionExpired` (in `public/js/state.js`) still reads "You have been signed out. Your draft message is preserved — sign in to continue." The claim is now backed by the localStorage mirror + the delete-after-success ordering.

## Session Management

- **Reset:** archives active session with LLM summary, creates new session, resets conversation title to "New Chat" in both header and sidebar. Shows an "Archiving session..." indicator bubble branded with the Agent Cockpit logo and name (not the CLI backend icon, since archival is a cockpit-level action). Blocked during streaming. Double-click prevented via `chatResettingConvs` set. Header title is also synced from server data whenever the conversation list reloads (via `chatLoadConversations`), ensuring the header stays consistent even if the inline update is missed. `chatLoadConversations` uses a generation counter to discard stale responses, preventing race conditions where an older response overwrites a title that was already updated by a `title_updated` event. A final `chatLoadConversations()` call in the streaming `finally` block ensures the sidebar and header reflect the latest server state after streaming ends.
- **History modal:** lists sessions with summaries, view and download buttons
- **View session:** fetches archived messages from API

## Settings Modal

Tabbed layout with five tabs:

**General tab:**
- Theme: System / Light / Dark
- Send behavior: Enter or Shift+Enter
- System prompt textarea (global)
- Default backend selector (also auto-updated when user sends a message with a different backend)
- Default model selector (shown only when the selected backend has models; auto-updated with backend changes)
- **Default Effort selector** (shown only when the default model declares `supportedEffortLevels`; options are dynamically built from the model's supported list — e.g. Opus 4.7 shows `low/medium/high/xhigh/max`, Opus 4.6 shows `low/medium/high/max`, Sonnet shows `low/medium/high`, Haiku hides the row entirely). Changing the default model to one without effort support drops `defaultEffort` on save.
- Working directory

**Usage Stats tab:**
- Time range filter: Today / This Week / This Month / All Time
- Per-backend usage table: input, output, cache read, cache write, total tokens, and cost
- Daily breakdown table (when multiple days selected): date, backend, tokens, cost
- "Clear All Data" button: clears the usage ledger (requires confirmation)
- Data loaded from `GET /usage-stats` endpoint

**Server tab:**
- **Restart Server** button: calls `POST /server/restart` which delegates to `UpdateService.restart()`. Same pm2 double-fork mechanism as self-update, without the git pull / npm install steps. Used to re-trigger startup-time binary detection (e.g. pandoc) after installing external tools. Confirms before firing; shows the existing restart overlay on success and reloads the page when the new pm2 process is back. `TypeError` / "Failed to fetch" during the in-flight POST is treated as success (the process died before the response flushed).

## Workspace Settings Modal

Triggered by the gear icon (`ICON_SETTINGS`) on workspace group headers. Multi-tab modal:

- **Instructions tab:** per-workspace instructions textarea; fetches/saves via the workspace instructions API.
- **Memory tab:** enable/disable toggle (persists immediately to `WorkspaceIndex.memoryEnabled`), plus a read-only browser of the workspace memory snapshot grouped by type (User / Feedback / Project / Reference / Other). Each entry has an inline delete icon that calls `DELETE /workspaces/:hash/memory/entries/:relpath`. Below the browser is a **Clear all memory** button that calls `DELETE /workspaces/:hash/memory/entries` (bulk) after a confirmation dialog — wipes every entry for the workspace but leaves the enabled flag untouched.
- **Knowledge Base tab:** enable/disable toggle (persists immediately to `WorkspaceIndex.kbEnabled`). When enabled, the workspace-group header in the sidebar shows a dedicated **KB** button (next to the existing pencil icon) that opens the full-screen KB Browser — the browser replaces the chat message area rather than living inside the modal.

## Knowledge Base Browser

The **KB Browser** is a full-screen panel that swaps into the main chat area (hiding the active conversation) when the user clicks the **KB** icon on a workspace-group header in the sidebar. It is implemented inline in `public/js/main.js` — look for the `chatKbBrowser*` functions.

- **State shape:** `chatKbBrowserState` is a module-scoped variable (null when closed, initialized on open):
  ```javascript
  {
    hash: string,                    // workspace hash
    label: string,                   // workspace label for display
    activeTab: string,               // 'raw'|'entries'|'synthesis'|'reflections'|'settings'
    enabled: boolean,                // KB enabled for this workspace
    autoDigest: boolean,             // auto-digest toggle state
    state: KbState|null,             // full KB state from server
    pollTimer: number|null,          // interval ID for 1500ms polling
    uploading: boolean,              // true while batch upload queue has active/queued items
    selectedFolder: string,          // currently selected folder path ('' = root)
    pandocStatus: object|null,       // { available, binaryPath, version } | null
    substeps: object,                // Map rawId → substep text ("Converting…", "Parsing…")
    processingStartTimes: object,    // Map rawId → Date.now() when processing began (for elapsed timer)
    uploadQueue: {                   // batch upload queue state
      items: QueueItem[],            // { file, folderPath, status, error, retries, xhr }
      concurrency: 3,                // max parallel XHR uploads (hardcoded max 5)
      activeCount: number,           // currently in-flight uploads
      paused: boolean,               // pause flag — in-flight uploads finish, no new dispatches
      totalBytes: number,            // sum of all enqueued file sizes
      uploadedBytes: number,         // bytes completed (uploaded + failed + deduped)
      uploadedCount: number,         // successfully uploaded file count
      skippedCount: number,          // deduped file count
      failedCount: number,           // permanently failed file count
      startTime: number|null,        // Date.now() when first item was enqueued
      fileProgress: Map,             // index → { loaded, total } for in-flight XHR progress
    },
    entries: {                       // Entries tab state
      loading: boolean,
      items: KbEntry[],              // current page from GET /kb/entries
      total: number,                 // total match count (pre-pagination)
      selectedEntryId: string|null,  // row selected on the left
      entryBody: string,             // fetched body for the right panel
      filters: {
        search: string,              // title substring
        tags: string[],              // AND-selected tag list
        uploadedPreset: string,      // 'all'|'today'|'7d'|'30d'|'90d'|'custom'
        uploadedFrom: string,        // '' or 'YYYY-MM-DD' (only when preset='custom')
        uploadedTo: string,
        digestedPreset: string,
        digestedFrom: string,
        digestedTo: string,
      },
      page: number,                  // 1-indexed
      pageSize: number,              // default 50, selector: 25/50/100
      allTags: Array<{tag,count}>,   // one-shot fetch from GET /kb/tags
      allTagsLoaded: boolean,
      tagPickerOpen: boolean,
      tagPickerQuery: string,        // in-dropdown tag search
      searchDebounceTimer: number|null,
    },
    synthesis: object|null,          // synthesis snapshot from GET /kb/synthesis
    graphInstance: object|null,      // ForceGraph3D instance reference
    selectedTopicId: string|null,    // clicked topic in graph
    topicDetail: object|null,        // fetched topic detail for panel
    embedding: object|null,         // cached embedding config from GET /kb/embedding-config
    reflections: {                   // Reflections tab state
      loading: boolean,
      items: KbReflectionSummary[]|null,
      selectedId: string|null,
      detail: KbReflectionDetail|null,
      typeFilter: string,            // 'all'|'pattern'|'contradiction'|'gap'|'trend'|'insight'
    },
    _dreamTriggeredAt: number|null,  // timestamp for 15-second grace period after Dream/Re-Dream click
  }
  ```
- **Entry point:** `chatKbBrowser.chat-conv-group-kb-btn` click handler (wired in `chatWireEvents`) reads `data-kb-hash` and `data-kb-label` off the button and calls `chatOpenKbBrowser(hash, label)`. The button is only rendered for workspace groups that have a real hash **and** whose `workspaceKbEnabled` flag is `true` (so the "ungrouped" placeholder never gets one, and workspaces with KB disabled in settings don't advertise an entry point). The flag is sourced from `ConversationListItem.workspaceKbEnabled` in the sidebar payload; toggling KB on/off via the settings modal calls `chatLoadConversations()` so the sidebar button appears/disappears without a page reload.
- **Layout:** A `<div class="chat-kb-browser">` sibling of `#chat-messages` with a header (workspace label + KB counter pills + close button), a tab row (**Raw** / **Entries** / **Synthesis** / **Reflections** / **Settings** — all five wired), and a tab-specific body. Opening the browser hides `#chat-messages` and the input area; closing restores them.
- **Header counters:** The header renders pills for `raw: N`, `entries: N`, and `pending: N` sourced from `state.counters` on every refetch. Counter text updates in place during polling so the user sees counts tick up as uploads/digests complete without scrolling or tab-switching.
- **Polling:** While the browser is mounted, `chatKbBrowserState.pollTimer` runs `chatKbBrowserRefetch()` every **1500 ms** against `GET /workspaces/:hash/kb?folder=<selectedFolder>` (or `GET /kb/entries?folder=…` on the Entries tab). During an active batch upload, the effective poll interval is stretched to **3000 ms** to reduce DOM thrash (implemented via a `_lastPollTick` timestamp guard inside the 1500ms `setInterval`). Polling is required because WS frames are conversation-scoped — the browser can be open for a workspace that has no active CLI stream. On close, the timer is cleared.
- **WS frame reactivity:** `streaming.js` dispatches `kb_state_update` frames to `window.chatHandleKbStateUpdate(convId, event)`. The handler checks whether the browser is open for the same workspace hash and, if so, triggers an immediate refetch (in addition to the polling cadence) so changes during active CLI streams render without waiting for the next poll tick.
- **Toolbar:** A single row above the tab body carries the workspace-wide controls:
  - **Auto-digest switch** — reads `state.autoDigest`, flips via `PUT /kb/auto-digest`. Changing the switch does not kick off any work itself; it only changes how subsequent uploads and deletes behave.
  - **+ Folder** button — prompts for a folder path (accepts nested paths like `ops/weekly`), hits `POST /kb/folders`, selects the new folder in the tree on success.
  - **Digest All Pending** button — disabled when `counters.pendingCount === 0`. Label is `Digest All Pending (N)` so the badge count is always visible. Clicking fires `POST /kb/digest-all`. A **digest progress indicator** renders to the left of the button showing unified aggregate progress across batch, single-file manual, and auto-digest runs — sourced from `kb_state_update` frames carrying `digestProgress: { done, total, avgMsPerItem, etaMs? }` (and from `GET /kb`'s `digestProgress` field on initial mount or after a reload, so a long-running digest rehydrates without losing accuracy). The indicator format is `"N / M items — ~E min remaining"` when ETA is available, falling back to `"N / M items"` before two tasks have settled. Entity count (from `digestion: { active, entriesCreated }` frames) is appended as a `· N entities created` suffix during active digestion. When the queue drains the server emits `digestProgress: null`, which clears the indicator. `digestion: { active: false, entriesCreated }` still populates `chatKbBrowserState.digestComplete`, surfacing a green **"Digestion complete — N entities created"** banner at the top of the Raw tab with an `×` dismiss button (clears `digestComplete`, re-renders). The banner persists across tab changes within the browser session and across raw-list polls; leaving the KB Browser resets the state.
    - **ETA formatting** — `chatKbFormatEta(ms)` renders `< 60 s` as `${secs}s`, `< 60 min` as `${mins} min`, and longer as `${hours}h ${mins}m` (or `${hours}h` on the minute boundary). Paired with `chatKbFormatDigestProgress(dp)` which composes the full indicator string; returns `''` when `dp` is falsy so the caller can unconditionally concatenate.
- **Raw tab:** Two-column layout — a **folder tree sidebar** on the left and a **main content area** on the right.
  - **Folder tree** — rendered from `state.folders` as a flat list indented by `folder_path` depth. Root is the implicit top node. Each folder row has a **chevron toggle** (non-leaf rows only — leaf rows render a fixed-width spacer for column alignment), its path label, and hover-revealed **Rename** / **Delete** buttons (rename prompts for a new path → `PUT /kb/folders`; delete warns about cascading children → `DELETE /kb/folders?folder=…&cascade=true`). Clicking a folder sets `chatKbBrowserState.selectedFolder` and immediately refetches the raw list scoped to that folder; selecting a deep folder auto-expands every ancestor so the selected row is always visible.
    - **Collapse / expand** — `chatKbBrowserState.collapsedFolderPaths` is a `Set<string>` of paths whose children are hidden. Empty by default (everything expanded). Root (`''`) has no chevron and cannot be collapsed. Clicking the chevron toggles the path in the set and calls `chatKbBrowserRenderTab()`; descendants of any collapsed path are filtered out of the rendered tree (the render function walks each candidate path's proper ancestors and skips it if any ancestor is in the set). The chevron rotates 90° in the `expanded` state and carries `aria-expanded="true"|"false"`. State is in-memory only — closing and reopening the KB browser resets to fully expanded.
    - **Scroll preservation** — the sidebar container (`.chat-kb-folder-tree`) scrolls independently (`overflow-y: auto; max-height: 60vh;`). Its `scrollTop` is saved before each full `innerHTML` swap and restored after, alongside the entries list and entry detail scroll positions, so the 1500 ms poll cadence and WS frames don't reset the user's scroll position while they browse.
  - **Breadcrumb** — above the file list, `chatKbBrowserFormatBreadcrumb` renders the current folder path as clickable segments (each hops to that ancestor), with `Root` as the leftmost crumb.
  - **Upload zone** — supports multi-file selection, folder selection, and drag-and-drop of files or folders. Two buttons: **"Upload Files"** (triggers `<input type="file" multiple>`) and **"Upload Folder"** (triggers `<input type="file" webkitdirectory>`). For drag-and-drop, the `DataTransferItem.webkitGetAsEntry()` API is used to recursively walk dropped directories via `FileSystemDirectoryReader.readEntries()` (looping until an empty batch since Chrome caps at 100 entries per call). If `webkitGetAsEntry` is unavailable, falls back to `e.dataTransfer.files` (flat, no structure). Folder structure is preserved: for `webkitdirectory` inputs, `webkitRelativePath` is parsed via `kbBuildFolderPath(selectedFolder, relativePath)` to extract the directory portion and prepend the selected folder. The backend auto-creates folders via `createFolder()`, so no pre-creation step is needed. All selected/dropped files are enqueued into `chatKbBrowserState.uploadQueue` and uploaded concurrently (up to `concurrency: 3` parallel XHR requests) against the existing `POST /workspaces/:hash/kb/raw` endpoint. Browser `File` objects are handles (not in-memory buffers), so holding tens of thousands is cheap — actual bytes stream from disk only when the XHR fires.
  - **Upload queue engine** — core functions: `chatKbEnqueueFiles(items)` pushes `{ file, folderPath }` items into the queue and starts the drain loop. `chatKbDrainQueue()` dispatches up to `concurrency` parallel XHR uploads. `chatKbUploadQueueItem(index)` handles a single item's XHR lifecycle. Per-item error handling: HTTP 400/409 (validation, conflict) are non-retryable and mark the item as error. HTTP 401 pauses the queue and shows the session-expired overlay. HTTP 500, network errors, and timeouts auto-retry up to 2 times with backoff (1s, 3s), then mark as error. Responses with `{ deduped: true, addedLocation: false }` are marked as `'deduped'` and increment `skippedCount`. Additional queue control functions: `chatKbPauseQueue()` / `chatKbResumeQueue()` toggle the `paused` flag (in-flight uploads finish naturally), `chatKbCancelQueue()` shows a confirm dialog then aborts in-flight XHRs and clears remaining queued items, `chatKbRetryFailed()` re-enqueues all `status: 'error'` items. `chatKbResetQueue()` clears the queue entirely after a batch is done.
  - **Batch progress UI** — rendered in a `<div id="chat-kb-batch-progress">` container between the upload zone and file list. When the queue is active, shows: (1) an overall progress bar (`uploadedBytes / totalBytes`), (2) summary text ("Uploading 342 of 10,000 files (1.2 GB of 4.8 GB) — 3 active, 12 failed"), (3) ETA after 10 files complete (via `chatFormatEta` from `utils.js`), (4) Pause/Resume and Cancel buttons. When the batch is idle (completed), shows Retry Failed (if any) and Dismiss buttons. An expandable `<details>` panel shows per-file detail rows: failed items pinned at top with error message, in-flight items with per-file XHR progress percentage, and last 10 completed items (showing "Already in KB" for deduped). Max 30 DOM rows at any time (sliding window). Render throttling via `requestAnimationFrame` debounce prevents more than ~60 updates/sec during rapid XHR progress events.
  - **Guards:** A `beforeunload` listener warns when the queue has active/queued items (browser's native "are you sure?" dialog). `chatCloseKbBrowser()` shows a confirm dialog if the queue is active before closing and aborting in-flight XHRs. Concurrency is hardcoded at 3 (max 5), leaving room for WS + polling among the browser's ~6 connections per origin. No cross-session resume — `File` handles aren't serializable; re-running a folder upload after refresh deduplicates automatically via SHA256.
  - **File list** — each row renders filename, size, relative upload time, the colored status badge (`ingesting | ingested | digesting | digested | failed | pending-delete`), a **Download** anchor (`GET /kb/raw/:rawId`), a **Digest now** button (shown only for `ingested | pending-delete | failed` rows — POSTs to `POST /kb/raw/:rawId/digest`), and a per-location **Delete** button that sends `DELETE /kb/raw/:rawId?folder=<folder>&filename=<name>`. **Row alignment:** the status badge, digest button, and delete button all use fixed widths (`min-width: 140px` on the badge with `justify-content: center`; `width: 32px` on both buttons) and `flex: 0 0 auto` so column positions stay stable regardless of which status text is showing or whether the digest button applies to the current row. The digest button is always rendered in the DOM — when the row's status does not allow digestion, it is given the `chat-kb-raw-digest--hidden` class (`visibility: hidden` + `pointer-events: none` + `aria-hidden="true"` + `tabindex="-1"`) so its slot is reserved without being interactive or keyboard-focusable. `.chat-kb-raw-row` also sets `flex-wrap: wrap` so that the substep line (`.chat-kb-raw-substep`, `flex: 1 1 100%`) and the error line (`.chat-kb-raw-error`, `flex: 1 1 100%`) wrap onto a new visual row beneath the filename/badge/buttons line during processing or failure states — keeping the main row's badge and action-button column alignment consistent with idle rows. **Status badge colors:** `ingested` = green (`#7fd4a6` on `#1f3e32`), `digested` = blue (`#82b1ff` on `#1a2f4a`), `ingesting`/`digesting` = purple pulse. **Processing progress:** when a raw item is in `ingesting` or `digesting` state, two pieces of UI appear: (1) an **elapsed-time counter** (e.g. `13s`, `3m 42s`) rendered inline on the main row as `<span class="chat-kb-raw-elapsed">` positioned between the meta span and the status badge — it stays on the main row so processing rows keep the same height as idle rows, and the badge/buttons column stays right-aligned; (2) a **substep text** line (e.g. "Converting…", "Running CLI analysis…", "Parsing entries…") from `kb_state_update` `substep` frames, rendered only when substep text is non-empty as a `.chat-kb-raw-substep` div that wraps beneath the main row via `flex: 1 1 100%` (enabled by `flex-wrap: wrap` on the row). Start times are tracked in `chatKbBrowserState.processingStartTimes[rawId]` and cleared when the status changes. The elapsed counter ticks forward because `chatKbBrowserRenderTab()` re-renders on the poll interval (1500ms / 3000ms). Failed rows expand to show the `errorClass` + `errorMessage` from the state entry. Pending-delete rows render a muted italic treatment to distinguish them from normal ingested rows. While `chatKbBrowserState.uploading` is true (batch queue has active items), `chatKbBrowserRenderTab()` returns early for the Raw tab so poll timer and WS frames cannot replace innerHTML and orphan the live batch progress DOM. Scroll position is preserved across re-renders by saving/restoring `scrollTop` on the `.chat-kb-browser` container (the actual overflow parent), not on `.chat-kb-raw-main`.
- **Entries tab:** Two-column layout — a filtered+paginated list of digested entries on the left (with a filter bar above and pagination controls below), a detail pane on the right. Scroll position is preserved across re-renders by saving/restoring `scrollTop` on the inner scrollable elements (`.chat-kb-entries-list` and `.chat-kb-entry-detail`), not just the outer `#chat-kb-browser` container.
  - **State:** `chatKbBrowserState.entries = { loading, items, total, selectedEntryId, entryBody, filters, page, pageSize, allTags, allTagsLoaded, tagPickerOpen, tagPickerQuery, searchDebounceTimer }`. `filters = { search, tags[], uploadedPreset, uploadedFrom, uploadedTo, digestedPreset, digestedFrom, digestedTo }`. `pageSize` defaults to `50` and the selector offers `25 / 50 / 100`.
  - **Filter bar** (`chatKbBrowserEntriesToolbar`): Four filter controls, all combining with AND semantics.
    - **Title search** — `<input type="search">` bound to `filters.search`, debounced **300 ms** via `setTimeout` stored on `entries.searchDebounceTimer`. Each change resets `page` to `1`.
    - **Tag picker** — a `Tags (N)` button opens a dropdown (`.chat-kb-tag-dropdown`) populated from `GET /kb/tags` (fetched once per browser open, cached on `entries.allTags`). The dropdown includes an in-list search input (`entries.tagPickerQuery`) so long tag lists remain navigable. Selected tags render as removable chips below the filter rows (the `×` button clears the chip). **Multi-tag semantics are AND** — an entry must carry every selected tag — and a small hint line in the dropdown reminds the user. Outside-click closes the dropdown (wired on each render while `tagPickerOpen` is true).
    - **Uploaded date** — `<select>` with presets `All time / Today / Last 7 days / Last 30 days / Last 90 days / Custom…`. Presets resolve to ISO bounds at refetch time via `chatKbBrowserResolveDateRange(preset, from, to)`. Picking `Custom…` reveals two `<input type="date">` pickers; the `from` input is floored to 00:00:00 and the `to` input is widened to 23:59:59.999 so the full day is inclusive. Filters against the joined `raw.uploaded_at` column server-side.
    - **Digested date** — same preset set; filters against `entries.digested_at` server-side.
    - **Clear filters** — shown only when any filter is active. Resets `filters` to defaults, clears `tagPickerOpen`/`tagPickerQuery`, snaps `page` to 1, refetches.
  - **List refetch** (`chatKbBrowserRefetchEntries`): Builds a query string via `chatKbBrowserBuildEntriesQuery` (search, tags as csv, uploadedFrom/To, digestedFrom/To, limit, offset) and hits `GET /kb/entries?<qs>`. Response is `{ entries, total }`. If `page > ceil(total / pageSize)` (filter change narrowed results past current page), the refetch snaps `page` back to 1 and re-fetches once. Empty-state copy distinguishes "No entries yet" (no filters active) from "No entries match these filters" (filters active).
  - **List rendering** — each row shows title + summary + tags (max 6 tag badges). Rows are still ordered by title server-side. Clicking a row calls `GET /kb/entries/:entryId` to populate the detail pane.
  - **Pagination** (`chatKbBrowserEntriesPagination`): Below the list. `← Prev` / `Next →` buttons (disabled at edges), a center info span (`N–M of T · Page P of Q`), and a `Page size` `<select>` (25 / 50 / 100). Changing page size or page triggers a refetch. The page state lives on `chatKbBrowserState.entries.page` and survives tab switches inside the same browser session.
  - **Detail pane** — renders the entry title, summary, tags, and the full `entry.md` body. YAML frontmatter is stripped client-side before rendering so the user only sees the prose. A "parent raw" link below the title jumps back to the Raw tab and scrolls to the source row.
  - **Folder filter** — the Entries tab honors the same `selectedFolder` state the Raw tab uses, so clicking a folder in the Raw tree and then switching to Entries shows only the entries whose parent raw has a location in that folder (via the `raw_locations` join inside `listEntries`).
- **Delete semantics:** Per-location deletes on the Raw tab hit `DELETE /kb/raw/:rawId?folder=<f>&filename=<n>` and respect ref-counting — deleting the last location of a raw file always fully purges it (bytes + converted + entries + DB row), regardless of auto-digest setting. Folder-cascade deletes follow the same rule.
- **Download:** The Download link is a plain `<a href="/api/chat/workspaces/:hash/kb/raw/:rawId" target="_blank">` — the server sets `Content-Disposition: inline` so the browser either previews or downloads based on MIME type.
- **Synthesis tab:** Interactive 3D WebGL force-directed graph (via [3d-force-graph](https://github.com/vasturiano/3d-force-graph) + Three.js) with an UnrealBloomPass glow effect, showing topics as nodes and topic-to-topic connections as edges. Dependencies are **lazy-loaded** only when the Synthesis tab opens — zero impact on initial page load. Three.js is loaded first via native ESM `import('three')` (resolved through a `<script type="importmap">` in `index.html` pointing to `https://esm.sh/three@0.183.2`), then assigned to `window.THREE` so that the 3d-force-graph UMD build (`public/js/vendor/3d-force-graph.min.js`, ~1.3MB) reuses the same Three.js instance instead of bundling its own — this shared-instance pattern is critical for bloom to work, because the UMD checks `window.THREE` at init time. The bloom pass (UnrealBloomPass) is loaded via native ESM dynamic `import()` from vendored Three.js addons (`public/js/vendor/three-addons/`), which also resolve `"three"` through the importmap. Loading sequence in `main.js`: `_loadThree()` → set `window.THREE` → `_loadForceGraph3D()` (UMD script injection) and `_loadBloom()` (dynamic `import()`). CSP allows `esm.sh` in both `script-src` and `connect-src`. Action bar above the graph has **Dream** (incremental) and **Re-Dream (full rebuild)** buttons, a status line (last run time, pending count, last error), the dream pipeline stepper (when running), and a search input. Dream/Re-Dream buttons return a 400 error (shown via `chatShowAlert`) when there are no entries to process. Dream/Re-Dream buttons are disabled while a dream is running. The Re-Dream button shows a destructive confirmation dialog before proceeding. The tab auto-fetches synthesis data from `GET /workspaces/:hash/kb/synthesis` on activation. **Dream/Re-Dream grace period:** when the Dream or Re-Dream button is clicked, `chatKbBrowserState._dreamTriggeredAt` is set to the current timestamp. For 15 seconds after that timestamp, the poll timer treats the dream status as "running" without overriding the optimistic UI state — this prevents the 1500ms poll from reverting the button/stepper to an idle state before the server has registered the newly-started dream job.
  - **Graph nodes:** 3D spheres whose size scales by entry count (nodeVal = `4 + 6 * (entryCount / maxEntries)`). God-node topics render in gold (`#ffcc00`); regular topics are colored by **cluster** using label-propagation community detection (`_chatKbComputeTopicClusters`). The algorithm runs up to 12 iterations over the graph, assigning each node the most common label among its neighbors (seeded by degree-descending order, ties broken lexicographically). The resulting cluster IDs map to a 10-color palette: `['#4fd1ff', '#7cf29a', '#ff8a5b', '#c084fc', '#ff5da2', '#ffd166', '#2dd4bf', '#60a5fa', '#f97316', '#a3e635']`. Bloom post-processing adds a neon glow effect to all nodes. Topic title + entry/connection counts are shown via hover tooltip (`nodeLabel`). Renderer uses `{ antialias: true, alpha: false }`.
  - **Graph edges:** 3D links styled by confidence level — `extracted` links are wider (1.2px), `inferred`/`speculative` links are thinner (0.5px). Links are colored by confidence: extracted = `#7df9ff`, inferred = `#5b8cff`, speculative = `#ff6b9a`. Opacity varies by confidence (0.6/0.4/0.2). No directional arrows or animated particles. No edge labels (relationship text is shown in the node click popup).
  - **Topic labels:** Floating HTML labels rendered in an overlay `div.chat-kb-graph-label-layer` on top of the WebGL canvas. Each label is a pill-shaped `div.chat-kb-graph-node-label` positioned via `graph2ScreenCoords()` on every animation frame (`requestAnimationFrame` loop in `tickTopicLabels()`). Labels appear only when the camera is close enough — a dynamic `labelZoomThreshold` is computed from the graph's bounding sphere radius (`max(160, min(320, maxR * 1.35))`). Topic titles are truncated to 28 characters. Labels are hidden for nodes outside the viewport or filtered out by search. The label layer and rAF loop are cleaned up by `_chatKbDestroyGraph()`.
  - **Bloom effect:** UnrealBloomPass with strength=2, radius=0.85, threshold=0.35. Dark background (`#03040f`) for optimal bloom visibility. The bloom pass is added to the graph's built-in `postProcessingComposer()` EffectComposer pipeline. A stale-graph guard after the async bloom load prevents adding the pass to a destroyed graph. Camera centering uses a manual centroid + bounding-sphere computation (`centerGraph()`) called at 300ms (instant), 2s (animated), and on engine stop — `zoomToFit()` is not used due to unreliability.
  - **Legend:** A `chatKbGraphLegendHtml()` function renders a color legend bar above the graph showing: god-node swatch (gold), topic cluster swatches (first 4 of the 10-color palette), and link confidence lines (extracted/inferred/speculative with their colors and glow). Styled as a flex-wrap row with `.chat-kb-graph-legend`.
  - **Graph lifecycle:** `_chatKbDestroyGraph()` tears down the graph instance (pauses animation, calls `_destructor()`), cancels the label rAF loop, and removes the label overlay. Called on KB browser close, tab switch away from Synthesis, and before re-init. The `chatKbBrowserRenderTab()` function short-circuits with an in-place update when the Synthesis tab is active and the graph already exists, avoiding unnecessary re-initialization. Similarly, `chatKbBrowserLoadPandocStatus()` skips re-rendering when the Synthesis or Reflections tab is active to prevent disrupting the live graph.
  - **Interaction:** Nodes are draggable (built-in). WebGL canvas supports orbit controls — left-drag to rotate, scroll to zoom, right-drag to pan. Clicking a node populates the detail panel; clicking the background clears it.
  - **Detail panel:** Fixed 280px right panel beside the graph (`.chat-kb-graph-panel`). Shows "Click a node to view details" when empty. On node click, displays: topic title, god-node star, entry/connection counts, then fetches topic detail from `GET /workspaces/:hash/kb/synthesis/:topicId` and renders an entry list and a connections list (with confidence badges and relationship labels). The panel is scrollable for topics with many entries.
  - **Search:** Text input filters nodes by title — matching nodes keep their cluster/god color, non-matching nodes dim to near-invisible (`rgba(30, 30, 50, 0.15)`). Unrelated edges dim to `rgba(70, 84, 118, 0.18)` color and 0.03 opacity; related edges retain their confidence-based color. Topic labels are also hidden for non-matching nodes. The camera flies to the first matching node via `graph.cameraPosition()` with a 1000ms transition.
  - **Dream pipeline stepper:** when a dream is running, a five-step pipeline stepper appears next to the buttons showing `Routing N/M` → `Verification N/M` → `Synthesis N/M` → `Discovery N/M` → `Reflection N/M` with the active phase highlighted (purple background), completed phases showing a checkmark (green), and upcoming phases dimmed. The stepper dynamically renders whichever phases are emitted (some runs skip Verification if there are no borderline matches; Discovery may have 0 candidates). A two-part elapsed timer appears to the right of the stages: `Total <t> · <PhaseLabel> <t>`, where `Total` counts from when the run first entered `running` and the phase timer counts from when the current phase began. Both values are derived from server-persisted `startedAt` and `phaseStartedAt` timestamps on `dreamProgress`, so closing and reopening the KB window preserves the elapsed time across mount/unmount. The backend emits an initial `done: 0` frame before the first CLI call, so "Starting..." only appears for 1-2 seconds. Progress data comes from `kb_state_update` `dreamProgress` frames with `phase: 'routing' | 'verification' | 'synthesis' | 'discovery' | 'reflection'`. During dream polls, only the stepper and status line are patched in-place (`chatKbSynthesisInPlaceUpdate`) — the 3D graph is not re-initialized.
  - **Stop button:** while a dream is running, a red **Stop** button (stop-sign octagon icon, `.chat-kb-stop-btn`) appears between the Dream and Re-Dream buttons. Clicking it fires `POST /workspaces/:hash/kb/dream/stop` and optimistically sets `chatKbBrowserState.synthesis._stopping = true`. The button then disables and relabels to "Stopping…" with a spinner. A `kb_state_update` frame with `changed.stopping: true` arrives immediately from the backend so the "Stopping…" state is visible before the current batch finishes. The state is also preserved across reloads via the `stopping` field in `GET /kb/synthesis`. When the run actually exits, the normal `synthesis: true` frame (without `stopping`) triggers a refetch that clears `_stopping` and hides the button. Already-committed work from prior batches is preserved; the next Dream/Re-Dream click is the resume mechanism.
- **Reflections tab:** Two-column layout (same pattern as the Entries tab) showing reflection artifacts produced by the Reflection phase of the dream pipeline. Tab button uses a lightbulb icon + "Reflections" label, `data-kb-tab="reflections"`. State is tracked in `chatKbBrowserState.reflections: { loading, items, selectedId, detail, typeFilter }`.
  - **Type filter dropdown** — rendered at the top of the left panel, above the card list. Options: "All Types", "Pattern", "Contradiction", "Gap", "Trend", "Insight". The selected value is stored in `chatKbBrowserState.reflections.typeFilter` and used to client-side-filter the items list before rendering. A reflection count label is rendered next to the dropdown showing the number of visible reflections after filtering. The poll timer skips re-rendering the reflections tab body while the dropdown is open (to prevent the dropdown from closing mid-interaction); it checks `chatKbBrowserState.reflections.typeFilter` state and the panel's own readiness before patching innerHTML.
  - **Left panel** (320px) — scrollable list of reflection cards fetched from `GET /workspaces/:hash/kb/reflections`, filtered by `typeFilter`. Each card renders: a **type badge** colored by reflection type (`pattern` = indigo, `contradiction` = red, `gap` = amber, `trend` = green, `insight` = slate), a **stale badge** if the reflection is marked stale, the reflection **title**, and the **citation count** (number of cited entries).
  - **Right panel** — detail view populated by `GET /workspaces/:hash/kb/reflections/:reflectionId`. Renders: type badge, title, summary (italic), full content (rendered as markdown via `chatKbRenderReflectionMarkdown(md)` with inline citation links), and a **"Cited Entries"** section listing all entries cited by the reflection.
  - **Clickable citation links** — both inline `[Entry: title](entry-id)` citation links inside the rendered markdown and the entries in the "Cited Entries" list are rendered as `<a>` tags with a `data-entry-id` attribute. Clicking either type of link calls `chatKbOpenEntryPopup(entryId)`, which fetches `GET /workspaces/:hash/kb/entries/:entryId` and renders a modal popup showing the full entry content.
  - **Entry popup (`chatKbOpenEntryPopup`)** — modal overlay displaying the full entry detail. At the top, a **"Source:"** section shows the original file's provenance using data from the `locations` array in the entry response: each location renders as a pair of monospace pills — one for the folder path and one for the filename. Below the source section, the entry title and full markdown body are rendered (YAML frontmatter stripped client-side). The popup is dismissed by clicking the backdrop or a close button.
  - **Stale banner** — when one or more reflections are marked stale, a banner appears at the top of the tab body: "N reflections are stale — run Dream to refresh."
  - **Functions:**
    - `chatKbBrowserRefetchReflections()` — fetches the reflection list from `GET /workspaces/:hash/kb/reflections` and updates `state.reflections.items`.
    - `chatKbBrowserRefetchReflectionDetail(reflectionId)` — fetches a single reflection's detail and populates `state.reflections.detail`.
    - `chatKbBrowserReflectionsTab()` — returns the HTML string for the full two-panel Reflections tab layout, including the type filter dropdown and reflection count.
    - `chatKbBrowserWireReflectionsTab()` — attaches click listeners on reflection cards (to load detail), the type filter dropdown (to update `typeFilter` and re-render), and cited entry links (to open the entry popup).
    - `chatKbRenderReflectionMarkdown(md)` — simple markdown-to-HTML renderer for reflection content; converts headings, bold/italic, lists, code blocks, and inline citation links to HTML. Citation links are rendered as `<a data-entry-id="…">` elements.
    - `chatKbOpenEntryPopup(entryId)` — fetches the entry detail (including `locations`) and renders a modal popup with source file info and full entry content.
- **Dream banner** (chat input area): A `#chat-dream-banner` div above the model-select row in the chat input wrapper. Behavior:
  - When `conv.kb.dreamingNeeded` and `pendingEntries > 0`: shows "{icon} N entries awaiting synthesis" + a **Dream now** button.
  - When `conv.kb.dreamingStatus === 'running'`: shows "Dreaming in progress" + the same pipeline stepper used by the Synthesis tab (Routing → Verification → Synthesis → Discovery → Reflection with active/done/pending states and batch counters) + a red **Stop** button (`chatTriggerDreamStop()`) that fires `POST /kb/dream/stop`. Once clicked, the button disables and relabels to "Stopping…" until the run exits. `conv.kb.dreamingStopping` mirrors the backend's stop-requested state so the banner reflects it across reloads.
  - Otherwise hidden.
  - Button click fires `POST /workspaces/:hash/kb/dream` and optimistically sets status to running. Progress updates arrive via `kb_state_update` WS frames with `dreamProgress` field. On completion, a conversation refetch updates the banner state.
  - The banner updates on: conversation selection (`chatSelectConversation`), `kb_state_update` events for the active conversation.
- **Settings tab:** Per-workspace embedding configuration for the PGLite vector search layer. Contains a form with three fields:
  - **Model** — Ollama model name (default `nomic-embed-text`). Text input.
  - **Ollama Host** — Server URL (default `http://localhost:11434`). Text input.
  - **Dimensions** — Embedding vector size (default `768`). Number input, min 1, max 4096.
  - **Test Connection** button — fires `POST /workspaces/:hash/kb/embedding-health` and shows a health indicator: green "Connected", red error message, or "Checking…" spinner.
  - **Save** button — fires `PUT /workspaces/:hash/kb/embedding-config` with the form values. Alerts on success/failure via `chatShowAlert`.
  - Config is fetched from `GET /workspaces/:hash/kb/embedding-config` on first tab activation and cached in `chatKbBrowserState.embedding`. Requires [Ollama](https://ollama.com) running locally. A description paragraph links to the Ollama website.

## File Explorer

The **File Explorer** is a full-screen split-pane view for browsing, previewing, and managing the files in a workspace's working directory. Implemented as a standalone ES module (`public/js/fileExplorer.js`) exporting `chatOpenFileExplorer(hash, label)` and `chatCloseFileExplorer()`. Swaps into the main chat area on open by hiding `#chat-messages` and the chat input wrapper, identical to the KB Browser pattern.

- **Entry point:** A hover-visible folder icon button (`.chat-conv-group-explorer-btn`) rendered in every workspace-group header in the sidebar, next to the KB button. Uses the same opacity-0 → 1 on parent hover behavior as the group settings gear — the button is invisible until the user hovers the group header row, which keeps the sidebar clean. Rendered only when `group.hash` is present (skipped for the "Ungrouped" placeholder). Click dispatches `chatOpenFileExplorer(hash, label)` from `main.js`'s event-delegation router. The button icon is `ICON_FOLDER`.
- **State shape:** `feState` is a module-scoped object (null-free, always initialized on open):
  ```javascript
  {
    hash: string,                    // workspace hash
    label: string,                   // workspace label for header
    currentFolder: string,           // selected folder rel path ('' = root)
    entries: Entry[],                // rows currently rendered in the tree pane (from the tree endpoint)
    selected: { path, type } | null, // the selected row for preview
    preview: {                       // preview pane state
      kind: 'text'|'markdown'|'image'|'oversize-text'|'oversize-image'|'unsupported'|null,
      filename, language, content, size, mtime, src
    } | null,
    expanded: Set<string>,           // dir paths whose children are expanded in-place in the tree
    children: Map<string, Entry[]>,  // cache of `path → entries` populated by lazy tree fetches
    uploads: Array<UploadItem>,      // in-flight / completed XHR upload rows
  }
  ```
- **Layout:** A `<div id="chat-file-explorer" class="chat-file-explorer">` sibling of `#chat-messages`. Header row shows the workspace label and a close button. Body is a CSS grid with two columns: a **tree pane** (`.chat-fe-tree-pane`, `minmax(260px, 2fr)`) and a **preview pane** (`.chat-fe-preview-pane`, `minmax(320px, 3fr)`).
- **Tree pane:**
  - Toolbar with **Up**, **Refresh**, **New Folder**, **New File**, and **Upload** buttons (in that order). The New Folder button prompts for a name, validates client-side (rejects `/`, `\`, `.`, `..`, and empty names before round-tripping), then hits `POST /explorer/mkdir` with `{ parent: <upload target folder>, name }`. The New File button prompts with `untitled.md` as the default; the client trims, applies the same `/`, `\`, `.`, `..`, empty-name guards, auto-appends `.md` when the typed name has no `.` (so users can type bare names but any extension is still accepted), then hits `POST /explorer/file` with `{ parent, name }`. On success the new file is auto-selected, its preview is fetched, and the pane is flipped straight into **Edit** mode so the user lands in the editor ready to type. On 409 the UI surfaces the server's conflict message via the standard alert modal. The "upload target folder" used by Upload, New Folder, and New File is the currently-selected directory row when one is selected, else the current breadcrumb folder.
  - Breadcrumb row showing the current folder path as clickable segments; clicking an ancestor snaps `currentFolder` back.
  - Lazy-loaded directory tree. Expanding a folder row calls `_expandFolder(rel)` which hits `GET /explorer/tree?path=<rel>` and caches the result in `feState.children`. Collapse just removes the path from `feState.expanded` without dropping the cache. Hidden files (names starting with `.`) are **always** rendered — explicit product requirement.
  - Row interactions: single-click selects and populates the preview pane; double-click on a directory expands it. Hover-revealed **Rename**, **Download** (files only), and **Delete** buttons on every row. Rename/Delete fire the `PATCH /explorer/rename` and `DELETE /explorer/entry` endpoints; on 409 conflict for rename, the user is prompted to confirm overwrite, which retries with `overwrite: true`.
  - Drag-and-drop source/target: the pane itself accepts dropped files via `_onDragOver` / `_onDrop`, and directory rows accept drops via `_onRowDragOver` / `_onRowDrop` so users can pick a specific destination folder by dropping onto it. While a drag is active over the pane, `.chat-fe-tree-pane.drag-over` adds an accent border; a row drop target paints `.chat-fe-row.drop-target` as a dashed outline.
  - Upload panel pinned to the bottom of the tree pane (`.chat-fe-upload-panel`). Each `UploadItem` renders a filename, a progress bar (driven by the XHR `upload.onprogress` event), and a status line (queued / uploading / complete / error / conflict-prompt). Uploads run with **concurrency 3** — additional items wait in `queued` state until a slot frees. On 409 the row surfaces an inline "Overwrite?" prompt; confirming re-POSTs with `?overwrite=true`.
- **Preview pane:**
  - `preview.kind === 'text'` renders the file as a syntax-highlighted `<pre><code>` block via highlight.js using `preview.language` inferred from extension. Fetched via `GET /explorer/preview?path=<rel>&mode=view`.
  - `preview.kind === 'markdown'` renders via marked with the same text source.
  - `preview.kind === 'image'` renders an `<img src="/api/chat/workspaces/:hash/explorer/preview?path=<rel>&mode=raw">` inside `.chat-fe-preview-image-wrap`. A client-side **25 MB** size guard (read from the entry's `size` before issuing the fetch) demotes oversized images to `oversize-image` and shows a download-only stub.
  - `preview.kind === 'oversize-text'` appears when the server responded with `413` (file > 5 MB cap) or the entry's own size exceeds the same cap — shown as a message + Download button.
  - `preview.kind === 'unsupported'` covers binary files outside the supported preview set. Shows filename, size, and a Download button.
  - The preview header always shows filename, size, and mtime; an explicit **Download** button sits next to the filename and hits `?mode=download`.
  - **Editor mode** — when `kind` is `text` or `markdown` and the content has loaded, the header exposes an **Edit** button. Clicking it flips `preview.editing = true` and swaps the `<pre>`/markdown body for a full-height `<textarea.chat-fe-preview-editor>` seeded from `preview.content` into a `preview.draft` field (updated live on `input`). The header swaps the **Edit** button for **Save** + **Cancel**. **Save** issues `PUT /explorer/file` with `{ path, content: draft }`, shows the button in a disabled "Saving…" state while the request is in flight, then on success updates `preview.content`/`preview.size`, clears `editing`/`draft`, and reloads the current folder so the tree's size/mtime column reflects the new bytes. Save failures surface the server error via the alert modal and keep the editor open. **Cancel** prompts for confirmation via `chatShowConfirm` when `draft !== content`, then exits edit mode. Markdown files render as markdown in view mode and as raw source in the textarea during edit — the textarea uses monospace + `white-space: pre` + `tab-size: 2`.
- **Upload UX details:** Multiple files can be selected from the file picker or dropped onto the pane. For each `File`, a fresh `UploadItem` is pushed to `feState.uploads` and enqueued; the drain loop (`_runUploadQueue`) keeps at most 3 XHRs in flight at a time. On any XHR `progress` event, the per-item bar updates; on `load` (HTTP 200) the row is marked complete, the tree pane refreshes the affected folder, and the queue advances. On HTTP 409 the row transitions to a prompt state and is excluded from the active-count until the user resolves it (overwrite or cancel).
- **Cleanup:** `chatCloseFileExplorer()` aborts any in-flight upload XHRs, re-shows `#chat-messages` and the input area, and clears `feState`. Also invoked on sign-out and on workspace deletion. Exposed on `window` so the close-button `onclick` in the static header markup can call it.

The related `ICON_FOLDER`, `ICON_FOLDER_OPEN`, `ICON_FILE`, and `ICON_TRASH` SVG constants are defined in `public/js/state.js` alongside the other `ICON_*` constants.

## Theme System

CSS custom properties on `:root` (light) and `[data-theme="dark"]`. Theme applied by setting `data-theme` on `<html>`. Persisted to `localStorage` under `agent-cockpit-theme`. Synced from server settings on init. Listens for system theme changes when set to "system".

## SVG Icon System

All UI icons are inline SVGs using `stroke="currentColor"` for automatic theme compatibility. No icon font or sprite sheet — icons are defined as JavaScript string constants in `public/js/state.js` and imported into whichever module renders them. Source SVG files are also stored in `public/icons/` for reference, but are not loaded at runtime.

**Icon constants** follow the naming pattern `ICON_<NAME>` (e.g. `ICON_SETTINGS`, `ICON_SEND`, `ICON_USER`). Each constant is a complete `<svg>` string with `class="kb-icon" width="14" height="14" viewBox="0 0 24 24"` and `stroke="currentColor"` so they inherit the parent element's text color. KB pipeline icons use the `KB_ICON_` prefix; reflection type icons use the `KB_REF_ICON_` prefix.

**Static HTML elements** (in `index.html`) that cannot use JS constants embed the same SVG markup directly inline — e.g. the sidebar buttons (New Chat, Sessions, Settings, Sign Out, Reset), the attach button, and the initial send button.

**Dynamic elements** rendered via JS template literals reference the constants — e.g. `${ICON_SETTINGS}` in a button's innerHTML. Elements that toggle icons (send ↔ stop button) use `el.innerHTML = ICON_SEND` / `el.innerHTML = ICON_STOP` rather than `textContent`.

**User avatar** uses `ICON_USER` with a special CSS override: `.chat-msg.user .chat-msg-avatar-svg` restores the blue circle background (`var(--accent-chat)`) and white color so the avatar remains visible on both light and dark themes. Other avatar SVGs (backend icons) render without a background circle.

**Current icon inventory** (all in `state.js`):
- **KB pipeline:** `KB_ICON_INGEST`, `KB_ICON_DIGEST`, `KB_ICON_DREAM`
- **Reflection types:** `KB_REF_ICON_PATTERN`, `KB_REF_ICON_CONTRADICTION`, `KB_REF_ICON_GAP`, `KB_REF_ICON_TREND`, `KB_REF_ICON_INSIGHT`
- **General UI:** `ICON_STALE`, `ICON_AI_MODEL`, `ICON_CLI`, `ICON_SERVER`, `ICON_STATS`, `ICON_KB`, `ICON_MEMORY`, `ICON_CANCEL`, `ICON_EDIT`, `ICON_DOWNLOAD`, `ICON_RESET`, `ICON_SIGNOUT`, `ICON_ARCHIVE`, `ICON_SETTINGS`, `ICON_REFLECTION`, `ICON_ATTACHMENT`, `ICON_SEND`, `ICON_STOP`, `ICON_WORKSPACE`, `ICON_TOKEN`, `ICON_USER`, `ICON_FILE_UPLOAD`, `ICON_FOLDER`, `ICON_FOLDER_OPEN`, `ICON_FILE`, `ICON_TRASH`

## Keyboard Shortcuts

- **Enter** — send message (when send behavior is "enter")
- **Shift+Enter** — newline (or send, depending on setting)
- **Ctrl+Shift+D** — download conversation
- **Ctrl+Shift+R** — reset session
